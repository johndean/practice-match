# --- node / frontend ---
node_modules/
frontend/dist/
frontend/playwright-report/
frontend/test-results/
frontend/tests/visual.spec.ts-snapshots/
frontend/tests/dom-snapshots/
coming-soon/dist/
coming-soon/coverage/
frontend/coverage/

# --- python ---
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml

# --- secrets & local env (never commit) ---
.env
.env.local
.env.*.local
.env.prod-backup*

# --- superpowers / sdd scratch ---
.superpowers/

# --- os / editor ---
.DS_Store
*.log

# --- QA click-through screenshots (scratch; never committed) ---
# The controller's browser checks write `qa-*.png` into the repository root before the files are
# moved into the workspace, and `PW_OUTPUT_DIR` sends a persona check's artefacts to the repo
# root's screenshots/ (the runbook's own recipe, run from frontend/). Anchored (review,
# HOUSEKEEPING-C fix round 1, Minor-2): unanchored, `screenshots/` also ignores a directory of
# that name at any depth, which is not what this rule is for.
qa-*.png
/screenshots/

# --- third-party POI exports (Google Places content: never commit) ---
Report_Hospital_Competitor*.csv

# --- git worktrees (never commit) ---
.worktrees/
.playwright-mcp/
