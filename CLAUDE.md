# CLAUDE.md — Practice Match

VIN Foundation veterinary practice marketplace (internal working title). Read this, then `.claude/skills/practice-match-workflow/SKILL.md` (the craft), before substantive work.

## Environments

| | URL | Railway env | Backend | Use |
|---|---|---|---|---|
| QA | https://qa.foundation.vin | `QA` | own PostGIS + Redis (isolated) | verify everything here first; test freely |
| Production | https://foundation.vin | `production` | own PostGIS + Redis | stakeholders' real data once Sub-project 2 ships |

`GET /api/healthz` on either host returns `environment`, `version`, `commit_sha`, `db.postgis_version`, `redis.ok`. **John does not run the app locally** — the loop is code → `scripts/deploy.sh QA` → verify on qa.foundation.vin → `scripts/deploy.sh production` → smoke on foundation.vin.

Every environment variable is set only in Railway (per service, per environment) — see `.env.example` for the full list and `DEPLOY.md` for how each is set. `CENSUS_API_KEY` (worker only, Sub-project 3; John holds it) is the one that must never appear in git, chat, or a CI log — same rule as `API_SECRET_KEY`, just worth naming. `SITE_MODE` (`app` | `coming_soon`) selects which built site the api serves; production runs `coming_soon` until launch; QA never does.

> ### 🚦 ALWAYS confirm the Railway target before uploading or changing anything
> This machine runs 5+ Railway projects; `railway up` ships to whatever is linked. Before ANY `railway up`, variable change, or service mutation run `railway status` and read it back — it must say **Project: Practice Match**. `scripts/deploy.sh` enforces this; do not bypass it with a bare `railway up`. deploy.sh uploads a `git archive` of the source's HEAD, never the working directory; a linked worktree must be passed as SOURCE_DIR (`scripts/deploy.sh QA .worktrees/<branch>`), because the CLI resolves a worktree's `.git` pointer file back to the main checkout and would otherwise ship main's tree (P14, `DEPLOY.md`). Never pass `--project` from memory. Never set a global `RAILWAY_TOKEN`.

## Source of truth for the UI

`docs/design-reference/design_handoff_practice_match_v3/Practice Match V3.dc.html` is the approved design. Rules, each violated by an assistant somewhere before:

- **Reference open first, port verbatim, absent beats faked.** No invented UI, no placeholder banners, no "TODO Phase X", no simplifications.
- **Ported files are byte-identical** except the edits listed in `docs/superpowers/specs/2026-09-06-browse-v3-mobile-design.md` §3, which `frontend/tests/app-generated.test.ts` enforces byte for byte. `logic.js` is never restructured. Inline styles stay inline. No CSS framework, no Pinia, no per-screen split without a visual diff per screen.
- **`npm run test:visual` is the arbiter.** Baselines are generated from the reference in the same run (`npm run test:visual:baselines`). Tolerance is zero: `maxDiffPixels: 0` beside `threshold: 0.1` (`playwright.config.ts`) — no pixel may differ, where a pixel counts as differing when its per-pixel YIQ distance is above 0.1. Relaxing either requires a recorded reason.
- The design-system cascade matters: `frontend/index.html` links `colors_and_type.css`, `preview/_preview.css`, `ui_kits/vin/kit.css` in that order, before the app styles.

The V3 file is the approved design **plus the local amendments listed in `docs/design-reference/design_handoff_practice_match_v3/LOCAL_AMENDMENTS.md` (spec D15)**: the pristine bundle file is kept beside it as `Practice Match V3.rev2.dc.html` and is never edited, `frontend/tests/design-amendments.ts` is the machine-readable list of ruled edits, and `frontend/tests/design-amendments.test.ts` proves pristine + amendments == the amended file byte for byte. Amendment **A1** (John, 2026-09-07: "keep the V2 header and do not restyle header or fonts") restores V2's display typography on 24 template elements — every display-size heading paired with V2 by tag, text and size takes V2's `text-transform`/`letter-spacing` values in place, and the key-fact values `{{ m.v }}` and the 28 px mobile asking price return to V2's uppercase `.005em` — so the thirteen non-Browse screens (`mobile-list`, `mobile-detail`, `detail`, `requests`, `seller-dash`, the four `wizard-*`, the four `admin-*`) were byte-identical to V2 again, hashes and all, **until the launch removal** (A6, 2026-09-07, ruled D-I8-6) took the prototype jump bar off the top of every screen: `frontend/tests/baseline-manifest.json` now holds the post-launch-removal hashes and is the within-worktree leak detector it was built to be — from here a moved hash means a CODE change moved a screen the design did not. The other three families are literal script or template edits: **A2** (spec D17 — the mobile practice card opens the detail; **A2.2–A2.5** then delete the `browseSel` orphans C13 left behind, including the unwired top-level `selectMarker`, under the bundle's own dead-code rule — the wired `mobileVals.selectMarker` is untouched), **A3** (spec D18 — "View full market report" → "View full listing") and **A4** (spec D21 — Compare hides the "What this means" card). Wave 2a's Task I8a added three more families of literal edits: **A5** (the prototype wiring — `signIn`/`signOut` through the `auth` adapter, `componentDidMount` reading the loaded account, and the `startGate` and `me` prototype props the reference is driven by), **A6** (the launch-removal list below) and **A7** (spec §Sign-in — "VIN username or email" → "Email"). The account-screens branch widened **A7** with **A7.3–A7.4** (spec §4.1/§4.2 — the sign-in card's footer gains "Forgot your password?" beside "Request access", and its sub-line drops the VIN language) and added two more families: **A8** (thirteen literal edits, A8.1a–A8.8b, John, 2026-09-08 — "approved" — composes the sign-up, check-email, verify-expired, forgot, reset, reset-expired, accept-invite, applicant-answer and re-apply cards from the V3 gate card's own elements, and widens `startGate`'s enum to all thirteen gate values) and **A9** (A9.1a/b, controller amendment A-S5, 2026-09-08 — one more declared prototype prop, `startAnswerNote`, so the reference renders the applicant-answer card's seeded note exactly as `startNotice` renders the sign-in ones). **A10** (John, 2026-09-08 — the sign-in card's second gate point, "Sellers control location & disclosure"; A8–A9 were reserved by the account-screens branch while this was ruled on `main`, and both families are above now), revised the same day by A10.2 ("Sellers control what buyers can see"). **A11** (John, 2026-09-08 — unifies the docked panel's other-tabs CTA with the Insights tab's: "Open full listing" → "View full listing", the unification A3's row had flagged). **A12** (John, 2026-09-08 — the Seed Listings launch: "Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA"; eleven literal script edits — A12.1–A12.5 so `practiceName`, both `photoSet` branches, `heroSrc` and `thumbSrc` read a listing's own `name` and `photos` (the thumbnail takes the second photograph, as the design's own thumbnail is a second view), A12.6–A12.7 so the detail's Community Context tolerates the `growth`/`hh` the API leaves null under D4, A12.8–A12.9 so the detail's subtitle and its "General location" row name the listing's OWN state through the design's `stateOf(market)` helper instead of a hard-coded `", TX"`, and A12.10–A12.11 so a listing with no community figures reaches the design's own "Community data unavailable" card instead of a four-tile grid of blanks under the Census attribution — the design's own fixtures are all in the Austin metro and carry no name, no photos and non-empty figures, so every approved state keeps its pixels). **A15** (John, 2026-09-09 — every uploaded photograph renders, with its own description: "surface all images uploaded and have the user articulate what it is and render ALL images"; six literal script edits — A15.1/A15.2 so both `photoSet` branches read a photograph's OWN `p.photoCaptions[i]` (the API's new `photo_captions`, `migrations/090_listing_photo_captions.sql`) with the design's fixed slot caption as the fallback, and A15.3a–A15.3d so each branch appends one tile per photograph beyond the sixth, captioned "Photo N" where nobody has described it — the design's six captions are fixed per practice type, so before this a photograph could only be shown by matching one of them and only 73 of the 195 in John's folders were rendered; his fixtures carry no `photos` and no `photoCaptions` at all (`p2`'s three photographs are the design's `SRC` map, keyed by slot id), so both guards are falsey and every approved state keeps its pixels.). **A16** (John, 2026-09-08 — the seller wizard and dashboard read and write the real API: "none of the existing Photes and Documents are being render4ed in the 'EDIT' of an existing listing … none of the actual inputs are appearing in the PREVIEW and SUBMIT … this gap must be corrected and the UX true"; twenty-two literal script edits — A16.1–A16.3 so the dashboard renders the seller's OWN listings and Continue/Edit/View open the row's real draft, A16.4–A16.6 so step 6 lists the listing's real photographs and documents, "Add files" adds a photograph or a document through the browser's own dialogs uncapped (A-SL20) and Continue saves the step, A16.7–A16.9 so Submit and the dashboard's three transitions go through the API and the bootstrap loads the seller's listings, A16.10 for the ruled `declined` pill, A16.11a/b for the eighth declared prototype prop `startMyListings` — the reference's only way to the EMPTY dashboard A-SL17 added to the oracle — A16.12/A16.13 so the preview counts photographs rather than files and names the listing's own state rather than Texas, A16.14/A16.15 (controller amendment A-SL23, on the SL7 review) so "Create a listing" creates one instead of silently re-opening the listing last edited and "Save and exit" saves the step it is on, and A16.16/A16.17 (A-SL25, on the SL7 re-review) declaring `wizAssets` and `creating` in the design's own state literal and adding the two helpers every adapter path shares — `openDraft`, the one place a fetched draft becomes the wizard's state, and `reloadListings`, the one loader, which carries the rejection arm its callers kept forgetting, and A16.18 (A-SL27 (3), on the round-3 re-review) so the step rail saves the step it leaves before it moves — in the adapter's partial mode, which A16.15's Save-and-exit now uses too (A-SL27 (2)): a blank required number is left out of a save that is not Continue rather than sent as `""` and refused, since a draft is incomplete by nature — while `toWizardState` leaves a null column alone so a just-created listing opens on the design's own four defaults instead of four blank selects (A-SL27 (1)), and the step → fields map both sides read is one data file, `frontend/src/listings/step-fields.json`, pinned two-way by pytest without parsing any TypeScript — and A16.19 (A-SL29 (1), on the round-4 re-review) so the Back button saves the step it leaves the same way, the rail's twin round 3's count of "the one navigation control that silently discards work" missed, and A16.20a/A16.20b (A-SL30 (3), on the round-5 re-review's Info-15) so the two remaining doors out of the wizard also save: the header nav (`go`) in the rail's own shape, and Sign out, which ATTEMPTS the same save and ends the session regardless of the answer — a session end is the seller's explicit act and is never held hostage to a save nobody asked for. With an adapter present the dashboard renders the loaded rows or NO rows and the wizard renders the fetched draft or NO tiles — never the design's four Austin fixtures and never its three fixture photographs, whatever the API answered (A-SL23 (2), A-SL25 (1)); the reference and the Claude Design preview pass no adapter and carry no listings, and the oracle answers the app with those same four rows, and with the design's own three photograph tiles as the created listing's draft, as real pages (`frontend/tests/design-seller-listings.mjs` and `design-wizard-draft.mjs`, both derived from `logic.js` as the D6 stub is derived from `P`), so every approved state keeps its pixels). A13 and A14 are the dropdown branch's, reserved while this was built. Fourteen families, 110 entries: A1's 24 derived edits plus 86 literals; `LOCAL_AMENDMENTS.md` carries one row per amendment and the test pins the set both ways (count and ids). `docs/design-reference/design_handoff_practice_match_v2/Practice Match V2.dc.html` remains the **pre-V3 oracle** — what a suspected regression is diffed against, not what the gates compare to. Every visual and DOM oracle is regenerated from the amended V3 design. `header-1100` and `header-1000` are not among the thirteen: they are Browse screenshots, and `mobile-map`, the Browse states and the two header states change by design.

## Non-negotiables (from po.vin / rounds.vin, still true here)

- **Surgical diffs.** The change contains the ask and nothing else. Never remove a function or feature while doing unrelated work. No drive-by refactors or reformatting.
- **No destructive actions** without explicit instruction in the current conversation.
- **Verification gate before every production deploy — all four:** (1) `poetry run pytest` + `npm run typecheck && npm test && npm run build`; (2) `npm run test:visual:baselines` then `npm run test:e2e` (the `app` project: visual + DOM oracle + smoke) green — the DOM oracle runs under neither `test:smoke` nor `test:visual`, and it is the secondary proof of zero regression on the thirteen non-Browse screens (byte-identical to V2 from V13 until the launch removal, A6/D-I8-6, moved all 28 states by ruled design change); (3) click-through on https://qa.foundation.vin of the changed flow; (4) post-deploy smoke on https://foundation.vin (`scripts/verify-deploy.sh production`). Verified = ship; no need to ask.
- **Push every commit to both remotes:** `origin` (vin-swe/practice-match) and `production` (johndean/practice-match). Conventional commits; trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- **Versions in lockstep:** `frontend/package.json` and `pyproject.toml`, one patch per release (`tests/test_versions.py`).
- **Close the loop:** forwardable plain-language summary + screenshots of the live screens + a one-line engineer's note with any risk.

## Legally load-bearing (from the approved design and the Census spec)

- **Attribution stays visible** on every map ("Tiles © Esri" today; whatever the VIN Foundation's final basemap licence requires) and under Community Context ("Source: U.S. Census Bureau, …"). Attribution strings will come from `dataset_registry.attribution_text`, not hard-coded, once Sub-project 3 lands.
- **Blocked datasets never ship.** Pet-ownership incidence (licence unresolved) and third-party practice-location data must not be ingested or displayed until the VIN Foundation clears them. The admin Data Sources tab shows this gate; keep it.
- **Open licence question — one decision record.** Esri (the approved design) vs CARTO (the Census spec) is **one** open question, recorded in the Census plan (`docs/superpowers/plans/2026-09-05-practice-match-census-data-layer.md`, "Basemap licence — one decision record") and referenced, not restated, here. Do not swap either way without the VIN Foundation's decision.

## Launch-removal list (executed in Task I8a, with real auth)

**Done, as ruled design amendments** (A6.1–A6.6, A7.1–A7.2 — the list left the DESIGN, so the oracle and the app lost it together and every gate kept holding): the prototype jump bar markup and its script (`jumps`, `jumpTo`, `showPrototypeBar`) · the "Prototype — access states" shortcuts and their `gateStates` array · the pre-filled demo credentials and the pre-filled application · the jump bar's "Mobile view" toggle and the now-dead `viewportLabel`/`toggleViewport`.

**Still there, deliberately.** All eight prototype props stay **declared** and none is passed by the app (D-I8-2): `frontend/tests/app-generated.test.ts` requires `app.setup.js` to declare every prop the design declares, so dropping one fails that gate. `startScreen`, `startGate` and `me` are the REFERENCE's way into a state — `frontend/tests/reference-server.mjs` injects them per request from `?props=`, which is how one navigation puts it signed in on the named screen. `prototypeBar` is declared for that parity check alone: A6.4b removed the only thing that ever read it. Wave 2a's account screens added two more the same way: `startNotice` (A8.8b) hands the reference the five sign-in-card notices, and `startAnswerNote` (A9.1a/b, controller amendment A-S5) hands it the applicant-answer card's seeded note — both default to `""`, both reached through the same `?props=` mechanism, and the app never passes either. Wave 2b's seller lifecycle added the eighth the same way: `startMyListings` (A16.11a/b, controller amendment A-SL22 (1)) hands the reference the seller's loaded listings, which is the only way to the EMPTY dashboard A-SL17 put in the oracle — it defaults to `null`, "nothing was handed over", and the app never passes it either. `startViewport` the app takes from the URL only (`?viewport=mobile`), which is how the prototype's 390×800 phone frame stays reachable until a responsive design exists (D-I8-7; the toggle that used to set it lived in the jump bar). Fixture data in `logic.js` (`P`, `MARKETS`, `VETS`, `ECON_K`, `sellerListings`, `requests`, admin rows, `state.me`) also stays — keep field names; the UI reads them: the API replaces `P` and `MARKETS` at boot (`frontend/src/main.ts` → `frontend/src/listings/load.ts`), and the fixture arrays stay in the script as the D6 stub's source for the gates.

## Layout

`frontend/` Vue app · `frontend/tests/` Playwright (`screens.ts` = the 44 approved states) · `app/` FastAPI (`api/health.py`, `static.py`, `checks.py`, `tasks/celery_app.py`) · `migrations/` numbered SQL (ledger runner `scripts/migrate.py`) · `seeds/` the demo hospital data and photographs (`hospitals.json`, `hospitals/photos/`) · `scripts/` `start.sh` (roles api|worker|migrate|seed), `deploy.sh`, `verify-deploy.sh`, `verify-image.sh`, `seed_listings.py`, `prepare_photos.py` · `tests/` pytest · `docs/design-reference/` the handoff bundle (never shipped) · `docs/superpowers/{specs,plans}/`.

## Common operations

```bash
docker compose -f docker-compose.dev.yml up -d && poetry run pytest -q -W error --cov=app --cov=scripts --cov-branch --cov=tests/e2e --cov-fail-under=100   # backend gate, exactly as CI runs it (tests/e2e is the Playwright api under test, A-SL29)
cd frontend && npm run typecheck && npm test && npm run build                  # frontend gates
docker compose -f docker-compose.dev.yml up -d && cd frontend && npm run test:visual:baselines && npm run test:e2e   # oracles from V3, then visual + DOM + smoke (the app project starts the API against the compose Postgres/Redis)
scripts/deploy.sh QA && scripts/deploy.sh production                           # after the gate
python scripts/seed_listings.py                                                # seed/refresh the QA demo hospitals — run inside the api container (see DEPLOY.md "Seeding the demo hospitals (QA)")
railway logs --service api --environment QA --lines 50
railway variable list --service api --environment QA --json | python3 -c 'import sys,json; print("\n".join(sorted(json.load(sys.stdin))))'   # names only — never pipe values to a terminal
```
