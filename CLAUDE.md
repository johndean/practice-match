# CLAUDE.md — Practice Match

VIN Foundation veterinary practice marketplace (internal working title). Read this, then `.claude/skills/practice-match-workflow/SKILL.md` (the craft), before substantive work.

## Environments

| | URL | Railway env | Backend | Use |
|---|---|---|---|---|
| QA | https://qa.foundation.vin | `QA` | own PostGIS + Redis (isolated) | verify everything here first; test freely |
| Production | https://foundation.vin | `production` | own PostGIS + Redis | stakeholders' real data once Sub-project 2 ships |

`GET /api/healthz` on either host returns `environment`, `version`, `commit_sha`, `db.postgis_version`, `redis.ok`. **John does not run the app locally** — the loop is code → `scripts/deploy.sh QA` → verify on qa.foundation.vin → `scripts/deploy.sh production` → smoke on foundation.vin.

Every environment variable is set only in Railway (per service, per environment) — see `.env.example` for the full list and `DEPLOY.md` for how each is set. `CENSUS_API_KEY` (worker only, Sub-project 3; John holds it) is the one that must never appear in git, chat, or a CI log — same rule as `API_SECRET_KEY`, just worth naming. `SITE_MODE` (`app` | `coming_soon`) selects which built site the api serves; production runs `coming_soon` until launch; QA never does.

> ### 🚦 ALWAYS confirm the Railway target before uploading or changing anything
> This machine runs 5+ Railway projects; `railway up` ships to whatever is linked. Before ANY `railway up`, variable change, or service mutation run `railway status` and read it back — it must say **Project: Practice Match**. `scripts/deploy.sh` enforces this; do not bypass it with a bare `railway up`. deploy.sh uploads a `git archive` of the source's HEAD, never the working directory; a linked worktree must be passed as SOURCE_DIR (`scripts/deploy.sh QA .worktrees/<branch>`), because the CLI resolves a worktree's `.git` pointer file back to the main checkout and would otherwise ship main's tree (P14, `DEPLOY.md`). Never pass `--project` from memory. Never set a global `RAILWAY_TOKEN`.

## Source of truth for the UI

`docs/design-reference/design_handoff_practice_match_v3/Practice Match V3.dc.html` is the approved design. Rules, each violated by an assistant somewhere before:

- **Reference open first, port verbatim, absent beats faked.** No invented UI, no placeholder banners, no "TODO Phase X", no simplifications.
- **Ported files are byte-identical** except the edits listed in `docs/superpowers/specs/2026-09-06-browse-v3-mobile-design.md` §3, which `frontend/tests/app-generated.test.ts` enforces byte for byte. `logic.js` is never restructured. Inline styles stay inline. No CSS framework, no Pinia, no per-screen split without a visual diff per screen.
- **`npm run test:visual` is the arbiter.** Baselines are generated from the reference in the same run (`npm run test:visual:baselines`). Tolerance is zero: `maxDiffPixels: 0` beside `threshold: 0.1` (`playwright.config.ts`) — no pixel may differ, where a pixel counts as differing when its per-pixel YIQ distance is above 0.1. Relaxing either requires a recorded reason.
- The design-system cascade matters: `frontend/index.html` links `colors_and_type.css`, `preview/_preview.css`, `ui_kits/vin/kit.css` in that order, before the app styles.

The V3 file is the approved design **plus the local amendments listed in `docs/design-reference/design_handoff_practice_match_v3/LOCAL_AMENDMENTS.md` (spec D15)**: the pristine bundle file is kept beside it as `Practice Match V3.rev2.dc.html` and is never edited, `frontend/tests/design-amendments.ts` is the machine-readable list of ruled edits, and `frontend/tests/design-amendments.test.ts` proves pristine + amendments == the amended file byte for byte. Amendment **A1** (John, 2026-09-07: "keep the V2 header and do not restyle header or fonts") restores V2's display typography on 24 template elements — every display-size heading paired with V2 by tag, text and size takes V2's `text-transform`/`letter-spacing` values in place, and the key-fact values `{{ m.v }}` and the 28 px mobile asking price return to V2's uppercase `.005em` — so the thirteen non-Browse screens (`mobile-list`, `mobile-detail`, `detail`, `requests`, `seller-dash`, the four `wizard-*`, the four `admin-*`) were byte-identical to V2 again, hashes and all, **until the launch removal** (A6, 2026-09-07, ruled D-I8-6) took the prototype jump bar off the top of every screen: `frontend/tests/baseline-manifest.json` now holds the post-launch-removal hashes and is the within-worktree leak detector it was built to be — from here a moved hash means a CODE change moved a screen the design did not. The other three families are literal script or template edits: **A2** (spec D17 — the mobile practice card opens the detail; **A2.2–A2.5** then delete the `browseSel` orphans C13 left behind, including the unwired top-level `selectMarker`, under the bundle's own dead-code rule — the wired `mobileVals.selectMarker` is untouched), **A3** (spec D18 — "View full market report" → "View full listing") and **A4** (spec D21 — Compare hides the "What this means" card). Wave 2a's Task I8a added three more families of literal edits: **A5** (the prototype wiring — `signIn`/`signOut` through the `auth` adapter, `componentDidMount` reading the loaded account, and the `startGate` and `me` prototype props the reference is driven by), **A6** (the launch-removal list below) and **A7** (spec §Sign-in — "VIN username or email" → "Email"). The account-screens branch widened **A7** with **A7.3–A7.4** (spec §4.1/§4.2 — the sign-in card's footer gains "Forgot your password?" beside "Request access", and its sub-line drops the VIN language) and added two more families: **A8** (thirteen literal edits, A8.1a–A8.8b, John, 2026-09-08 — "approved" — composes the sign-up, check-email, verify-expired, forgot, reset, reset-expired, accept-invite, applicant-answer and re-apply cards from the V3 gate card's own elements, and widens `startGate`'s enum to all thirteen gate values) and **A9** (A9.1a/b, controller amendment A-S5, 2026-09-08 — one more declared prototype prop, `startAnswerNote`, so the reference renders the applicant-answer card's seeded note exactly as `startNotice` renders the sign-in ones). **A10** (John, 2026-09-08 — the sign-in card's second gate point, "Sellers control location & disclosure"; A8–A9 were reserved by the account-screens branch while this was ruled on `main`, and both families are above now), revised the same day by A10.2 ("Sellers control what buyers can see"). **A11** (John, 2026-09-08 — unifies the docked panel's other-tabs CTA with the Insights tab's: "Open full listing" → "View full listing", the unification A3's row had flagged). **A12** (John, 2026-09-08 — the Seed Listings launch: "Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA" — his words of the day, kept as he said them; the set is TWENTY-NINE since Task SD1 added his eleven Dallas hospitals on 2026-09-11, and `seeds/hospitals.json` is the count of record, never this sentence; eleven literal script edits — A12.1–A12.5 so `practiceName`, both `photoSet` branches, `heroSrc` and `thumbSrc` read a listing's own `name` and `photos` (the thumbnail takes the second photograph, as the design's own thumbnail is a second view), A12.6–A12.7 so the detail's Community Context tolerates the `growth`/`hh` the API leaves null under D4, A12.8–A12.9 so the detail's subtitle and its "General location" row name the listing's OWN state through the design's `stateOf(market)` helper instead of a hard-coded `", TX"`, and A12.10–A12.11 so a listing with no community figures reaches the design's own "Community data unavailable" card instead of a four-tile grid of blanks under the Census attribution — the design's own fixtures are all in the Austin metro and carry no name, no photos and non-empty figures, so every approved state keeps its pixels). **A13** (John, 2026-09-08 — "must fix this drop-down to be an acutal drop-down vs the popup following the same design logic of a normal dropdown"): the Browse toolbar's metro `<select>`, which on macOS opened the operating system's popup menu, becomes a trigger and a `role="listbox"` panel composed from the Market data card's own layer menu (`md.toggleLayerMenu`'s trigger and its panel) and anchored beneath the field like the "More filters" popover in the same toolbar row; eight literal edits — `setMarket` moves to a class property beside `setF` with its state transition unchanged, `renderVals()` gains the menu's state, styles, keyboard handler and rows, the markup is replaced, `componentDidMount`/`componentWillUnmount` arm and remove the Escape and outside-click listeners the design has never had, and A13.6–A13.7 delete the two render-value orphans the `<select>` left behind (`market:` and the rows' `v: m`) under the bundle's own dead-code rule, as A2.2–A2.5 did for `browseSel`. The focused trigger is a `role="combobox"` — the role ARIA 1.2 supports `aria-activedescendant` on, `button` not being among them (final review I1) — carrying `aria-activedescendant` and `aria-controls`, the option rows carry `tabindex="-1"` so focus stays on the element that holds the highlight, the highlighted row is scrolled into view both when the arrow keys move it and when the panel opens, and a choice — mouse or keyboard — returns focus to the trigger rather than dropping the user on `<body>`, because the seeded metro list is longer than the panel (Q5). A13.8 (final review m4) gives the listbox the Tab dismissal A14.7 gave the Give menu, in the same shared `focusout` closure and applied after it; the five filter selects were left native here and that scope was overruled on 2026-09-11 (family **A26**, John: 'the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design'), which converted them and — in the same family's Task F2 — the three inside the More-filters popover on this same idiom, each panel taking the width of the trigger that opened it so their edges line up (A26.16, John, 2026-09-11); the results-rail sort select (defect D-F1, a control that displays an order the list never takes) becomes its own change, wired as well as converted (John, 2026-09-11), and the wizard's four field selects convert later as their own change (John, 2026-09-11), so until that lands the product deliberately carries two dropdown behaviours — Browse in-design, the wizard native — which is a ruled state and not a drift. **A14** (John, 2026-09-08 — "the Give button must be identical to the https://vinfoundation.org/ where the button is an actual drop down (match button design pixel-by-pixel)") replaces the header's inert Give button with the live site's Give dropdown, MEASURED on vinfoundation.org rather than composed from this design's tokens: a `#339dde` pill that turns `#07386f` when hovered or open, a Font Awesome `angle-down` chevron inlined at the live 11.25 × 18 px box, a 3 px `#339dde` underline 4.34 px beneath it, and a square white panel with a `1px solid #07386f` border listing Annual Fund, Cor Group, Legacy Giving and the Dr. Sophia Yin Memorial Fund — John's four hrefs, in the same tab, because the live site's carry no `target`. Eight literal edits: A14.1 `giveFocus` beside `money(n)`, A14.2 the render values, A14.3 the markup, A14.4/A14.5 the Give branches inside A13's shared `trackMenuDismiss` closures (placed so A13's metro dismissal is unchanged, which a characterisation case proves), A14.6 the one `@font-face`, A14.7 a third closure in the same function so Tab out of the menu closes it, and A14.8 the nav toggle's own `giveMenu: false`. Opening any one of the four menus closes the other three (final review m7): A14.2's `toggleUserMenu` and both of Give's open paths, A13.2's `toggleMarketMenu` and A14.8's `toggleNavMenu` carry the six directions between them, rather than the global `pointerdown`/`focusout` pair being the only thing that held the invariant. Home and End reach the Give trigger as they do the metro one (m6). The arrow keys seed a pending index that the panel's own mount ref spends, rather than focusing inside a `setState` callback: the app's `setState` runs its callback synchronously and Vue has not rendered the panel yet, so the callback form worked on the React reference and did nothing in the app — A13's `marketPanelRef` idiom, for the same trap (review round 1, C1). On the plan's blocking Q1 John ruled the same day: "Self-host Montserrat 600 under the SIL Open Font Licence, scoped exclusively to the Give button and its menu. Keep the rest of the design typography unchanged" — so the bundle now ships `assets/fonts/Montserrat-SemiBold.woff2` with its `OFL.txt`, the app carries a byte-identical copy under `frontend/public/assets/fonts/` and the same rule in `src/styles/global.css` under the `assets/` → `/assets/` rewrite, and the design names the family in exactly three `font-family` declarations — the `@font-face` itself and A14.2's two style strings — and the test walks every other `font-family:` in the file and fails if any of them names anything but the bundle's own faces, so no other element's face can change. Click-to-open, Arrow/Home/End, Escape and outside-click are John's ruling, not measurement: the live menu opens on hover and has none of them. The Give button is in the header of every DESKTOP screen, so 40 of the 45 baselines re-based and eleven of `baseline-manifest.json`'s thirteen hashes were re-pinned under the ruling — the same mechanism A6's launch removal used; `mobile-list` and `mobile-detail` did not move, because the prototype's 390 × 800 phone frame renders its own header, which is the proof the change reached nothing else. **A15** (John, 2026-09-09 — every uploaded photograph renders, with its own description: "surface all images uploaded and have the user articulate what it is and render ALL images"; six literal script edits — A15.1/A15.2 so both `photoSet` branches read a photograph's OWN `p.photoCaptions[i]` (the API's new `photo_captions`, `migrations/090_listing_photo_captions.sql`) with the design's fixed slot caption as the fallback, and A15.3a–A15.3d so each branch appends one tile per photograph beyond the sixth, captioned "Photo N" where nobody has described it — the design's six captions are fixed per practice type, so before this a photograph could only be shown by matching one of them and only 73 of the 195 in John's folders were rendered; his fixtures carry no `photos` and no `photoCaptions` at all (`p2`'s three photographs are the design's `SRC` map, keyed by slot id), so both guards are falsey and every approved state keeps its pixels). **A16** (John, 2026-09-08 — the seller wizard and dashboard read and write the real API: "none of the existing Photes and Documents are being render4ed in the 'EDIT' of an existing listing … none of the actual inputs are appearing in the PREVIEW and SUBMIT … this gap must be corrected and the UX true"; twenty-four literal script edits — A16.1–A16.3 so the dashboard renders the seller's OWN listings and Continue/Edit/View open the row's real draft, A16.4–A16.6 so step 6 lists the listing's real photographs and documents, "Add files" adds a photograph or a document through the browser's own dialogs uncapped (A-SL20) and Continue saves the step, A16.7–A16.9 so Submit and the dashboard's three transitions go through the API and the bootstrap loads the seller's listings, A16.10 for the ruled `declined` pill, A16.11a/b for the eighth declared prototype prop `startMyListings` — the reference's only way to the EMPTY dashboard A-SL17 added to the oracle — A16.12/A16.13 so the preview counts photographs rather than files and names the listing's own state rather than Texas, A16.14/A16.15 (controller amendment A-SL23, on the SL7 review) so "Create a listing" creates one instead of silently re-opening the listing last edited and "Save and exit" saves the step it is on, and A16.16/A16.17 (A-SL25, on the SL7 re-review) declaring `wizAssets` and `creating` in the design's own state literal and adding the two helpers every adapter path shares — `openDraft`, the one place a fetched draft becomes the wizard's state, and `reloadListings`, the one loader, which carries the rejection arm its callers kept forgetting, and A16.18 (A-SL27 (3), on the round-3 re-review) so the step rail saves the step it leaves before it moves — in the adapter's partial mode, which A16.15's Save-and-exit now uses too (A-SL27 (2)): a blank required number is left out of a save that is not Continue rather than sent as `""` and refused, since a draft is incomplete by nature — while `toWizardState` leaves a null column alone so a just-created listing opens on the design's own four defaults instead of four blank selects (A-SL27 (1)), and the step → fields map both sides read is one data file, `frontend/src/listings/step-fields.json`, pinned two-way by pytest without parsing any TypeScript — and A16.19 (A-SL29 (1), on the round-4 re-review) so the Back button saves the step it leaves the same way, the rail's twin round 3's count of "the one navigation control that silently discards work" missed, and A16.20a/A16.20b (A-SL30 (3), on the round-5 re-review's Info-15) so the two remaining doors out of the wizard also save: the header nav (`go`) in the rail's own shape, and Sign out, which ATTEMPTS the same save and ends the session regardless of the answer — a session end is the seller's explicit act and is never held hostage to a save nobody asked for — and A16.21/A16.22 (A-SL25 (10), Task SL7b) so the step-6 tile re-describes an EXISTING photograph, seeded ones included, by clicking it: the tile's own `describe` (photographs only, one chained promise into `wizErr`) routes by the tile's own `source` to `caption()` for an asset or the new positional route `PATCH /api/seller/listings/{id}/photos/{n}` for a seed entry — the SAME overloaded adapter method the browser's own prompt already is — and one template literal wires the tile's `onClick` to it. With an adapter present the dashboard renders the loaded rows or NO rows and the wizard renders the fetched draft or NO tiles — never the design's four Austin fixtures and never its three fixture photographs, whatever the API answered (A-SL23 (2), A-SL25 (1)); the reference and the Claude Design preview pass no adapter and carry no listings, and the oracle answers the app with those same four rows, and with the design's own three photograph tiles as the created listing's draft, as real pages (`frontend/tests/design-seller-listings.mjs` and `design-wizard-draft.mjs`, both derived from `logic.js` as the D6 stub is derived from `P`), so every approved state keeps its pixels). **A17** (Task SL8; D24 and John's standing rule, verbatim: "every Admin tab must show real database data, never dummy rows"; two literal script edits — A17.1 is A16.1's own shape applied to the Admin Listings tab's rows, and A17.2 is A16.9's, one line after A16.11b's own bootstrap load: with an `adminListings` adapter present the tab renders the loaded review queue or NO rows — never the design's own five literal rows, whatever the API answered — and the reference and the Claude Design preview pass no adapter and keep the design's fixture path unchanged. The oracle holds `admin-listings`'s frozen pixels through the SUCCESS path: `frontend/tests/harness.ts` answers `GET /api/admin/listings` with all FIVE of the design's own Listings rows, "Flagged" included — that row's status backs no real `listing.status` value and inventing one is out of scope (spec §13), which is a limit on the LIVE mapping's `PILLS`/`ACTIONS` tables, never on this oracle-only fixture — derived from `logic.js`'s own fixture by `frontend/tests/design-admin-listings.mjs`). **A18** (John, 2026-09-09 — "the arrow icons are backwards on each location, reverse each"; screenshots of the docked panel's "View full listing" button and the detail's "Back to results" link): `navigate-arrow.svg` points left unrotated, and the design's own way to point it right is `transform: rotate(180deg)` (the docked panel's Next arrow); the Insights-tab CTA carried it unrotated after its label and the Back link carried it rotated before its label, so each pointed away from where it goes. Two literal template edits — A18.1 adds the rotation to the CTA's arrow, A18.2 removes it from the Back link's — and no SVG changes: the docked panel's prev/next pair and the two sign-out arrows (the account menu's row and the phone frame's pill — John's open question, D-A18) share the glyph and are untouched. `browse-market-panel`, `detail` and `interest-modal` re-based; `detail` is the one frozen row that moved and was re-pinned under the ruling, the other twelve — the two phone-frame captures included — unchanged. **A19** (John, 2026-09-09 — "the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close"): every photograph a member can see — the detail's filled tiles, A15's extra tiles included, and the Browse docked panel's current photograph — carries a transparent hit-target that opens a lightbox composed from the design's own elements: the interest modal's scrim at `z-index: 1100`, above Leaflet's attribution and controls at 1000 on Browse; a `role="dialog"` box in the tile frame's declarations; the photograph at natural size, never upscaled, bounded to the viewport; the docked panel's 34 px `nav-arrow-white` prev/next pair verbatim, hidden for a single photograph; its 38 px close button with `close-x-gray` whitened, "Close photo"; and its counter and caption pills verbatim — the caption the photograph's own A15 description, "Photo N" where nobody has described it. Twelve literal edits (A19.1–A19.12): the state keys, the five class members (open, close, the photo list the carousel already counts, step with wrap-around, the render values), the render key, the two openers, the two hit-targets on the `hasSrc`/`hasAny` branches only (an empty slot's click on the reference is the design tool's file chooser), the overlay once at the root, the Escape/ArrowLeft/ArrowRight branch in A14.5's shared `key` closure and a focus trap in A13.8's `out` closure — no new listener — and `go()`/`signOut` clearing it; `frontend/src/router/useStateRouteSync.ts` clears it on Browser Back, the one leg the reference (no router) cannot carry. Focus enters through the mount-ref idiom (A13/A14, review C1), returns to the opener on close; `e.currentTarget` is new to the design. The results-rail thumbnails do not open it (a card click is the selection) and the phone frame has no photograph. Three approved states appended (`detail-lightbox`, `browse-panel-lightbox`, `detail-lightbox-next`), all through Round Rock, the design's own three-photograph fixture; the frozen manifest did not move — the closed lightbox is one unmounted `sc-if` and a button that paints nothing — which is the proof, checked after A18's one-row re-pin. Two fix rounds followed review. **Fix round 1 (A-LB2, John's ruling on the L2 review)** pinned the docked panel's own `.rf-scroll` scroll in `atTop()` (a real flake, not load noise) and added the Step 10 note to A19.2/A19.10's rows; building the third finding's gate, it walked real Chromium, found A19.10's `out`-closure focus trap does not hold — forward Tab off the last control reaches real page content before self-correcting, Shift+Tab from the first never wraps — and stopped rather than write a test against the defect. **Fix round 2 (A-LB3)** ruled the trap out: A19.10's `focusout` branch is REMOVED (a null `relatedTarget` cannot tell a window blur from focus leaving the dialog's own tabbable set, so no threshold fixes it), and A19.9's shared `keydown` closure now also owns Tab — reading the dialog's controls in DOM order and wrapping deterministically at both ends, one code path, no timer, no `relatedTarget` — with an e2e gate proving a full cycle in both directions. A19.10's row stays, now a documentation-only comment at the closure head recording that a trap was tried and retracted, not never attempted. **A21** (controller amendment A-C28, 2026-09-10, controller amendment A-C29, 2026-09-10) addresses missing census data (Task B8 review): A21.1 removes the `|| 0` defaults from `communities()` so missing figures return `undefined` instead of zero; A21.1b skips those undefined values at the assembly point in `marketVals()` so they never reach `bucket()` or `fmtMetric()` and the renderer never displays "undefined" or "$NaNK" labels. **A21.2 and A21.2b are REVERTED (amendment A-C29)**: the economic figure IS payroll per establishment, not revenue; the metric is merely misnamed `revenue_per_establishment` in the database, and the design's original labels "Average Practice Payroll (CBP)" / "Avg. payroll per practice" were correct. **A21.3 is completed (amendment A-C29) to remove hard-coded years from three places in the design** where the vintage is data-dependent, not fixed: A21.3a the VALUE_LAYERS label, A21.3b the LAYER_META sub-line, A21.3c the Data Layers card's blurb and caption. A21.3d, the detail's Growth row (the real defect where the API now returns "+11.6% since 2018", so the hard-coded strip and label no longer match; the fix splits the API string and reconstructs the sub-line from the year it carries) is applied through the amendment mechanism like A21.3a/b/c. **Task B10 (D-C31 and D-C32, 2026-09-10) adds thirteen more A21 entries and revises A21.2d.** The ROOT CAUSE the earlier entries could not reach was `communities()` itself: `num()` returns 0 for a null and `parseFloat(...) || 0` did the same for growth, so every `!== undefined` guard was satisfied by a 0 and the docked panel read "0" Population, "0.0% (5 yrs)", "$0K" Median Income and a "Flat" growth verdict for a listing the Census has no figures for — A21.1c extends A21.1's treatment to `pop`, `hh`, `income`, `growth` and the `pets` estimate derived from `hh`, which is what makes those guards live. A21.2d's revision makes each overview sub-line `undefined` as a whole rather than leaving its unit behind; A21.2m omits the three competition bars, A21.2n/o take the Market-data strip cards' metro median over the DEFINED values only (a card whose metro has no figure keeps its title, source and link and shows no value and no bars), and A21.2p drops a Compare row's bar where that community has no figure — a bar drawn at its floor is a reading, not an absence. A21.4a–A21.4d put the DESIGN'S OWN "Community data unavailable" card on the docked panel through the detail's own `hasDemo`/`noDemo` idiom (A12.10/A12.11), with the "View full listing" CTA outside both branches because navigation is not data. A21.5a–A21.5d carry D-C32's `community_label` — the API serves it when a listing with no `place`-band FIGURES falls back to its `drive_10` band (`app/census/serve.py`) — into `ApiListing`, `Practice.communityLabel` and the design, and render it in both places that name the area: the panel's Insights heading ("Market Overview (10 min drive)" → the label) and the detail's Community Context card (the Population and Households sub-lines and the attribution paragraph's trailing sentence, the Census attribution itself untouched). Where the label is absent every string is the design's own, byte for byte, and the design's fixtures carry no `communityLabel` key at all, so every approved state keeps its pixels and none of the thirteen frozen hashes moves. A21.5c is the family's one CHAINED entry: its `find` is A12.7's whole `replace`, the A21.3d/A12.6 pattern, so it does not occur in the pristine bundle. **A22** (John, 2026-09-10 — Task SL10: "Preserve existing seed wording/detail"): the wizard's ownership select widens from four options (the design's four) to ten, adding the seeds' own six phrasings alongside the design's four. The API's `OWNERSHIPS` tuple and the design's option array are identical and pinned two-way by pytest (Task SL10 step 1: `tests/api/test_seller_listings.py::test_the_design_ownership_options_equal_ownerships_tuple`); the closed select paints the same so no frozen hash moves. **A23** (John, 2026-09-10 — Task MD1: "the collapse widget top left expand/collapse is disconnected to the drop down"): collapsing the Market data card leaves its LAYER dropdown floating over the map with no card above it. Exactly ONE of the card's two menus escapes the collapse, and it is the one John reported: the layer menu's panel (`frontend/src/App.vue:475-476`) is `position: absolute; left: 16px; top: 118px; z-index: 620` and sits OUTSIDE both of the card's `v-if="v.md?.legendOpen"` templates (`:382-410` and `:413-472`; the card div closes at `:411`), so nothing unmounts it. The comparison listbox never floated: its panel (`:434-435`) is nested inside the second `legendOpen` template in normal flow (`margin-top: 6px`) and has always unmounted with the card — `mdCompareMenu` is cleared all the same, for a weaker and different reason: a menu left open in state reappears already-open when the card is expanded again, which is its own surprise. The clear is UNCONDITIONAL — the one `setState` runs on expand exactly as on collapse, so neither menu can come back open in either direction, which `frontend/src/logic.test.ts`'s four-quadrant characterisation pins. The fix mirrors the design's own idiom where `insightOpen` gates the "What this means" panel on `s.mdLegendOff !== true`, which is why that panel behaves correctly. One literal edit. **A24** (John's rulings D-C34–D-C37, 2026-09-10 — Task 4 of the neighbourhood-shading plan: the Browse map stops painting a grid and starts painting real Census boundaries): `mosaicCells` drew a 3,070-to-12,560-rectangle mosaic of grid cells coloured by the nearest community centroid, which is why the complaint that started this stream was that the map is "just a blob over the whole city". Sixty-three literal edits — fifty-nine in the `.dc.html` and four in `MarketMapV3.jsx`, the family the second-file partition was put in place for (the engine grew `file?: 'dc' | 'jsx'` and a second frozen pristine twin, `MarketMapV3.rev2.jsx`, in Task 1); A28.1 reached that file first, by merging first. Each fill layer now draws at the geography its figure is honest at (D-C35), with the legend naming it: Census tract (`140`) for Median Household Income, Households and Pet Ownership (estimated), place (`160`) for Population Growth, county (`050`) for Average Practice Payroll and ZIP Code Tabulation Area (`860`) for Veterinary Competition. Income moved `860` → `140` on 2026-09-12 (the tract ruling, A24.19/A24.20) and the last three joined on the same day (**D-L1**, John: "households, average practice payroll, veterinary competition and pet ownership (estimated) render NOTHING"): all four painted nothing on QA, because payroll's writer suppressed every county in the country on the CBP NOISE flag (`EMP_N=0;PAYANN_N=0` is noise level zero — published cleanly — and `materialize._cbp_suppression` is now the one decision both writers make about that column) and the other three had no `SHADING` entry at all, so the route answered `422 BAD_LAYER` and the app correctly drew nothing. They were kept as graduated symbols at the listing point on the grounds that "city-scale class breaks on small areas carry no information", which was true of the BREAKS and never of the geography: `AREA_LAYERS` (A24.25) is a SECOND class-break table for the three COUNT layers, cut over the distribution the MAP paints — 85,381 US tracts (the design's `[10000, 25000, 45000]` put **100.0 %** of them in one class; `[1000, 1500, 2000]` takes 21.9/31.4/26.0/20.7 %), the same times 0.57 for pets, and 4,720 US ZIP areas carrying a 541940 count (the design's `[3, 6, 10]` left its FIRST class empty; `[4, 6, 10]` takes 37.0/36.1/21.4/5.5 %) — while `VALUE_LAYERS` is untouched, so the snapshot strip, the Compare rows, the graduated symbols and the docked panel keep their own community-scale classification and their own pixels. Competition is at the ZIP area because ZIP Code Business Patterns is published per ZIP code and exists at no other geography, which is the one exception the Census spec's §6 grants, and the response, the legend and the tip all name it; §15's "N veterinary practices within this ZIP Code Tabulation Area" is in the tip, never a bare number, and §9's "this is a modelled estimate" is in pets'. The legend claims no ramp and no geography when ZERO polygons were drawn (A24.32), which is what a metro in no CBSA gets — though NOT while a metro's areas are still loading (A24.41/A24.42, fix round 1), because a legend that disappears and returns is a flicker rather than a state. **Fix round 1** (the review of e984c85..304b80f, 2026-09-12) added A24.33–A24.42 and is where the layers became honest: `areaSet` did not know the design's own field aliases (`hh`, `vets`), so `households` drew 503 of 503 polygons in the no-data grey UNDER a full four-class ramp naming a geography — the exact statement A24.32 exists to prevent, and the characterisation case that looped the layer checked only the legend, never the fill (A24.33); `LAYER_META.competition` still named County Business Patterns, a 2023 release and "community level" for a layer served as `zbp`/`2022`/ZCTA, so one tooltip named two datasets (A24.34), and income's and households' source lines still said "community level" for tract layers (A24.35/A24.36); and the ZIP threshold was invisible — ZIP Code Business Patterns publishes no count for a category with fewer than three establishments, though it counts them in its all-industry TOTAL, so `app/census/zbp.py` now loads that total (`NAICS 00`) in the same pass, `geo_metric` writes a third state (`suppressed: true, suppress_reason: "source_threshold"`) for a ZIP area the Census withheld, and the tip says which rule hid it in `app.api.market.THRESHOLD_RULE`'s own words, pinned across both sides (A24.38); before it, 393 of Dallas's 535 ZCTAs — 73 % of that map — read "No data for this area" over cells the Census had deliberately withheld, the mirror image of the CBP over-suppression this same branch fixed. The served floor of three is also why the first competition class is labelled "3" and not "1–3" (A24.37): a class that promises a 1 or a 2 is false precision, and `scripts/measure_area_breaks.py` is the committed re-derivation of every break in `AREA_LAYERS`. A metric that SHADES the map no longer also draws its own graduated symbols (A24.40), so one legend describes what is drawn. **A24.43** is ruling MS1, recorded on 2026-09-05 and never fixed until now: the Market snapshot strip reported Dallas Population growth as "+1.5% metro median" while every Dallas listing's own figure is −1.5 % — the design's `num` helper stripped every character but digits and a dot, which loses a leading minus and glues on the year that follows ("-1.5% since 2018" → 1.52018), and `stripCards` is the one growth reader that goes through it while `communities()` keeps the sign with `parseFloat`, which is why the docked panel was right and the strip was wrong about the same city. It reads the first number in the string and nothing after it, sign included, and keeps the design's zero-for-null contract. **Fix round 2** (A24.44–A24.57, 2026-09-12, ruling D-C50's interim) separates the two surfaces that were sharing one sentence: the snapshot strip's Households card read "162K metro median · U.S. Census ACS 5-year estimates (2023) · Census tract", where the 162K is the median of the LISTINGS' own five-mile-ring totals (`stripCards` reads one row per listing) and a Census tract holds about 1,500 households — A24.34–A24.36 were right to make `LAYER_META.*.source` name the MAP's geography, and the strip was borrowing their string. Each layer whose line named a geography now carries the DATASET alone and `metaSource(k, basis)` composes the rest — the map's geography for the legend, the tip and the map's community notes, the practice-area label the API already serves for the snapshot (all twelve Dallas listings carry "Within about 5 miles of the practice"; the design's own fixtures carry none, so the reference path reads "community level" exactly as it does today) — one string per fact, nothing duplicated to serve two readers. The strip's footnote's FIRST sentence becomes one true of both surfaces while A24.20's growth caveat stays beside it byte for byte — the sentence A24.56's own first draft swallowed, restored in fix round 3 because growth was then the one layer whose snapshot card still read "· community level" and the paragraph is where that is said — growth carries its own `dataset:` from Task SNAP fix round 1 (A31.12c, 2026-09-13), so that card and the map legend now read "· Place (city/town)" and the caveat stands on its own merits rather than as cover for a vaguer line — which re-bases `browse-market-strip` and nothing else, and the threshold tip drops the lead-in A24.38 shipped in front of `THRESHOLD_RULE`: the ruling was one sentence and no other new copy. The same round took the design's SECOND number parser out: `communities()` read a growth figure with its own regex, which glued the trailing year onto the digits ("+14.2% since 2015" → 14.22015), and it now calls `num()` like everything else (A24.58) — the sign survives because A24.43 taught `num` to keep it, which was the only reason a second parser existed. A polygon with no usable figure takes a neutral `#e6e6e6` class at the same `fillOpacity` as every other and is NEVER omitted (D-NS16, John 2026-09-10) — a hole in a choropleth reads as a boundary, not as an absence — and the legend gains one row reading exactly `No data`; grey means UNMEASURED and only that, since a figure whose margin spans a band is still shown with its value (D-C36). Every polygon enters by ONE door, `areaVals`, which takes its colour from the design's own `bucket()` and its label from its own `fmtMetric()`, so the fill and the legend cannot disagree; the `rf-tip` is built ONCE, in `areaTip`, instead of twice by hand in the design and in `MarketMapView.vue`. The geometry is a committed fixture, `frontend/tests/design-boundary-fixture.ts`, generated from real `geo_area` rows by `scripts/export_design_boundaries.py` — 88 ZCTAs, 72 places and 5 counties whose centroid is inside CBSA 12420, simplified at 0.010° to fit a 120 KB cap and then REPAIRED (`ST_MakeValid` inside `ST_CollectionExtract(…, 3)`; D-C49, John 2026-09-11) so all 165 are valid and none is a GeometryCollection; the values are the design's own nine Austin community figures assigned to the nearest polygon, which is `mosaicCells`'s own assignment rule on real boundaries and invents no number. The committed bytes are the artefact and are pinned by SHA-256, because `ST_SimplifyPreserveTopology` is NOT reproducible (five distinct geometries from six identical queries) and A24.1 interpolates the literal verbatim into the approved design file, so a casual regeneration would move every map baseline for no design reason. **A24.13** is the family's other ruling, **D-C46** (John, 2026-09-11 — the growth class breaks are re-scaled to real ACS data, with a band below zero, so a declining area reads as declining rather than as the bottom of a growth scale): `stops: [10, 20, 35]` could not represent data that runs roughly −5 % to +15 % and had no band below zero at all — measured against ACS 2014–2018 against 2019–2023 place populations, the exact pair `population_growth_pct` divides, it put 79.9 % of US places of 10,000 people or more into ONE bucket while 30.5 % of them were declining, so real polygons would have shipped still one colour; the stops become `[0, 5, 15]` with buckets `Declining / 0–5% / 5–15% / > 15%`, the tertiles of the non-declining half of that distribution rounded to legend-readable numbers, and FOUR buckets rather than five because the growth ramp carries exactly four colours in all three palettes. `frontend/src/map/mosaic.js` and its nine cases are deleted; `MarketMapView.vue` draws `props.areas` through Task 2's `geoJson` engine primitive on the same shared canvas renderer. Seventeen approved states re-base — the fifteen desktop Browse captures, `browse-market-strip` among them, and the phone frame's `mobile-map` and `mobile-sheet` — and `baseline-manifest.json`'s thirteen frozen hashes did not move, which is the proof the change stayed inside Browse. **A24.14–A24.18** (Task 10) then wire that same drawing to the real API, in the seam A16 and A17 established: an app-only `market` adapter prop the reference never receives (`frontend/src/market/boundaries.ts`, resolving the metro by NAME through `/api/markets` because `logic.js` speaks in market names and knows no CBSA geoid), a `mdAreas` state key, one loader `loadAreas` above `componentDidMount`, and A16.1's own ternary in `md.areas` keyed on adapter PRESENCE rather than on data — with it the map draws the polygons `GET /api/markets/{cbsa}/boundaries` answered **or none at all**, whatever it answered, and never the design's fixture, because that fixture is the AUSTIN metro carrying the design's own nine figures and drawing it over a real metro would show the wrong city's outlines under numbers nobody measured. The loader clears before it asks and discards an answer for a market the member has since left, so neither a metro change nor two fetches resolving out of order can leave one city's boundaries painted over another's. **A refusal empties the shading and nothing else** — `MarketMapView.drawOverlay` paints the C7 drive ring before it reaches the polygon layer and `drawPins()` is a separate call, so the basemap, the pins, the rail, the filters and the detail all still render — which makes the endpoint's PRESENCE an operational precondition: this adapter must not reach an environment ahead of the route. Where the route exists but holds no values yet, the API still answers with real outlines and null values and those paint in the design's own `No data` grey. Zero approved states re-base: the oracle answers the app with the design's own polygons, derived from `areaSet` by `frontend/tests/design-boundaries.mjs` as the D6 stub is derived from `P`. **A25** (John, 2026-09-10 — Task MP1: "a listing with no coordinates keeps its place in the results and does not get a pin"): a published listing whose seller has not disclosed its location is served `lat: null, lng: null` (`app/api/listings.py`’s `serialise`; `location_disclosed` is `NOT NULL DEFAULT false` in `migrations/016_listing.sql`, so the nulls are the DEFAULT, not an edge case), and Leaflet 1.9.4’s `toLatLng([null, null])` returns `null` — the array branch is gated on `typeof a[0] !== "object"` and `typeof null === "object"` — so `_setPos` read `.lat` off a null and ONE such listing took pin drawing down for every listing after it, re-throwing from inside Leaflet’s own event loop on every later zoom pass. Six literal script edits: A25.1 the pin list, A25.2 the drive-ring centre, A25.3 the map’s community list and A25.6 the drive ring itself each skip a listing with no FINITE point (`Number.isFinite`, not `!= null`: a `NaN` reaches `toLatLng` intact) — the third because `mosaicBbox` takes `Math.min`/`Math.max` over those coordinates and `null` coerces to 0, which stretched the Austin metro box to the equator and the prime meridian, 100 million mosaic cells and a hung tab; A25.4 stops `marketPanel`’s last-resort community object standing in zeros for absent figures (D-C31, the one place in the panel A21 could not reach); and A25.5 gives the docked panel the `p8` term A21.4a dropped, so the panel and the detail agree about the design’s own "Community data unavailable" fixture. A25.6 is fix round 1's own (Important-1, "no point, no ring"): `showDrive` was `!!sel` with no coordinate term, so A25.2's restored else-branch made the C7 ring PAINTABLE for the first time and a 16 km dashed "ten minutes' drive" circle appeared around the middle of Austin — a missing pin omits, a ring centred on a place the practice is not fabricates. The listing itself keeps its place in the rail, the count, the sort and the filters — only the map skips it, and nothing is drawn at [0, 0] or at the metro centre, because the design has no treatment for "somewhere in this metro". Every design fixture and all eighteen seeds carry a point, so no approved state moves. **A27** (John, 2026-09-11 — rulings D-C38 and D-C39, the Community Context card’s geography). He was shown a Dallas practice headed "Specialty practice — Highland Park / affluent central" whose card read the same four numbers as every other Dallas listing, South Dallas included — median income $67,760 on all twelve — because all twelve sit inside one Census place and `serve.py`’s whole-row rule (D-C32, written for the Orlando listing in unincorporated Orange County and never asked what it does to a listing INSIDE a large city) served the place band to every one of them, while each practice’s own catchment figures sat materialised in `market_metric` and unreachable by any screen. He chose the option in which EACH TILE NAMES WHERE ITS OWN NUMBER COMES FROM, against the cheaper one that relabels every tile uniformly — which he rejected because it moves the defect one tile over rather than fixing it, the Growth number still being the whole City of Dallas under a caption saying otherwise. So `community_rows` now chooses per figure: the three area figures and the off-card `vets` come from the catchment band with `place` as the fallback, AS ONE GROUP (one `community_label` describes all of them, and a group drawn half from each band would put a city figure under a ring caption); growth and payroll are taken from whichever band carries them, because neither can vary by band at all — `materialize.py` computes growth once per listing OUTSIDE the band loop and writes that one value into all three (D12), and always writes the county CBP row. Two new payload fields say where the figures the label does NOT describe come from: `growth_scope` names growth’s own place-or-county geography with the name TIGER itself gives — "Dallas", "Orange County", no composed "City of " prefix, because level 160 loads the bare `NAME` and a Census designated place is not a city (the controller’s ruling on the implementer’s own concern, 2026-09-11), which needs the geoid lookup `serve.py`’s own docstring used to say was not wanted — superseded deliberately, `practice_location.place_geoid` joined to `geo_area.name` at summary level 160 — and `income_note` carries the approximate qualifier the contract has always asked for and the card had no slot for. **D-C39**: the ring is described by DISTANCE, not by time — the band is an 8 km straight-line buffer (spec §8), not a routed drive time, and true isochrones are still open for V1 (spec §15) — so the label reads "Within about 5 miles of the practice" and the two live sentences that said otherwise over PLACE-band figures are corrected in the same release. Eight literal edits: A27.1 gives the Median income tile `p.incomeNote || "Household, 2023"` (A21.5b/A21.5c took only the two sub-lines that said "the community", so the median was the one area figure whose caption could not follow its own geography — and it is the one that most needs it, a catchment median being a household-weighted median of tract medians (a weighted median since 2026-09-12, Task INCOME-MEDIAN; a weighted average before), never published and never suppressible); A27.2 gives the Growth tile its own sub-line, chained on A21.3d, naming the geography where the API supplies one and returning A21.3d’s own output byte for byte where it does not; A27.3 drops "(10 min drive)" from the docked panel’s Insights heading and A27.4 stops its footnote calling a straight-line catchment a drive time — the two entries with no null branch to preserve, because the default IS the false sentence, so they re-base the Browse states that render the panel while none of `baseline-manifest.json`’s thirteen frozen hashes moves (none is a Browse capture). THE HONEST MEASURE, which no summary of this may soften: four of the six served figures gain neighbourhood detail and THREE of the card’s four tiles do; the fourth gains an honest label and nothing more until the 2010–2020 tract crosswalk lands, and on the Browse map two of the three fill layers still paint one flat colour per city or county — **this is not "per-neighbourhood market data"**. The THIRD false sentence, the Browse Market data card’s "Figures describe the community around each practice…", IS corrected too, as **A27.5** (2026-09-11, ruled on the implementer’s own report): D-C39 named two sentences, but the figures that sentence describes are precisely the ones this release moves to the catchment, so leaving it would have meant one release corrected two false sentences and made a third one false by its own act. It carries its own id, its own ruling text and its own `LOCAL_AMENDMENTS.md` row rather than being folded silently into A27.4. The sentence lives inside the Browse "Market data" strip behind `v.md?.stripOpen`, which no approved state opened — a ruled change with no oracle, the same gap A26’s F2 closed for the More-filters popover — so `browse-market-strip` is appended to `screens.ts` in this release (D-C40), clicking the card’s own "Expand all six layers" control, and it is the third Browse state to re-base. **D-C42** (John, 2026-09-11), ruled on this same review and built on top of it: A21.5a let `communityLabel` REPLACE that heading, and D-C38 gives 28 of 29 QA listings a label — so on QA the heading read "Within about 5 miles of the practice" and the words "Market Overview" appeared NOWHERE, while A27.3’s own correction was invisible because its default was never reached. The ruling that produced Option C named exactly this as a cost of the Option B he rejected; it landed under Option C too and had never been put to him. He ruled the heading back and the geography onto its own sub-line beneath it, on the reasoning that this is the card’s own established idiom — A21.5b, A21.5c, A27.1 and A27.2 all put the geography under the value it describes, and the heading was the only place on the card where the label replaced the thing it was meant to qualify. A27.6 retires A21.5a’s `||` as a BEHAVIOUR (chained on A27.3, the A27.2/A21.5c pattern) while A21.5a’s own markup edit STAYS — it is what made the heading data, and the heading is still data — and A27.7 adds the sub-line, composed from the docked panel’s OWN `md.panel.place` declaration (`font-size: 12.5px; color: var(--vf-text); margin-top: 2px;`), the design’s one existing heading-and-geography pair, gated by `sc-if` so a listing with no label has no element rather than an empty one. It is not a softening of D-C38: the geography is still stated on the panel, in the place the rest of the card states it. Its oracle is the real-browser gate in `frontend/tests/smoke.spec.ts`, not an approved state — the design’s fixtures carry no `communityLabel` and the reference has no way to be handed one without editing approved fixture data or declaring a ninth prototype prop. **A27.8** closes what A27.7 left (John, 2026-09-11 — ruling D-C48, on the whole-branch review of this branch): A27.7’s ONE sub-line sits above the whole four-tile grid and three of the four tiles are the area group it describes, but the Population tile’s SUB-LINE is not a population figure at all — it is GROWTH, which `serve.py` measures at place-or-county and serves with its own `growth_scope`, reading −1.5% for the whole of Dallas, so the panel printed a city number under a caption describing a ring on 28 of 29 QA listings: the defect D-C38 removed, one card over, live on QA. John ruled it is named ON THE TILE, the way the detail card’s own Growth tile already names it (A27.2) and the API’s `income_note` qualifies the median (A27.1) — the design’s ` · ` joining a figure to what qualifies it — with the figure leading and the geography following, because unlike A27.2’s line this one begins with the number. Chained on A21.2d, inside whose guard the new term sits, so no figure is still no sub-line; the design’s fixtures carry no `growthScope`, so the branch is falsey and every approved state keeps its pixels. **A28** (John, 2026-09-11 — ruling D-C44: the ring is drawn at the distance the card names). D-C38 gives the Community Context card “Within about 5 miles of the practice” while `MarketMapV3.jsx` drew an unlabelled dashed circle at 16 000 m — about TEN miles — so a buyer read one distance and was shown a circle twice its size, on every listing with a point. Neither number is invented: V2 drew TWO rings (`docs/design-reference/design_handoff_practice_match_v2/MarketMap.jsx:160-169`), `drive10` at 16 000 m in `#339dde` and `drive5` at 8 000 m in `#003a70`; V3 replaced the panel that toggled them, kept ONE hard-coded ring, and kept the FAR radius in the NEAR ring’s colour. The radius becomes 8 000 m and the colour stays `#003a70`, so the number and the colour agree for the first time since the V3 panel rewrite. No visible control is added — John was offered V2’s two toggleable rings, composed into V3’s own “Market data layers” drawer, and chose against it: V3 deliberately replaced that panel, and the defect is the radius, not the absence of a toggle. Nine literal edits: A28.1 is the FIRST amendment in the programme’s history to edit `MarketMapV3.jsx` rather than the `.dc.html` (`file: 'jsx'`, the partition spec §9.2 added for A24 — corrected 2026-09-13: A24.9–A24.12 were first; A28.1 rides A24's `file: 'jsx'` partition), and A28.2–A28.4 delete the legacy panel’s orphans under the bundle’s own dead-code rule — `layerHelp`, `fillRows`, `overlayRows` and the `drive5`/`drive10` layer-default flags, each measured first as ONE declaration and ZERO template readers on either target, with the design’s own comment on the deleted line saying so (“GROUP 1 — retained for the legacy panel; the compact control above is canonical”). That removes the LAST “drive time” strings in the product, which is what D-C39 was reaching for and could not name correctly. A28.2–A28.4 left the helpers those rows called (`radioRow`, `layerRow`, `setValue`, `setLayer`) standing, because that ruling named the rows and the flags and nothing else; A28.5–A28.8 (controller amendment D-C45, 2026-09-11) close it — re-measured the same way, each occurring exactly once in `logic.js` and once in the amended design (its own declaration) and zero times in `App.vue` or any test — and delete the four helpers under the same dead-code rule, taking the D-C44 implementer's own "kept" test with them. **A28.9** (controller amendment, 2026-09-11, whole-branch review) is the last orphan the same deletion left: the `practices` member of the Browse layer defaults, whose only reader was A28.2’s own deleted `layerRow("practices", …)` — `SYMBOL_KEYS` is `["pets", "households", "competition"]`, so `activeSymbols` never asks for it, and the one other place `layers` is spread copies the key forward without reading it. What is left is exactly `SYMBOL_KEYS`; `md.practices`, the pin list the map component is handed, is a different declaration and is untouched. A25.6’s “no point, no ring” (2026-09-10) is untouched and keeps its test: the radius moves, the finite-point term that decides whether anything is drawn at all does not. `browse-market-panel` and `browse-panel-lightbox` re-base — the only approved states that select a practice with a point on a screen that renders the map (the docked panel’s lightbox scrim is `rgba(0,58,112,.55)`, so the map shows through it) — and none of `baseline-manifest.json`’s thirteen frozen hashes moves, none being a Browse capture. **A30** (John's ruling, Task PANEL-STALE, 2026-09-12 — root cause read from QA 0.1.21, `screenshots/qa-0121-dallas.png`: with GHI Veterinary Hospital, Austin, selected and its docked panel open, switching the metro to Dallas left the panel OPEN with GHI's header — "GHI Veterinary Hospital · $2.76M · Austin, TX" — over the FIRST Dallas listing's community figures, a false statement about a practice reachable in two clicks and present since 0.1.20, the design's own behaviour): `setMarket` (A13.1's class property) set `market`, `activeId` and `hoverId` but never `mdSel`, so `sel` — resolved by id alone, `P.filter((x) => x.id === s.mdSel)[0]`, market-blind — survived a metro change while `selComm`, filtered to the NEW market's own `communities()`, no longer matched it; `marketPanel`'s own fallback chain (`selComm || comms[0] || { …all undefined }`) then substituted the new market's FIRST community — `comms[0]`, with the design's own fixtures (Sacramento's `c1`-`c4`, Orlando's `o1`-`o4`, Atlanta's `g1`-`g4`), never A25.4's own empty-market literal, which stands in only when the market has no communities of its own at all and is untouched here. The design has no treatment for "the selected practice is not in this metro", and closing is the only honest state. One literal edit: `mdSel: null` joins the object literal `setMarket` already writes, beside `activeId: null` and `hoverId: null`. The design's own fixtures switch metro in no approved state with a panel open, so no frozen hash moves. **A32** (Task ADAPT-STALE-3, 2026-09-12 — measured on QA `db8bf67`, New York at 1,912 × 1,228: a metro switch pulled the whole metro TWICE, 24 requests and 7.85 MB gzipped): `setMarket` runs BEFORE the map has moved, so A24.18's request carries the box the OLD metro is still settled on; on a wide screen that box is past the route's span cap, the adapter's ladder falls back to the whole metro and pays for it, and `loadAreas`'s own guard then DISCARDS the answer because the settled view is the new metro's by the time it lands — A24.22's settled-view listener repeats the sequence and THAT answer is the one drawn. The first was never answerable, so it is not made: with a viewport-publishing adapter AND a metro whose CENTRE is somewhere else, `setMarket` sets `mdAreas: null` — the PENDING state A24.41 keeps the legend's ramp and geography line for, so nothing flickers — and A24.22's listener asks once, with the box the new metro actually settles on. The comparison is the centre and not the name because what decides whether there is a move to wait for is whether the map is going to move: the same metro re-selected, a metro `MARKETS` no longer holds (`load.ts` prunes it to what the API served, so `MARKETS[v]` can be undefined), and every path with no viewport adapter — the reference, the Claude Design preview, any build predating the bbox wiring — keep the design's own immediate load, because there is no listener to wait for and waiting would mean never loading. It is NOT a guess about which metro a box can see, and one was tried and withdrawn: the boundaries route is bbox-scoped and METRO-AGNOSTIC (`_BOUNDARY_SQL` filters on level, vintage and `ST_Intersects(geom, bbox)`, never on the CBSA — the `cbsa` path segment picks only the whole-metro fallback box, the cache key and the 404), so shading has always FOLLOWED a member who pans off the selected metro, and a client refusing on geometry would blank exactly that. One literal edit, chained on A24.18. Its companion is not an amendment at all: `src/map/viewport.ts`'s `publish(v, { force })`, armed by `MarketMapView`'s recentre watcher and never by a user pan, so two metro centres that snap to the same 1/8-tile cell cannot leave the map pending for ever. The design's own fixtures switch metro in no approved state, so no frozen hash moves. **A31** (John's ruling, Task SNAP, 2026-09-12 — ruling D-C50 as REVISED, in his own words: "if this needs 2 modes, for the 'AREA' / 'CITY' and when a user clicks a specific location it must render that facility market information — it should reflect boldly which is being viewed CITY/AREA vs LOCATION"): the Browse "Market snapshot" strip computed its "metro median" and its seven bars from `communities()` — one row per LISTING, each hospital's own five-mile ring — while the map beside it painted Census geography per layer. Audited on QA 304b80f, Dallas: the strip said Households **162K** where the map's tracts hold 0–5,988, and Competition **41** where its ZIP areas hold 3–16; the bars were the first seven hospitals in listing order (`vals.slice(0, 7)`), which is not a distribution at all. In the design's fixture era the map mosaic was built from those same listing figures, so both agreed by construction, and the tract map broke that identity without anything noticing, because the two surfaces are read separately. A24.44–A24.56 (D-C50's INTERIM) made the CAPTION honest about it — "· community level", the practice-area label — which was the right holding fix and is what this supersedes. There are TWO MODES now: **AREA**, nothing selected, is the metro as the MAP paints it — the median and the five quantiles over the polygons of each layer's own geography, from the new `GET /api/markets/{cbsa}/summary`, with the geography and the count named ("metro median · 1,791 Census tracts") — and **LOCATION**, a practice selected and its docked panel open, is THAT practice's own community figure, the same `comms` row the panel and the Community Context card already read, with the bars staying the METRO's distribution and the class the practice falls in kept at full strength while the rest take the strip caret's own `opacity: .6`, so the two modes read against each other instead of the card repeating a number the panel already shows. Closing the panel returns to AREA. The mode is stated first, in the design's own 800-weight Insights heading with the `md.panel.place` sub-line beneath it (A27.7's own source, so the strip gains no style the design does not already carry), and the two words are never shown at once. Sixteen literal edits — A31.1 `AREA_PLURAL` (the ruling's own four plurals, "Census tracts"/"places"/"counties"/"ZIP areas"; a label the table does not carry is printed as it comes, because a pluralisation RULE would produce "Place (city/town)s"), A31.2 the `mdSummary` state key beside A24.16's `mdAreas`, A31.3 `loadSummary` in `loadAreas`' own shape minus the viewport (the summary describes the METRO, so a pan cannot change it and it is asked for once per metro), A31.4/A31.5 its two callers beside `loadAreas`' own, A31.6 `summarySet()` — the design's OWN answer to the endpoint, measured over the polygons `areaSet` draws and quantiled by `percentile_cont`'s own rule, which is what keeps the reference and the app describing ONE distribution — A31.7 the three mode render values, A31.8 the whole `stripCards` block, A31.9 the orphan it leaves (`communities()`'s own `communityLabel`, A24.44's, whose only reader was `stripBasis`; the PRACTICE's `communityLabel` is a different field on a different object, read in four places, and is untouched), A31.10 the markup and A31.11 the footnote, whose "Figures describe the area around each practice, not the practice itself" became false by this release's own act (the A27.5 rule) while A24.20's growth caveat stays beside the new sentence byte for byte, and **fix round 1** (controller, 2026-09-13) adds A31.12 and A31.12b: in LOCATION mode the caption over a figure is THAT FIGURE's own geography — `growth` is measured at place or county and the API names it (`growth_scope`), `econ` is the county CBP row everywhere and always (`app/census/serve.py`: “`econ_k` is county everywhere and always”), so on 28 of 29 QA listings the ring sentence “Within about 5 miles of the practice” was false on two of the six cards, which is the defect D-C48 removed from the docked panel's Population tile one day earlier, one surface over — and the geography is named ONCE there, on the card's own note, while the `src` line carries the dataset alone (`metaSource` composes a line for a caller with no geography to name rather than leaving a dangling separator): measured, the basis printed TEN times on one strip, four cards printing it twice, and seven times after, each true; and D-C51, the same round's caption audit, gives the income card the API's own `income_note` where it serves one (“Within about 5 miles of the practice · approximate”), because a catchment median is a household-weighted median of tract medians — never published, never suppressible — and the detail card has carried that qualifier since A27.1 while the strip printed the bare ring label beside the same number. The same round settles the AREA caption too: it states exactly what the number IS and drops the word “metro” (“median of 503 Census tracts”), because the Census PUBLISHES a metro median at summary level 310 — 97,638 ± 1,163 for CBSA 12420 against this figure's 94,801 — so “$95K metro median” beside a published “$98K” gave a stakeholder a fourth number to explain; a card with NO figure carries NO caption, where it used to read “metro median · 0 ZIP areas” over a blank; and **A31.12c** gives `growth` the `dataset:` its five siblings carry (Minor 3), so its snapshot card and the map legend read “· Place (city/town)” rather than the vaguer “· community level” A24.45's split had left on the one layer it did not convert. The same round's **A31.13/A31.13b** take a bar's colour from the SAME door the polygons do (`bucket(k, v, true)`'s own `color`): the design's own pre-A31 expression `ramp(k)[Math.round(t * 3)]` can address only four classes, and `income`'s ramp carries FIVE colours, so the approved `browse-market-strip` drew income's five bars in four colours — classes 2 and 3 the same one, class 4 unreachable — beside a map painting five, which is the one thing A31.8's own comment said could not happen; the height keeps its `t` and `bucket()`'s return is unchanged, `t = i / (ramp.length - 1)` being injective in `i` within one ramp, so no index had to be added to it. FIVE bars, not the design's seven: five is what the endpoint publishes (p10/p25/p50/p75/p90) and the bar row is `flex: 1` per bar, so it divides whatever space it has — 38.4 px per bar at the card's 232 px minimum against 26.6 px at seven — and no approved layout moves; p10/p90 rather than the extremes because one outlying tract is not a class a summary should draw. TWO CORRECTIONS came from asking what the cards do on REAL Census values rather than on the design's own community figures, which is all any fixture carries: the bars are classed on the MAP's OWN breaks (`bucket`'s third argument, A24.25) because they ARE the map's polygons — a Census tract holds about 1,500 households and `VALUE_LAYERS` starts at 10,000, so the community scale puts all five quantiles in ONE class and draws five identical 6 px stubs, which is the 100.0-%-in-one-class collapse `AREA_LAYERS` was cut to remove on the map, re-imported one surface over — and the LOCATION highlight is drawn ONLY where the practice's figure and the metro's polygons are one measurement, a RATE or a MEDIAN, which is exactly the set `AREA_LAYERS` does not re-scale: a practice's five-mile ring holds 162K households and a tract holds 1,500, so marking that ring inside a distribution of tract counts would light the last bar for every practice in the country and mean nothing — this ruling's own defect, one card over. That narrows the BRIEF's sentence, never John's ruling, and is the one judgement in A31 that wants his eye. **Where the summary comes from is A24.14's ternary exactly**: the adapter's where there is one, `summarySet()`'s where there is not — which is why `browse-market-strip` re-bases (the AREA cards read the fixture POLYGONS' median now, not the LISTINGS') and `browse-market-strip-location` is appended (LOCATION mode had no approved state at all). The oracle answers the app with the design's own distribution, `frontend/tests/design-summary.mjs`, derived from `summarySet` as `design-boundaries.mjs` is derived from `areaSet`, so the two targets cannot diverge; both states are Browse captures, so none of `baseline-manifest.json`'s thirteen frozen hashes moves. THE HONEST MEASURE: this makes the strip describe the map, and the map's own geography is still a tract for three layers, a place for growth and a county for payroll — the AREA cards are no more granular than the layer they summarise. **A35** (John, 2026-09-13 — ruling D-C52: "allow a user to zoom in BELOW the level of the last actual map layer … the map allows user to zoom in as far as they want … this message is never seen"). The Browse basemap is Esri's Canvas/World_Light_Gray_Base with its matching Reference labels, and MEASURED on the day (every number re-fetched by an adversarial verifier) each service publishes its cache "from Level 0 … through Level 13" worldwide and "from Level 14 … through Level 16" in North America, with ZERO cached tiles at 17+ at all nine US points probed — for any tile past 16 Esri answers **HTTP 200** with a constant 2,521-byte grey JPEG reading "Map data not yet available", a SUCCESSFUL image, which is why `errorTileUrl` and `tileerror` (both of which fire on an `<img>` ERROR) could never have caught it. The design created both layers with `maxZoom: 18` and NO `maxNativeZoom` and the map with no ceiling of its own, so Leaflet 1.9.4 derived the map's 18 from the layers (`getMaxZoom` → `_layersMaxZoom`), `_getZoomForUrl` put 17 and 18 in the URL, and the tract polygons, the tooltip and the pin — canvas and marker layers with no zoom limit — went on rendering over the grey. Seven literal edits, all in `MarketMapV3.jsx` (`file: 'jsx'`, A28.1's precedent) and none chained: A35.1/A35.2 give each basemap the last level its own service is actually cached to (`maxNativeZoom` **16** for the gray canvas, **19** for World_Imagery — Esri's published US floor at 0.3 m, measured real at every probed point including rural Texas, while Dallas, New York, Atlanta and Denver reach 20 and Austin, Sacramento and Orlando 21, a depth that is per-area and not knowable client-side, so one level of native sharpness is given up for a guarantee the placeholder cannot appear anywhere); A35.3 makes the base layer REQUEST at that level and DISPLAY to **20**, Leaflet upscaling 2×/4×/8×/16× between them; A35.4 gives the labels the same native cap while they keep their own `maxZoom: 18`, so they soften to 18 and then HIDE (a GridLayer draws nothing above its own maxZoom while the base keeps the map's ceiling — 16× upscaled text is not legible, and the z16 reference tile at GHI is real but EMPTY at 872 B and zero opaque pixels, so the labels contribute nothing there either way); A35.5 gives the MAP an explicit `maxZoom: 20` so the + button's ceiling does not depend on which basemap is on; A35.6 writes the new cap FIRST on a basemap switch and then RESETS the layer — `setUrl(url, true)` and a `remove()`/`addTo(map)` — because `GridLayer.redraw()` moves `_tileZoom` to the new clamp but never calls `_resetGrid()`, the only place `_globalTileRange` is recomputed, so a redraw alone left the layer asking for z19 coordinates while holding the z16 world range and `_isValidTile` rejected every one of them: measured in Chromium as a switch to Satellite at zoom 20 that requested NOTHING and left the tile pane empty, against twelve imagery tiles for the same switch at zoom 10, where both services clamp to the same tile zoom. **Fix round 1** (2026-09-13) then gave that same entry the two things the reset had broken and the one it had never had. The credit is assigned BETWEEN `remove()` and `addTo()`, because `Control.Attribution._update()` rebuilds the footer from its own `_attributions` REGISTRY — written from `getAttribution()` at ADD time and cleared from it at REMOVE time, never from the layer's current `options.attribution` — so assigned on the line after `addTo` the first switch re-registered the OLD credit (A35.7's string never reached the footer on Satellite at all, a defect the pre-A35 code shared) and the second left BOTH credits in it for the life of the map, measured as "Tiles © Esri, Source: Esri, Vantor, Earthstar Geographics, and the GIS User Community" on the GRAY basemap; and a `setBase` for the basemap ALREADY on the map now returns, because `MarketMapView`'s watcher carries `status` among its deps and fires one at every mount — free while the line was `setUrl(sameUrl)`, which sets `noRedraw` itself, and not free once the reset is unconditional: 24 tile elements built against the label layer's 12, the A32 class of waste one surface over. The gate for each is behavioural and new: the e2e reads the RENDERED `.leaflet-control-attribution` text after a mount, a switch and a round trip, and counts the tile elements a mount actually builds; and A35.7 replaces the satellite attribution with the service's own current `copyrightText` — "Source: Esri, Vantor, Earthstar Geographics, and the GIS User Community", Maxar being the vendor's former name — VERBATIM rather than composed from, the rule `dataset_registry.attribution_text` already follows, while the gray canvas's "Tiles © Esri" is untouched. `detectRetina` stays unset, deliberately: it adds a zoomOffset WITHOUT touching `maxNativeZoom`, which is the same defect by another door. Why no gate caught it: `frontend/tests/harness.ts` answers every arcgisonline request with a transparent 1×1 GIF at any zoom and every approved state captures at z10 (desktop) or z9 (phone) with no zoom step, so `frontend/tests/smoke.spec.ts` now walks real Chromium from 10 to the ceiling and records every tile URL on both basemaps. Zero approved states re-base and none of the thirteen frozen hashes moves — `_clampZoom(10)` is a no-op, no capture selects Satellite, and the harness blanks every tile. Thirty-one families, 310 entries: A1's 24 derived edits plus 286 literals; `LOCAL_AMENDMENTS.md` carries one row per amendment and the test pins the set both ways (count and ids). `docs/design-reference/design_handoff_practice_match_v2/Practice Match V2.dc.html` remains the **pre-V3 oracle** — what a suspected regression is diffed against, not what the gates compare to. Every visual and DOM oracle is regenerated from the amended V3 design. **A24.19–A24.20** (controller, 2026-09-12) then moved the granular layer to the **Census tract**: `income` draws summary level **140** labelled "Census tract", nationwide, while `growth` (Place) and `econ` (County) deliberately do NOT follow it — a tract-level growth figure is not computable across the 2010→2020 tract boundary change (plan D12, a registered Phase C deferral) — so each layer keeps its own `geo_label` and the Data Layers caption gained "Population growth is measured for the surrounding city or county, not the tract." Both entries are CHAINED (A24.19 reads A24.2's output, A24.20 reads A24.7's). `app/api/market.SHADING` is the single source and `tests/census/test_design_shading_labels.py` pins the design against it, so the map can never label a geography it is not drawing. The design's boundary fixture was regenerated at level 140 — **503 tracts** for the Austin metro where there were 88 ZCTAs — which moved `SIMPLIFY_DEG` to 0.005 and `MAX_FIXTURE_BYTES` to 280 KB; that cap is a BUNDLE budget, because A24.1 interpolates the payload into the design's `state` literal and `frontend/src/logic.js` ships it (measured after: main bundle 165.7 KB gzipped against `bundle-budget.test.ts`'s 220 KB). **A24.21–A24.23** (controller, 2026-09-12) then close the last gap that ruling left: the route has taken a `bbox` since Task 9 and A24.15's loader never sent one, so every request was for the whole metro ENVELOPE — at tract scale **5,935 tracts in New York** where the Browse map's own view holds 3,706, and on the stakeholder's 1460 × 1228 screen **7,470**. **The caps were re-measured for Census tracts the same day (2026-09-12, Task CAP)**: `MAX_FEATURES` 4000 → **12000** and `MAX_BODY_BYTES` 2_000_000 → **6_000_000**, both measured on QA's own tract geometry through the route's own `_BOUNDARY_SQL` and `compose()` byte arithmetic — 12,000 clears 9,767, the densest 4° × 4° box anywhere in the country (the New York–Philadelphia corridor, found by sweeping the lower 48 on a half-degree lattice) and therefore the largest `MAX_BBOX_DEG` admits; 6,000,000 clears 4,269,628, the largest FIRST VIEW at a delivery tolerance of 0.78 CSS px of latitude. `MAX_BBOX_DEG = 4.0` is unchanged and is what refuses a state, which **retires the old ordering argument** ("below the 6,884 tracts a whole-Texas box returns") outright: a whole-Texas box is ~13° on a side and is refused on SPAN before a row is counted, and the largest first view is 7,530, above 6,884. Until that measurement the metro request was a 422 outright and New York opened blank; it is served now, and what the bbox buys is the size of the ANSWER rather than the difference between a map and a blank one. `frontend/src/map/viewport.ts` is the new channel: `MarketMapView` publishes the map's own bounds at mount and on Leaflet's `moveend`/`zoomend`, and `src/market/boundaries.ts` reads them. The box is PADDED by 0.3 of the span on every side — measured, not chosen: that is the padding the engine's own shared canvas renderer is built with, and `Renderer._update` paints nothing beyond it during a drag, so a larger box buys no pixel — then snapped OUTWARD to a grid of one eighth of a 256 px basemap tile (32 CSS px, `360 / 2^(zoom+3)`), so the same ground always produces the same query string and therefore the same 24 h cache entry, instead of the centimetre-scale float jitter `getBounds()` returns on every `moveend`. The debounce is **250 ms**, Leaflet's own zoom-transition settle (`leaflet-src.js:4827`, `leaflet.css:198`), and a publish whose snapped box has not moved notifies nobody, so `invalidateSize()` and a selection pan cost no request at all. Three script edits, every one of them CHAINED: A24.21 rewrites A24.15's whole `loadAreas` — it reads `market.viewport()` ONCE and uses that one value both as the `bbox` it sends and as the token an arriving answer is checked against (A24.15's market guard extended, never replaced), KEEPS the polygons up while a pan reloads (a pan is the same city; a metro change still clears) and asks for NOTHING until a map has published a box, so the boot no longer fires the doomed whole-metro request; A24.22 subscribes in `componentDidMount` beside A24.17's own load; A24.23 unsubscribes in `componentWillUnmount` beside A13.5's three document listeners. An adapter with no `viewport` — the reference, the Claude Design preview, any older build — takes the old whole-metro path unchanged, both new terms being guarded on the method's presence, so every approved state keeps its pixels and none of the thirteen frozen hashes moves. The padding is not free and the cost is recorded rather than hidden: measured on real TIGER tracts (31,252 rows, ten states), a 1440 × 940 Browse map centred on the New York CBSA centroid at zoom 10 sees **3,706** tracts unpadded and **4,811** padded — so the adapter retries ONCE, unpadded, on the route's own `AREA_TOO_LARGE`, which is doing what that refusal literally asks for ("Zoom in or pass a smaller bbox") and not a workaround; a box wider than `MAX_BBOX_DEG` (`BBOX_TOO_LARGE`, a view of more than one metro at a far zoom) falls back ONCE to the whole-metro request this adapter used to make, so a zoomed-out small metro keeps its colour instead of losing it to this change; anything no box can fix — `BAD_LAYER`, a 401, a 404 — is never retried and leaves the map in the SAME unshaded state Task 10 already photographs in `smoke.spec.ts`. The cap numbers are never restated client-side: the SERVER says which one it hit and the adapter answers that code. A Manhattan-centred zoom-10 view is 4,425 tracts even unpadded and stays refused — the route's own "zoom in" — while at zoom 11 it is 3,942 padded and served. `tests/census/test_boundaries.py::test_the_viewport_bbox_narrows_new_yorks_answer_and_both_arms_now_serve` pins both arms through the real route at the real caps, and `test_the_default_new_york_view_serves_at_a_sub_two_pixel_tier` pins the stakeholder's own first view at a delivery tolerance no member can see. `header-1100` and `header-1000` are not among the thirteen: they are Browse screenshots, and `mobile-map`, the Browse states and the two header states change by design. **AMEND-GUARD** (Task HOUSEKEEPING-B, 2026-09-13) is what keeps this ledger honest about what it TAKES AWAY: `frontend/tests/amend-guard.ts` walks every line and every prose sentence an entry's `replace` introduced and fails unless it is still in the amended design or a later row DECLARES the removal, in two tiers — the LINE tier takes the token `consumes <id>` (or the stronger `supersedes <id>`), the SENTENCE tier takes only `supersedes <id>` or, on the entry being retired, `superseded by <id>`, which is A10 and A10.2's own vocabulary. A bare mention of an id is not a declaration and neither is a citation: at `db8bf67` A24.56's row both cited A24.20 and quoted the sentence it was dropping, and A24.20's growth caveat left the product all the same — which is the defect this gate exists for, and 44 of the 68 consumed-line pairs were passing on an incidental mention until the token rule landed. `LOCAL_AMENDMENTS.md`'s own header says how to write a row and a citation.
The V3 file is the approved design **plus the local amendments listed in `docs/design-reference/design_handoff_practice_match_v3/LOCAL_AMENDMENTS.md` (spec D15)**: the pristine bundle file is kept beside it as `Practice Match V3.rev2.dc.html` and is never edited, `frontend/tests/design-amendments.ts` is the machine-readable list of ruled edits, and `frontend/tests/design-amendments.test.ts` proves pristine + amendments == the amended file byte for byte. Amendment **A1** (John, 2026-09-07: "keep the V2 header and do not restyle header or fonts") restores V2's display typography on 24 template elements — every display-size heading paired with V2 by tag, text and size takes V2's `text-transform`/`letter-spacing` values in place, and the key-fact values `{{ m.v }}` and the 28 px mobile asking price return to V2's uppercase `.005em` — so the thirteen non-Browse screens (`mobile-list`, `mobile-detail`, `detail`, `requests`, `seller-dash`, the four `wizard-*`, the four `admin-*`) were byte-identical to V2 again, hashes and all, **until the launch removal** (A6, 2026-09-07, ruled D-I8-6) took the prototype jump bar off the top of every screen: `frontend/tests/baseline-manifest.json` now holds the post-launch-removal hashes and is the within-worktree leak detector it was built to be — from here a moved hash means a CODE change moved a screen the design did not. The other three families are literal script or template edits: **A2** (spec D17 — the mobile practice card opens the detail; **A2.2–A2.5** then delete the `browseSel` orphans C13 left behind, including the unwired top-level `selectMarker`, under the bundle's own dead-code rule — the wired `mobileVals.selectMarker` is untouched), **A3** (spec D18 — "View full market report" → "View full listing") and **A4** (spec D21 — Compare hides the "What this means" card). Wave 2a's Task I8a added three more families of literal edits: **A5** (the prototype wiring — `signIn`/`signOut` through the `auth` adapter, `componentDidMount` reading the loaded account, and the `startGate` and `me` prototype props the reference is driven by), **A6** (the launch-removal list below) and **A7** (spec §Sign-in — "VIN username or email" → "Email"). The account-screens branch widened **A7** with **A7.3–A7.4** (spec §4.1/§4.2 — the sign-in card's footer gains "Forgot your password?" beside "Request access", and its sub-line drops the VIN language) and added two more families: **A8** (thirteen literal edits, A8.1a–A8.8b, John, 2026-09-08 — "approved" — composes the sign-up, check-email, verify-expired, forgot, reset, reset-expired, accept-invite, applicant-answer and re-apply cards from the V3 gate card's own elements, and widens `startGate`'s enum to all thirteen gate values) and **A9** (A9.1a/b, controller amendment A-S5, 2026-09-08 — one more declared prototype prop, `startAnswerNote`, so the reference renders the applicant-answer card's seeded note exactly as `startNotice` renders the sign-in ones). **A10** (John, 2026-09-08 — the sign-in card's second gate point, "Sellers control location & disclosure"; A8–A9 were reserved by the account-screens branch while this was ruled on `main`, and both families are above now), revised the same day by A10.2 ("Sellers control what buyers can see"). **A11** (John, 2026-09-08 — unifies the docked panel's other-tabs CTA with the Insights tab's: "Open full listing" → "View full listing", the unification A3's row had flagged). **A12** (John, 2026-09-08 — the Seed Listings launch: "Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA" — his words of the day, kept as he said them; the set is TWENTY-NINE since Task SD1 added his eleven Dallas hospitals on 2026-09-11, and `seeds/hospitals.json` is the count of record, never this sentence; eleven literal script edits — A12.1–A12.5 so `practiceName`, both `photoSet` branches, `heroSrc` and `thumbSrc` read a listing's own `name` and `photos` (the thumbnail takes the second photograph, as the design's own thumbnail is a second view), A12.6–A12.7 so the detail's Community Context tolerates the `growth`/`hh` the API leaves null under D4, A12.8–A12.9 so the detail's subtitle and its "General location" row name the listing's OWN state through the design's `stateOf(market)` helper instead of a hard-coded `", TX"`, and A12.10–A12.11 so a listing with no community figures reaches the design's own "Community data unavailable" card instead of a four-tile grid of blanks under the Census attribution — the design's own fixtures are all in the Austin metro and carry no name, no photos and non-empty figures, so every approved state keeps its pixels). **A13** (John, 2026-09-08 — "must fix this drop-down to be an acutal drop-down vs the popup following the same design logic of a normal dropdown"): the Browse toolbar's metro `<select>`, which on macOS opened the operating system's popup menu, becomes a trigger and a `role="listbox"` panel composed from the Market data card's own layer menu (`md.toggleLayerMenu`'s trigger and its panel) and anchored beneath the field like the "More filters" popover in the same toolbar row; eight literal edits — `setMarket` moves to a class property beside `setF` with its state transition unchanged, `renderVals()` gains the menu's state, styles, keyboard handler and rows, the markup is replaced, `componentDidMount`/`componentWillUnmount` arm and remove the Escape and outside-click listeners the design has never had, and A13.6–A13.7 delete the two render-value orphans the `<select>` left behind (`market:` and the rows' `v: m`) under the bundle's own dead-code rule, as A2.2–A2.5 did for `browseSel`. The focused trigger is a `role="combobox"` — the role ARIA 1.2 supports `aria-activedescendant` on, `button` not being among them (final review I1) — carrying `aria-activedescendant` and `aria-controls`, the option rows carry `tabindex="-1"` so focus stays on the element that holds the highlight, the highlighted row is scrolled into view both when the arrow keys move it and when the panel opens, and a choice — mouse or keyboard — returns focus to the trigger rather than dropping the user on `<body>`, because the seeded metro list is longer than the panel (Q5). A13.8 (final review m4) gives the listbox the Tab dismissal A14.7 gave the Give menu, in the same shared `focusout` closure and applied after it; the five filter selects were left native here and that scope was overruled on 2026-09-11 (family **A26**, John: 'the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design'), which converted them and — in the same family's Task F2 — the three inside the More-filters popover on this same idiom, each panel taking the width of the trigger that opened it so their edges line up (A26.16, John, 2026-09-11); the results-rail sort select (defect D-F1, a control that displays an order the list never takes) becomes its own change, wired as well as converted (John, 2026-09-11), and the wizard's four field selects convert later as their own change (John, 2026-09-11), so until that lands the product deliberately carries two dropdown behaviours — Browse in-design, the wizard native — which is a ruled state and not a drift. **A14** (John, 2026-09-08 — "the Give button must be identical to the https://vinfoundation.org/ where the button is an actual drop down (match button design pixel-by-pixel)") replaces the header's inert Give button with the live site's Give dropdown, MEASURED on vinfoundation.org rather than composed from this design's tokens: a `#339dde` pill that turns `#07386f` when hovered or open, a Font Awesome `angle-down` chevron inlined at the live 11.25 × 18 px box, a 3 px `#339dde` underline 4.34 px beneath it, and a square white panel with a `1px solid #07386f` border listing Annual Fund, Cor Group, Legacy Giving and the Dr. Sophia Yin Memorial Fund — John's four hrefs, in the same tab, because the live site's carry no `target`. Eight literal edits: A14.1 `giveFocus` beside `money(n)`, A14.2 the render values, A14.3 the markup, A14.4/A14.5 the Give branches inside A13's shared `trackMenuDismiss` closures (placed so A13's metro dismissal is unchanged, which a characterisation case proves), A14.6 the one `@font-face`, A14.7 a third closure in the same function so Tab out of the menu closes it, and A14.8 the nav toggle's own `giveMenu: false`. Opening any one of the four menus closes the other three (final review m7): A14.2's `toggleUserMenu` and both of Give's open paths, A13.2's `toggleMarketMenu` and A14.8's `toggleNavMenu` carry the six directions between them, rather than the global `pointerdown`/`focusout` pair being the only thing that held the invariant. Home and End reach the Give trigger as they do the metro one (m6). The arrow keys seed a pending index that the panel's own mount ref spends, rather than focusing inside a `setState` callback: the app's `setState` runs its callback synchronously and Vue has not rendered the panel yet, so the callback form worked on the React reference and did nothing in the app — A13's `marketPanelRef` idiom, for the same trap (review round 1, C1). On the plan's blocking Q1 John ruled the same day: "Self-host Montserrat 600 under the SIL Open Font Licence, scoped exclusively to the Give button and its menu. Keep the rest of the design typography unchanged" — so the bundle now ships `assets/fonts/Montserrat-SemiBold.woff2` with its `OFL.txt`, the app carries a byte-identical copy under `frontend/public/assets/fonts/` and the same rule in `src/styles/global.css` under the `assets/` → `/assets/` rewrite, and the design names the family in exactly three `font-family` declarations — the `@font-face` itself and A14.2's two style strings — and the test walks every other `font-family:` in the file and fails if any of them names anything but the bundle's own faces, so no other element's face can change. Click-to-open, Arrow/Home/End, Escape and outside-click are John's ruling, not measurement: the live menu opens on hover and has none of them. The Give button is in the header of every DESKTOP screen, so 40 of the 45 baselines re-based and eleven of `baseline-manifest.json`'s thirteen hashes were re-pinned under the ruling — the same mechanism A6's launch removal used; `mobile-list` and `mobile-detail` did not move, because the prototype's 390 × 800 phone frame renders its own header, which is the proof the change reached nothing else. **A15** (John, 2026-09-09 — every uploaded photograph renders, with its own description: "surface all images uploaded and have the user articulate what it is and render ALL images"; six literal script edits — A15.1/A15.2 so both `photoSet` branches read a photograph's OWN `p.photoCaptions[i]` (the API's new `photo_captions`, `migrations/090_listing_photo_captions.sql`) with the design's fixed slot caption as the fallback, and A15.3a–A15.3d so each branch appends one tile per photograph beyond the sixth, captioned "Photo N" where nobody has described it — the design's six captions are fixed per practice type, so before this a photograph could only be shown by matching one of them and only 73 of the 195 in John's folders were rendered; his fixtures carry no `photos` and no `photoCaptions` at all (`p2`'s three photographs are the design's `SRC` map, keyed by slot id), so both guards are falsey and every approved state keeps its pixels). **A16** (John, 2026-09-08 — the seller wizard and dashboard read and write the real API: "none of the existing Photes and Documents are being render4ed in the 'EDIT' of an existing listing … none of the actual inputs are appearing in the PREVIEW and SUBMIT … this gap must be corrected and the UX true"; twenty-four literal script edits — A16.1–A16.3 so the dashboard renders the seller's OWN listings and Continue/Edit/View open the row's real draft, A16.4–A16.6 so step 6 lists the listing's real photographs and documents, "Add files" adds a photograph or a document through the browser's own dialogs uncapped (A-SL20) and Continue saves the step, A16.7–A16.9 so Submit and the dashboard's three transitions go through the API and the bootstrap loads the seller's listings, A16.10 for the ruled `declined` pill, A16.11a/b for the eighth declared prototype prop `startMyListings` — the reference's only way to the EMPTY dashboard A-SL17 added to the oracle — A16.12/A16.13 so the preview counts photographs rather than files and names the listing's own state rather than Texas, A16.14/A16.15 (controller amendment A-SL23, on the SL7 review) so "Create a listing" creates one instead of silently re-opening the listing last edited and "Save and exit" saves the step it is on, and A16.16/A16.17 (A-SL25, on the SL7 re-review) declaring `wizAssets` and `creating` in the design's own state literal and adding the two helpers every adapter path shares — `openDraft`, the one place a fetched draft becomes the wizard's state, and `reloadListings`, the one loader, which carries the rejection arm its callers kept forgetting, and A16.18 (A-SL27 (3), on the round-3 re-review) so the step rail saves the step it leaves before it moves — in the adapter's partial mode, which A16.15's Save-and-exit now uses too (A-SL27 (2)): a blank required number is left out of a save that is not Continue rather than sent as `""` and refused, since a draft is incomplete by nature — while `toWizardState` leaves a null column alone so a just-created listing opens on the design's own four defaults instead of four blank selects (A-SL27 (1)), and the step → fields map both sides read is one data file, `frontend/src/listings/step-fields.json`, pinned two-way by pytest without parsing any TypeScript — and A16.19 (A-SL29 (1), on the round-4 re-review) so the Back button saves the step it leaves the same way, the rail's twin round 3's count of "the one navigation control that silently discards work" missed, and A16.20a/A16.20b (A-SL30 (3), on the round-5 re-review's Info-15) so the two remaining doors out of the wizard also save: the header nav (`go`) in the rail's own shape, and Sign out, which ATTEMPTS the same save and ends the session regardless of the answer — a session end is the seller's explicit act and is never held hostage to a save nobody asked for — and A16.21/A16.22 (A-SL25 (10), Task SL7b) so the step-6 tile re-describes an EXISTING photograph, seeded ones included, by clicking it: the tile's own `describe` (photographs only, one chained promise into `wizErr`) routes by the tile's own `source` to `caption()` for an asset or the new positional route `PATCH /api/seller/listings/{id}/photos/{n}` for a seed entry — the SAME overloaded adapter method the browser's own prompt already is — and one template literal wires the tile's `onClick` to it. With an adapter present the dashboard renders the loaded rows or NO rows and the wizard renders the fetched draft or NO tiles — never the design's four Austin fixtures and never its three fixture photographs, whatever the API answered (A-SL23 (2), A-SL25 (1)); the reference and the Claude Design preview pass no adapter and carry no listings, and the oracle answers the app with those same four rows, and with the design's own three photograph tiles as the created listing's draft, as real pages (`frontend/tests/design-seller-listings.mjs` and `design-wizard-draft.mjs`, both derived from `logic.js` as the D6 stub is derived from `P`), so every approved state keeps its pixels). **A17** (Task SL8; D24 and John's standing rule, verbatim: "every Admin tab must show real database data, never dummy rows"; two literal script edits — A17.1 is A16.1's own shape applied to the Admin Listings tab's rows, and A17.2 is A16.9's, one line after A16.11b's own bootstrap load: with an `adminListings` adapter present the tab renders the loaded review queue or NO rows — never the design's own five literal rows, whatever the API answered — and the reference and the Claude Design preview pass no adapter and keep the design's fixture path unchanged. The oracle holds `admin-listings`'s frozen pixels through the SUCCESS path: `frontend/tests/harness.ts` answers `GET /api/admin/listings` with all FIVE of the design's own Listings rows, "Flagged" included — that row's status backs no real `listing.status` value and inventing one is out of scope (spec §13), which is a limit on the LIVE mapping's `PILLS`/`ACTIONS` tables, never on this oracle-only fixture — derived from `logic.js`'s own fixture by `frontend/tests/design-admin-listings.mjs`). **A18** (John, 2026-09-09 — "the arrow icons are backwards on each location, reverse each"; screenshots of the docked panel's "View full listing" button and the detail's "Back to results" link): `navigate-arrow.svg` points left unrotated, and the design's own way to point it right is `transform: rotate(180deg)` (the docked panel's Next arrow); the Insights-tab CTA carried it unrotated after its label and the Back link carried it rotated before its label, so each pointed away from where it goes. Two literal template edits — A18.1 adds the rotation to the CTA's arrow, A18.2 removes it from the Back link's — and no SVG changes: the docked panel's prev/next pair and the two sign-out arrows (the account menu's row and the phone frame's pill — John's open question, D-A18) share the glyph and are untouched. `browse-market-panel`, `detail` and `interest-modal` re-based; `detail` is the one frozen row that moved and was re-pinned under the ruling, the other twelve — the two phone-frame captures included — unchanged. **A19** (John, 2026-09-09 — "the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close"): every photograph a member can see — the detail's filled tiles, A15's extra tiles included, and the Browse docked panel's current photograph — carries a transparent hit-target that opens a lightbox composed from the design's own elements: the interest modal's scrim at `z-index: 1100`, above Leaflet's attribution and controls at 1000 on Browse; a `role="dialog"` box in the tile frame's declarations; the photograph at natural size, never upscaled, bounded to the viewport; the docked panel's 34 px `nav-arrow-white` prev/next pair verbatim, hidden for a single photograph; its 38 px close button with `close-x-gray` whitened, "Close photo"; and its counter and caption pills verbatim — the caption the photograph's own A15 description, "Photo N" where nobody has described it. Twelve literal edits (A19.1–A19.12): the state keys, the five class members (open, close, the photo list the carousel already counts, step with wrap-around, the render values), the render key, the two openers, the two hit-targets on the `hasSrc`/`hasAny` branches only (an empty slot's click on the reference is the design tool's file chooser), the overlay once at the root, the Escape/ArrowLeft/ArrowRight branch in A14.5's shared `key` closure and a focus trap in A13.8's `out` closure — no new listener — and `go()`/`signOut` clearing it; `frontend/src/router/useStateRouteSync.ts` clears it on Browser Back, the one leg the reference (no router) cannot carry. Focus enters through the mount-ref idiom (A13/A14, review C1), returns to the opener on close; `e.currentTarget` is new to the design. The results-rail thumbnails do not open it (a card click is the selection) and the phone frame has no photograph. Three approved states appended (`detail-lightbox`, `browse-panel-lightbox`, `detail-lightbox-next`), all through Round Rock, the design's own three-photograph fixture; the frozen manifest did not move — the closed lightbox is one unmounted `sc-if` and a button that paints nothing — which is the proof, checked after A18's one-row re-pin. Two fix rounds followed review. **Fix round 1 (A-LB2, John's ruling on the L2 review)** pinned the docked panel's own `.rf-scroll` scroll in `atTop()` (a real flake, not load noise) and added the Step 10 note to A19.2/A19.10's rows; building the third finding's gate, it walked real Chromium, found A19.10's `out`-closure focus trap does not hold — forward Tab off the last control reaches real page content before self-correcting, Shift+Tab from the first never wraps — and stopped rather than write a test against the defect. **Fix round 2 (A-LB3)** ruled the trap out: A19.10's `focusout` branch is REMOVED (a null `relatedTarget` cannot tell a window blur from focus leaving the dialog's own tabbable set, so no threshold fixes it), and A19.9's shared `keydown` closure now also owns Tab — reading the dialog's controls in DOM order and wrapping deterministically at both ends, one code path, no timer, no `relatedTarget` — with an e2e gate proving a full cycle in both directions. A19.10's row stays, now a documentation-only comment at the closure head recording that a trap was tried and retracted, not never attempted. **A21** (controller amendment A-C28, 2026-09-10, controller amendment A-C29, 2026-09-10) addresses missing census data (Task B8 review): A21.1 removes the `|| 0` defaults from `communities()` so missing figures return `undefined` instead of zero; A21.1b skips those undefined values at the assembly point in `marketVals()` so they never reach `bucket()` or `fmtMetric()` and the renderer never displays "undefined" or "$NaNK" labels. **A21.2 and A21.2b are REVERTED (amendment A-C29)**: the economic figure IS payroll per establishment, not revenue; the metric is merely misnamed `revenue_per_establishment` in the database, and the design's original labels "Average Practice Payroll (CBP)" / "Avg. payroll per practice" were correct. **A21.3 is completed (amendment A-C29) to remove hard-coded years from three places in the design** where the vintage is data-dependent, not fixed: A21.3a the VALUE_LAYERS label, A21.3b the LAYER_META sub-line, A21.3c the Data Layers card's blurb and caption. A21.3d, the detail's Growth row (the real defect where the API now returns "+11.6% since 2018", so the hard-coded strip and label no longer match; the fix splits the API string and reconstructs the sub-line from the year it carries) is applied through the amendment mechanism like A21.3a/b/c. **Task B10 (D-C31 and D-C32, 2026-09-10) adds thirteen more A21 entries and revises A21.2d.** The ROOT CAUSE the earlier entries could not reach was `communities()` itself: `num()` returns 0 for a null and `parseFloat(...) || 0` did the same for growth, so every `!== undefined` guard was satisfied by a 0 and the docked panel read "0" Population, "0.0% (5 yrs)", "$0K" Median Income and a "Flat" growth verdict for a listing the Census has no figures for — A21.1c extends A21.1's treatment to `pop`, `hh`, `income`, `growth` and the `pets` estimate derived from `hh`, which is what makes those guards live. A21.2d's revision makes each overview sub-line `undefined` as a whole rather than leaving its unit behind; A21.2m omits the three competition bars, A21.2n/o take the Market-data strip cards' metro median over the DEFINED values only (a card whose metro has no figure keeps its title, source and link and shows no value and no bars), and A21.2p drops a Compare row's bar where that community has no figure — a bar drawn at its floor is a reading, not an absence. A21.4a–A21.4d put the DESIGN'S OWN "Community data unavailable" card on the docked panel through the detail's own `hasDemo`/`noDemo` idiom (A12.10/A12.11), with the "View full listing" CTA outside both branches because navigation is not data. A21.5a–A21.5d carry D-C32's `community_label` — the API serves it when a listing with no `place`-band FIGURES falls back to its `drive_10` band (`app/census/serve.py`) — into `ApiListing`, `Practice.communityLabel` and the design, and render it in both places that name the area: the panel's Insights heading ("Market Overview (10 min drive)" → the label) and the detail's Community Context card (the Population and Households sub-lines and the attribution paragraph's trailing sentence, the Census attribution itself untouched). Where the label is absent every string is the design's own, byte for byte, and the design's fixtures carry no `communityLabel` key at all, so every approved state keeps its pixels and none of the thirteen frozen hashes moves. A21.5c is the family's one CHAINED entry: its `find` is A12.7's whole `replace`, the A21.3d/A12.6 pattern, so it does not occur in the pristine bundle. **A22** (John, 2026-09-10 — Task SL10: "Preserve existing seed wording/detail"): the wizard's ownership select widens from four options (the design's four) to ten, adding the seeds' own six phrasings alongside the design's four. The API's `OWNERSHIPS` tuple and the design's option array are identical and pinned two-way by pytest (Task SL10 step 1: `tests/api/test_seller_listings.py::test_the_design_ownership_options_equal_ownerships_tuple`); the closed select paints the same so no frozen hash moves. **A23** (John, 2026-09-10 — Task MD1: "the collapse widget top left expand/collapse is disconnected to the drop down"): collapsing the Market data card leaves its LAYER dropdown floating over the map with no card above it. Exactly ONE of the card's two menus escapes the collapse, and it is the one John reported: the layer menu's panel (`frontend/src/App.vue:475-476`) is `position: absolute; left: 16px; top: 118px; z-index: 620` and sits OUTSIDE both of the card's `v-if="v.md?.legendOpen"` templates (`:382-410` and `:413-472`; the card div closes at `:411`), so nothing unmounts it. The comparison listbox never floated: its panel (`:434-435`) is nested inside the second `legendOpen` template in normal flow (`margin-top: 6px`) and has always unmounted with the card — `mdCompareMenu` is cleared all the same, for a weaker and different reason: a menu left open in state reappears already-open when the card is expanded again, which is its own surprise. The clear is UNCONDITIONAL — the one `setState` runs on expand exactly as on collapse, so neither menu can come back open in either direction, which `frontend/src/logic.test.ts`'s four-quadrant characterisation pins. The fix mirrors the design's own idiom where `insightOpen` gates the "What this means" panel on `s.mdLegendOff !== true`, which is why that panel behaves correctly. One literal edit. **A24** (John's rulings D-C34–D-C37, 2026-09-10 — Task 4 of the neighbourhood-shading plan: the Browse map stops painting a grid and starts painting real Census boundaries): `mosaicCells` drew a 3,070-to-12,560-rectangle mosaic of grid cells coloured by the nearest community centroid, which is why the complaint that started this stream was that the map is "just a blob over the whole city". Sixty-three literal edits — fifty-nine in the `.dc.html` and four in `MarketMapV3.jsx`, the family the second-file partition was put in place for (the engine grew `file?: 'dc' | 'jsx'` and a second frozen pristine twin, `MarketMapV3.rev2.jsx`, in Task 1); A28.1 reached that file first, by merging first. Each fill layer now draws at the geography its figure is honest at (D-C35), with the legend naming it: Census tract (`140`) for Median Household Income, Households and Pet Ownership (estimated), place (`160`) for Population Growth, county (`050`) for Average Practice Payroll and ZIP Code Tabulation Area (`860`) for Veterinary Competition. Income moved `860` → `140` on 2026-09-12 (the tract ruling, A24.19/A24.20) and the last three joined on the same day (**D-L1**, John: "households, average practice payroll, veterinary competition and pet ownership (estimated) render NOTHING"): all four painted nothing on QA, because payroll's writer suppressed every county in the country on the CBP NOISE flag (`EMP_N=0;PAYANN_N=0` is noise level zero — published cleanly — and `materialize._cbp_suppression` is now the one decision both writers make about that column) and the other three had no `SHADING` entry at all, so the route answered `422 BAD_LAYER` and the app correctly drew nothing. They were kept as graduated symbols at the listing point on the grounds that "city-scale class breaks on small areas carry no information", which was true of the BREAKS and never of the geography: `AREA_LAYERS` (A24.25) is a SECOND class-break table for the three COUNT layers, cut over the distribution the MAP paints — 85,381 US tracts (the design's `[10000, 25000, 45000]` put **100.0 %** of them in one class; `[1000, 1500, 2000]` takes 21.9/31.4/26.0/20.7 %), the same times 0.57 for pets, and 4,720 US ZIP areas carrying a 541940 count (the design's `[3, 6, 10]` left its FIRST class empty; `[4, 6, 10]` takes 37.0/36.1/21.4/5.5 %) — while `VALUE_LAYERS` is untouched, so the snapshot strip, the Compare rows, the graduated symbols and the docked panel keep their own community-scale classification and their own pixels. Competition is at the ZIP area because ZIP Code Business Patterns is published per ZIP code and exists at no other geography, which is the one exception the Census spec's §6 grants, and the response, the legend and the tip all name it; §15's "N veterinary practices within this ZIP Code Tabulation Area" is in the tip, never a bare number, and §9's "this is a modelled estimate" is in pets'. The legend claims no ramp and no geography when ZERO polygons were drawn (A24.32), which is what a metro in no CBSA gets — though NOT while a metro's areas are still loading (A24.41/A24.42, fix round 1), because a legend that disappears and returns is a flicker rather than a state. **Fix round 1** (the review of e984c85..304b80f, 2026-09-12) added A24.33–A24.42 and is where the layers became honest: `areaSet` did not know the design's own field aliases (`hh`, `vets`), so `households` drew 503 of 503 polygons in the no-data grey UNDER a full four-class ramp naming a geography — the exact statement A24.32 exists to prevent, and the characterisation case that looped the layer checked only the legend, never the fill (A24.33); `LAYER_META.competition` still named County Business Patterns, a 2023 release and "community level" for a layer served as `zbp`/`2022`/ZCTA, so one tooltip named two datasets (A24.34), and income's and households' source lines still said "community level" for tract layers (A24.35/A24.36); and the ZIP threshold was invisible — ZIP Code Business Patterns publishes no count for a category with fewer than three establishments, though it counts them in its all-industry TOTAL, so `app/census/zbp.py` now loads that total (`NAICS 00`) in the same pass, `geo_metric` writes a third state (`suppressed: true, suppress_reason: "source_threshold"`) for a ZIP area the Census withheld, and the tip says which rule hid it in `app.api.market.THRESHOLD_RULE`'s own words, pinned across both sides (A24.38); before it, 393 of Dallas's 535 ZCTAs — 73 % of that map — read "No data for this area" over cells the Census had deliberately withheld, the mirror image of the CBP over-suppression this same branch fixed. The served floor of three is also why the first competition class is labelled "3" and not "1–3" (A24.37): a class that promises a 1 or a 2 is false precision, and `scripts/measure_area_breaks.py` is the committed re-derivation of every break in `AREA_LAYERS`. A metric that SHADES the map no longer also draws its own graduated symbols (A24.40), so one legend describes what is drawn. **A24.43** is ruling MS1, recorded on 2026-09-05 and never fixed until now: the Market snapshot strip reported Dallas Population growth as "+1.5% metro median" while every Dallas listing's own figure is −1.5 % — the design's `num` helper stripped every character but digits and a dot, which loses a leading minus and glues on the year that follows ("-1.5% since 2018" → 1.52018), and `stripCards` is the one growth reader that goes through it while `communities()` keeps the sign with `parseFloat`, which is why the docked panel was right and the strip was wrong about the same city. It reads the first number in the string and nothing after it, sign included, and keeps the design's zero-for-null contract. **Fix round 2** (A24.44–A24.57, 2026-09-12, ruling D-C50's interim) separates the two surfaces that were sharing one sentence: the snapshot strip's Households card read "162K metro median · U.S. Census ACS 5-year estimates (2023) · Census tract", where the 162K is the median of the LISTINGS' own five-mile-ring totals (`stripCards` reads one row per listing) and a Census tract holds about 1,500 households — A24.34–A24.36 were right to make `LAYER_META.*.source` name the MAP's geography, and the strip was borrowing their string. Each layer whose line named a geography now carries the DATASET alone and `metaSource(k, basis)` composes the rest — the map's geography for the legend, the tip and the map's community notes, the practice-area label the API already serves for the snapshot (all twelve Dallas listings carry "Within about 5 miles of the practice"; the design's own fixtures carry none, so the reference path reads "community level" exactly as it does today) — one string per fact, nothing duplicated to serve two readers. The strip's footnote's FIRST sentence becomes one true of both surfaces while A24.20's growth caveat stays beside it byte for byte — the sentence A24.56's own first draft swallowed, restored in fix round 3 because growth was then the one layer whose snapshot card still read "· community level" and the paragraph is where that is said — growth carries its own `dataset:` from Task SNAP fix round 1 (A31.12c, 2026-09-13), so that card and the map legend now read "· Place (city/town)" and the caveat stands on its own merits rather than as cover for a vaguer line — which re-bases `browse-market-strip` and nothing else, and the threshold tip drops the lead-in A24.38 shipped in front of `THRESHOLD_RULE`: the ruling was one sentence and no other new copy. The same round took the design's SECOND number parser out: `communities()` read a growth figure with its own regex, which glued the trailing year onto the digits ("+14.2% since 2015" → 14.22015), and it now calls `num()` like everything else (A24.58) — the sign survives because A24.43 taught `num` to keep it, which was the only reason a second parser existed. A polygon with no usable figure takes a neutral `#e6e6e6` class at the same `fillOpacity` as every other and is NEVER omitted (D-NS16, John 2026-09-10) — a hole in a choropleth reads as a boundary, not as an absence — and the legend gains one row reading exactly `No data`; grey means UNMEASURED and only that, since a figure whose margin spans a band is still shown with its value (D-C36). Every polygon enters by ONE door, `areaVals`, which takes its colour from the design's own `bucket()` and its label from its own `fmtMetric()`, so the fill and the legend cannot disagree; the `rf-tip` is built ONCE, in `areaTip`, instead of twice by hand in the design and in `MarketMapView.vue`. The geometry is a committed fixture, `frontend/tests/design-boundary-fixture.ts`, generated from real `geo_area` rows by `scripts/export_design_boundaries.py` — 88 ZCTAs, 72 places and 5 counties whose centroid is inside CBSA 12420, simplified at 0.010° to fit a 120 KB cap and then REPAIRED (`ST_MakeValid` inside `ST_CollectionExtract(…, 3)`; D-C49, John 2026-09-11) so all 165 are valid and none is a GeometryCollection; the values are the design's own nine Austin community figures assigned to the nearest polygon, which is `mosaicCells`'s own assignment rule on real boundaries and invents no number. The committed bytes are the artefact and are pinned by SHA-256, because `ST_SimplifyPreserveTopology` is NOT reproducible (five distinct geometries from six identical queries) and A24.1 interpolates the literal verbatim into the approved design file, so a casual regeneration would move every map baseline for no design reason. **A24.13** is the family's other ruling, **D-C46** (John, 2026-09-11 — the growth class breaks are re-scaled to real ACS data, with a band below zero, so a declining area reads as declining rather than as the bottom of a growth scale): `stops: [10, 20, 35]` could not represent data that runs roughly −5 % to +15 % and had no band below zero at all — measured against ACS 2014–2018 against 2019–2023 place populations, the exact pair `population_growth_pct` divides, it put 79.9 % of US places of 10,000 people or more into ONE bucket while 30.5 % of them were declining, so real polygons would have shipped still one colour; the stops become `[0, 5, 15]` with buckets `Declining / 0–5% / 5–15% / > 15%`, the tertiles of the non-declining half of that distribution rounded to legend-readable numbers, and FOUR buckets rather than five because the growth ramp carries exactly four colours in all three palettes. `frontend/src/map/mosaic.js` and its nine cases are deleted; `MarketMapView.vue` draws `props.areas` through Task 2's `geoJson` engine primitive on the same shared canvas renderer. Seventeen approved states re-base — the fifteen desktop Browse captures, `browse-market-strip` among them, and the phone frame's `mobile-map` and `mobile-sheet` — and `baseline-manifest.json`'s thirteen frozen hashes did not move, which is the proof the change stayed inside Browse. **A24.14–A24.18** (Task 10) then wire that same drawing to the real API, in the seam A16 and A17 established: an app-only `market` adapter prop the reference never receives (`frontend/src/market/boundaries.ts`, resolving the metro by NAME through `/api/markets` because `logic.js` speaks in market names and knows no CBSA geoid), a `mdAreas` state key, one loader `loadAreas` above `componentDidMount`, and A16.1's own ternary in `md.areas` keyed on adapter PRESENCE rather than on data — with it the map draws the polygons `GET /api/markets/{cbsa}/boundaries` answered **or none at all**, whatever it answered, and never the design's fixture, because that fixture is the AUSTIN metro carrying the design's own nine figures and drawing it over a real metro would show the wrong city's outlines under numbers nobody measured. The loader clears before it asks and discards an answer for a market the member has since left, so neither a metro change nor two fetches resolving out of order can leave one city's boundaries painted over another's. **A refusal empties the shading and nothing else** — `MarketMapView.drawOverlay` paints the C7 drive ring before it reaches the polygon layer and `drawPins()` is a separate call, so the basemap, the pins, the rail, the filters and the detail all still render — which makes the endpoint's PRESENCE an operational precondition: this adapter must not reach an environment ahead of the route. Where the route exists but holds no values yet, the API still answers with real outlines and null values and those paint in the design's own `No data` grey. Zero approved states re-base: the oracle answers the app with the design's own polygons, derived from `areaSet` by `frontend/tests/design-boundaries.mjs` as the D6 stub is derived from `P`. **A25** (John, 2026-09-10 — Task MP1: "a listing with no coordinates keeps its place in the results and does not get a pin"): a published listing whose seller has not disclosed its location is served `lat: null, lng: null` (`app/api/listings.py`’s `serialise`; `location_disclosed` is `NOT NULL DEFAULT false` in `migrations/016_listing.sql`, so the nulls are the DEFAULT, not an edge case), and Leaflet 1.9.4’s `toLatLng([null, null])` returns `null` — the array branch is gated on `typeof a[0] !== "object"` and `typeof null === "object"` — so `_setPos` read `.lat` off a null and ONE such listing took pin drawing down for every listing after it, re-throwing from inside Leaflet’s own event loop on every later zoom pass. Six literal script edits: A25.1 the pin list, A25.2 the drive-ring centre, A25.3 the map’s community list and A25.6 the drive ring itself each skip a listing with no FINITE point (`Number.isFinite`, not `!= null`: a `NaN` reaches `toLatLng` intact) — the third because `mosaicBbox` takes `Math.min`/`Math.max` over those coordinates and `null` coerces to 0, which stretched the Austin metro box to the equator and the prime meridian, 100 million mosaic cells and a hung tab; A25.4 stops `marketPanel`’s last-resort community object standing in zeros for absent figures (D-C31, the one place in the panel A21 could not reach); and A25.5 gives the docked panel the `p8` term A21.4a dropped, so the panel and the detail agree about the design’s own "Community data unavailable" fixture. A25.6 is fix round 1's own (Important-1, "no point, no ring"): `showDrive` was `!!sel` with no coordinate term, so A25.2's restored else-branch made the C7 ring PAINTABLE for the first time and a 16 km dashed "ten minutes' drive" circle appeared around the middle of Austin — a missing pin omits, a ring centred on a place the practice is not fabricates. The listing itself keeps its place in the rail, the count, the sort and the filters — only the map skips it, and nothing is drawn at [0, 0] or at the metro centre, because the design has no treatment for "somewhere in this metro". Every design fixture and all eighteen seeds carry a point, so no approved state moves. **A27** (John, 2026-09-11 — rulings D-C38 and D-C39, the Community Context card’s geography). He was shown a Dallas practice headed "Specialty practice — Highland Park / affluent central" whose card read the same four numbers as every other Dallas listing, South Dallas included — median income $67,760 on all twelve — because all twelve sit inside one Census place and `serve.py`’s whole-row rule (D-C32, written for the Orlando listing in unincorporated Orange County and never asked what it does to a listing INSIDE a large city) served the place band to every one of them, while each practice’s own catchment figures sat materialised in `market_metric` and unreachable by any screen. He chose the option in which EACH TILE NAMES WHERE ITS OWN NUMBER COMES FROM, against the cheaper one that relabels every tile uniformly — which he rejected because it moves the defect one tile over rather than fixing it, the Growth number still being the whole City of Dallas under a caption saying otherwise. So `community_rows` now chooses per figure: the three area figures and the off-card `vets` come from the catchment band with `place` as the fallback, AS ONE GROUP (one `community_label` describes all of them, and a group drawn half from each band would put a city figure under a ring caption); growth and payroll are taken from whichever band carries them, because neither can vary by band at all — `materialize.py` computes growth once per listing OUTSIDE the band loop and writes that one value into all three (D12), and always writes the county CBP row. Two new payload fields say where the figures the label does NOT describe come from: `growth_scope` names growth’s own place-or-county geography with the name TIGER itself gives — "Dallas", "Orange County", no composed "City of " prefix, because level 160 loads the bare `NAME` and a Census designated place is not a city (the controller’s ruling on the implementer’s own concern, 2026-09-11), which needs the geoid lookup `serve.py`’s own docstring used to say was not wanted — superseded deliberately, `practice_location.place_geoid` joined to `geo_area.name` at summary level 160 — and `income_note` carries the approximate qualifier the contract has always asked for and the card had no slot for. **D-C39**: the ring is described by DISTANCE, not by time — the band is an 8 km straight-line buffer (spec §8), not a routed drive time, and true isochrones are still open for V1 (spec §15) — so the label reads "Within about 5 miles of the practice" and the two live sentences that said otherwise over PLACE-band figures are corrected in the same release. Eight literal edits: A27.1 gives the Median income tile `p.incomeNote || "Household, 2023"` (A21.5b/A21.5c took only the two sub-lines that said "the community", so the median was the one area figure whose caption could not follow its own geography — and it is the one that most needs it, a catchment median being a household-weighted median of tract medians (a weighted median since 2026-09-12, Task INCOME-MEDIAN; a weighted average before), never published and never suppressible); A27.2 gives the Growth tile its own sub-line, chained on A21.3d, naming the geography where the API supplies one and returning A21.3d’s own output byte for byte where it does not; A27.3 drops "(10 min drive)" from the docked panel’s Insights heading and A27.4 stops its footnote calling a straight-line catchment a drive time — the two entries with no null branch to preserve, because the default IS the false sentence, so they re-base the Browse states that render the panel while none of `baseline-manifest.json`’s thirteen frozen hashes moves (none is a Browse capture). THE HONEST MEASURE, which no summary of this may soften: four of the six served figures gain neighbourhood detail and THREE of the card’s four tiles do; the fourth gains an honest label and nothing more until the 2010–2020 tract crosswalk lands, and on the Browse map two of the three fill layers still paint one flat colour per city or county — **this is not "per-neighbourhood market data"**. The THIRD false sentence, the Browse Market data card’s "Figures describe the community around each practice…", IS corrected too, as **A27.5** (2026-09-11, ruled on the implementer’s own report): D-C39 named two sentences, but the figures that sentence describes are precisely the ones this release moves to the catchment, so leaving it would have meant one release corrected two false sentences and made a third one false by its own act. It carries its own id, its own ruling text and its own `LOCAL_AMENDMENTS.md` row rather than being folded silently into A27.4. The sentence lives inside the Browse "Market data" strip behind `v.md?.stripOpen`, which no approved state opened — a ruled change with no oracle, the same gap A26’s F2 closed for the More-filters popover — so `browse-market-strip` is appended to `screens.ts` in this release (D-C40), clicking the card’s own "Expand all six layers" control, and it is the third Browse state to re-base. **D-C42** (John, 2026-09-11), ruled on this same review and built on top of it: A21.5a let `communityLabel` REPLACE that heading, and D-C38 gives 28 of 29 QA listings a label — so on QA the heading read "Within about 5 miles of the practice" and the words "Market Overview" appeared NOWHERE, while A27.3’s own correction was invisible because its default was never reached. The ruling that produced Option C named exactly this as a cost of the Option B he rejected; it landed under Option C too and had never been put to him. He ruled the heading back and the geography onto its own sub-line beneath it, on the reasoning that this is the card’s own established idiom — A21.5b, A21.5c, A27.1 and A27.2 all put the geography under the value it describes, and the heading was the only place on the card where the label replaced the thing it was meant to qualify. A27.6 retires A21.5a’s `||` as a BEHAVIOUR (chained on A27.3, the A27.2/A21.5c pattern) while A21.5a’s own markup edit STAYS — it is what made the heading data, and the heading is still data — and A27.7 adds the sub-line, composed from the docked panel’s OWN `md.panel.place` declaration (`font-size: 12.5px; color: var(--vf-text); margin-top: 2px;`), the design’s one existing heading-and-geography pair, gated by `sc-if` so a listing with no label has no element rather than an empty one. It is not a softening of D-C38: the geography is still stated on the panel, in the place the rest of the card states it. Its oracle is the real-browser gate in `frontend/tests/smoke.spec.ts`, not an approved state — the design’s fixtures carry no `communityLabel` and the reference has no way to be handed one without editing approved fixture data or declaring a ninth prototype prop. **A27.8** closes what A27.7 left (John, 2026-09-11 — ruling D-C48, on the whole-branch review of this branch): A27.7’s ONE sub-line sits above the whole four-tile grid and three of the four tiles are the area group it describes, but the Population tile’s SUB-LINE is not a population figure at all — it is GROWTH, which `serve.py` measures at place-or-county and serves with its own `growth_scope`, reading −1.5% for the whole of Dallas, so the panel printed a city number under a caption describing a ring on 28 of 29 QA listings: the defect D-C38 removed, one card over, live on QA. John ruled it is named ON THE TILE, the way the detail card’s own Growth tile already names it (A27.2) and the API’s `income_note` qualifies the median (A27.1) — the design’s ` · ` joining a figure to what qualifies it — with the figure leading and the geography following, because unlike A27.2’s line this one begins with the number. Chained on A21.2d, inside whose guard the new term sits, so no figure is still no sub-line; the design’s fixtures carry no `growthScope`, so the branch is falsey and every approved state keeps its pixels. **A28** (John, 2026-09-11 — ruling D-C44: the ring is drawn at the distance the card names). D-C38 gives the Community Context card “Within about 5 miles of the practice” while `MarketMapV3.jsx` drew an unlabelled dashed circle at 16 000 m — about TEN miles — so a buyer read one distance and was shown a circle twice its size, on every listing with a point. Neither number is invented: V2 drew TWO rings (`docs/design-reference/design_handoff_practice_match_v2/MarketMap.jsx:160-169`), `drive10` at 16 000 m in `#339dde` and `drive5` at 8 000 m in `#003a70`; V3 replaced the panel that toggled them, kept ONE hard-coded ring, and kept the FAR radius in the NEAR ring’s colour. The radius becomes 8 000 m and the colour stays `#003a70`, so the number and the colour agree for the first time since the V3 panel rewrite. No visible control is added — John was offered V2’s two toggleable rings, composed into V3’s own “Market data layers” drawer, and chose against it: V3 deliberately replaced that panel, and the defect is the radius, not the absence of a toggle. Nine literal edits: A28.1 is the FIRST amendment in the programme’s history to edit `MarketMapV3.jsx` rather than the `.dc.html` (`file: 'jsx'`, the partition spec §9.2 added for A24 — corrected 2026-09-13: A24.9–A24.12 were first; A28.1 rides A24's `file: 'jsx'` partition), and A28.2–A28.4 delete the legacy panel’s orphans under the bundle’s own dead-code rule — `layerHelp`, `fillRows`, `overlayRows` and the `drive5`/`drive10` layer-default flags, each measured first as ONE declaration and ZERO template readers on either target, with the design’s own comment on the deleted line saying so (“GROUP 1 — retained for the legacy panel; the compact control above is canonical”). That removes the LAST “drive time” strings in the product, which is what D-C39 was reaching for and could not name correctly. A28.2–A28.4 left the helpers those rows called (`radioRow`, `layerRow`, `setValue`, `setLayer`) standing, because that ruling named the rows and the flags and nothing else; A28.5–A28.8 (controller amendment D-C45, 2026-09-11) close it — re-measured the same way, each occurring exactly once in `logic.js` and once in the amended design (its own declaration) and zero times in `App.vue` or any test — and delete the four helpers under the same dead-code rule, taking the D-C44 implementer's own "kept" test with them. **A28.9** (controller amendment, 2026-09-11, whole-branch review) is the last orphan the same deletion left: the `practices` member of the Browse layer defaults, whose only reader was A28.2’s own deleted `layerRow("practices", …)` — `SYMBOL_KEYS` is `["pets", "households", "competition"]`, so `activeSymbols` never asks for it, and the one other place `layers` is spread copies the key forward without reading it. What is left is exactly `SYMBOL_KEYS`; `md.practices`, the pin list the map component is handed, is a different declaration and is untouched. A25.6’s “no point, no ring” (2026-09-10) is untouched and keeps its test: the radius moves, the finite-point term that decides whether anything is drawn at all does not. `browse-market-panel` and `browse-panel-lightbox` re-base — the only approved states that select a practice with a point on a screen that renders the map (the docked panel’s lightbox scrim is `rgba(0,58,112,.55)`, so the map shows through it) — and none of `baseline-manifest.json`’s thirteen frozen hashes moves, none being a Browse capture. **A30** (John's ruling, Task PANEL-STALE, 2026-09-12 — root cause read from QA 0.1.21, `screenshots/qa-0121-dallas.png`: with GHI Veterinary Hospital, Austin, selected and its docked panel open, switching the metro to Dallas left the panel OPEN with GHI's header — "GHI Veterinary Hospital · $2.76M · Austin, TX" — over the FIRST Dallas listing's community figures, a false statement about a practice reachable in two clicks and present since 0.1.20, the design's own behaviour): `setMarket` (A13.1's class property) set `market`, `activeId` and `hoverId` but never `mdSel`, so `sel` — resolved by id alone, `P.filter((x) => x.id === s.mdSel)[0]`, market-blind — survived a metro change while `selComm`, filtered to the NEW market's own `communities()`, no longer matched it; `marketPanel`'s own fallback chain (`selComm || comms[0] || { …all undefined }`) then substituted the new market's FIRST community — `comms[0]`, with the design's own fixtures (Sacramento's `c1`-`c4`, Orlando's `o1`-`o4`, Atlanta's `g1`-`g4`), never A25.4's own empty-market literal, which stands in only when the market has no communities of its own at all and is untouched here. The design has no treatment for "the selected practice is not in this metro", and closing is the only honest state. One literal edit: `mdSel: null` joins the object literal `setMarket` already writes, beside `activeId: null` and `hoverId: null`. The design's own fixtures switch metro in no approved state with a panel open, so no frozen hash moves. **A32** (Task ADAPT-STALE-3, 2026-09-12 — measured on QA `db8bf67`, New York at 1,912 × 1,228: a metro switch pulled the whole metro TWICE, 24 requests and 7.85 MB gzipped): `setMarket` runs BEFORE the map has moved, so A24.18's request carries the box the OLD metro is still settled on; on a wide screen that box is past the route's span cap, the adapter's ladder falls back to the whole metro and pays for it, and `loadAreas`'s own guard then DISCARDS the answer because the settled view is the new metro's by the time it lands — A24.22's settled-view listener repeats the sequence and THAT answer is the one drawn. The first was never answerable, so it is not made: with a viewport-publishing adapter AND a metro whose CENTRE is somewhere else, `setMarket` sets `mdAreas: null` — the PENDING state A24.41 keeps the legend's ramp and geography line for, so nothing flickers — and A24.22's listener asks once, with the box the new metro actually settles on. The comparison is the centre and not the name because what decides whether there is a move to wait for is whether the map is going to move: the same metro re-selected, a metro `MARKETS` no longer holds (`load.ts` prunes it to what the API served, so `MARKETS[v]` can be undefined), and every path with no viewport adapter — the reference, the Claude Design preview, any build predating the bbox wiring — keep the design's own immediate load, because there is no listener to wait for and waiting would mean never loading. It is NOT a guess about which metro a box can see, and one was tried and withdrawn: the boundaries route is bbox-scoped and METRO-AGNOSTIC (`_BOUNDARY_SQL` filters on level, vintage and `ST_Intersects(geom, bbox)`, never on the CBSA — the `cbsa` path segment picks only the whole-metro fallback box, the cache key and the 404), so shading has always FOLLOWED a member who pans off the selected metro, and a client refusing on geometry would blank exactly that. One literal edit, chained on A24.18. Its companion is not an amendment at all: `src/map/viewport.ts`'s `publish(v, { force })`, armed by `MarketMapView`'s recentre watcher and never by a user pan, so two metro centres that snap to the same 1/8-tile cell cannot leave the map pending for ever. The design's own fixtures switch metro in no approved state, so no frozen hash moves. **A31** (John's ruling, Task SNAP, 2026-09-12 — ruling D-C50 as REVISED, in his own words: "if this needs 2 modes, for the 'AREA' / 'CITY' and when a user clicks a specific location it must render that facility market information — it should reflect boldly which is being viewed CITY/AREA vs LOCATION"): the Browse "Market snapshot" strip computed its "metro median" and its seven bars from `communities()` — one row per LISTING, each hospital's own five-mile ring — while the map beside it painted Census geography per layer. Audited on QA 304b80f, Dallas: the strip said Households **162K** where the map's tracts hold 0–5,988, and Competition **41** where its ZIP areas hold 3–16; the bars were the first seven hospitals in listing order (`vals.slice(0, 7)`), which is not a distribution at all. In the design's fixture era the map mosaic was built from those same listing figures, so both agreed by construction, and the tract map broke that identity without anything noticing, because the two surfaces are read separately. A24.44–A24.56 (D-C50's INTERIM) made the CAPTION honest about it — "· community level", the practice-area label — which was the right holding fix and is what this supersedes. There are TWO MODES now: **AREA**, nothing selected, is the metro as the MAP paints it — the median and the five quantiles over the polygons of each layer's own geography, from the new `GET /api/markets/{cbsa}/summary`, with the geography and the count named ("metro median · 1,791 Census tracts") — and **LOCATION**, a practice selected and its docked panel open, is THAT practice's own community figure, the same `comms` row the panel and the Community Context card already read, with the bars staying the METRO's distribution and the class the practice falls in kept at full strength while the rest take the strip caret's own `opacity: .6`, so the two modes read against each other instead of the card repeating a number the panel already shows. Closing the panel returns to AREA. The mode is stated first, in the design's own 800-weight Insights heading with the `md.panel.place` sub-line beneath it (A27.7's own source, so the strip gains no style the design does not already carry), and the two words are never shown at once. Sixteen literal edits — A31.1 `AREA_PLURAL` (the ruling's own four plurals, "Census tracts"/"places"/"counties"/"ZIP areas"; a label the table does not carry is printed as it comes, because a pluralisation RULE would produce "Place (city/town)s"), A31.2 the `mdSummary` state key beside A24.16's `mdAreas`, A31.3 `loadSummary` in `loadAreas`' own shape minus the viewport (the summary describes the METRO, so a pan cannot change it and it is asked for once per metro), A31.4/A31.5 its two callers beside `loadAreas`' own, A31.6 `summarySet()` — the design's OWN answer to the endpoint, measured over the polygons `areaSet` draws and quantiled by `percentile_cont`'s own rule, which is what keeps the reference and the app describing ONE distribution — A31.7 the three mode render values, A31.8 the whole `stripCards` block, A31.9 the orphan it leaves (`communities()`'s own `communityLabel`, A24.44's, whose only reader was `stripBasis`; the PRACTICE's `communityLabel` is a different field on a different object, read in four places, and is untouched), A31.10 the markup and A31.11 the footnote, whose "Figures describe the area around each practice, not the practice itself" became false by this release's own act (the A27.5 rule) while A24.20's growth caveat stays beside the new sentence byte for byte, and **fix round 1** (controller, 2026-09-13) adds A31.12 and A31.12b: in LOCATION mode the caption over a figure is THAT FIGURE's own geography — `growth` is measured at place or county and the API names it (`growth_scope`), `econ` is the county CBP row everywhere and always (`app/census/serve.py`: “`econ_k` is county everywhere and always”), so on 28 of 29 QA listings the ring sentence “Within about 5 miles of the practice” was false on two of the six cards, which is the defect D-C48 removed from the docked panel's Population tile one day earlier, one surface over — and the geography is named ONCE there, on the card's own note, while the `src` line carries the dataset alone (`metaSource` composes a line for a caller with no geography to name rather than leaving a dangling separator): measured, the basis printed TEN times on one strip, four cards printing it twice, and seven times after, each true; and D-C51, the same round's caption audit, gives the income card the API's own `income_note` where it serves one (“Within about 5 miles of the practice · approximate”), because a catchment median is a household-weighted median of tract medians — never published, never suppressible — and the detail card has carried that qualifier since A27.1 while the strip printed the bare ring label beside the same number. The same round settles the AREA caption too: it states exactly what the number IS and drops the word “metro” (“median of 503 Census tracts”), because the Census PUBLISHES a metro median at summary level 310 — 97,638 ± 1,163 for CBSA 12420 against this figure's 94,801 — so “$95K metro median” beside a published “$98K” gave a stakeholder a fourth number to explain; a card with NO figure carries NO caption, where it used to read “metro median · 0 ZIP areas” over a blank; and **A31.12c** gives `growth` the `dataset:` its five siblings carry (Minor 3), so its snapshot card and the map legend read “· Place (city/town)” rather than the vaguer “· community level” A24.45's split had left on the one layer it did not convert. The same round's **A31.13/A31.13b** take a bar's colour from the SAME door the polygons do (`bucket(k, v, true)`'s own `color`): the design's own pre-A31 expression `ramp(k)[Math.round(t * 3)]` can address only four classes, and `income`'s ramp carries FIVE colours, so the approved `browse-market-strip` drew income's five bars in four colours — classes 2 and 3 the same one, class 4 unreachable — beside a map painting five, which is the one thing A31.8's own comment said could not happen; the height keeps its `t` and `bucket()`'s return is unchanged, `t = i / (ramp.length - 1)` being injective in `i` within one ramp, so no index had to be added to it. FIVE bars, not the design's seven: five is what the endpoint publishes (p10/p25/p50/p75/p90) and the bar row is `flex: 1` per bar, so it divides whatever space it has — 38.4 px per bar at the card's 232 px minimum against 26.6 px at seven — and no approved layout moves; p10/p90 rather than the extremes because one outlying tract is not a class a summary should draw. TWO CORRECTIONS came from asking what the cards do on REAL Census values rather than on the design's own community figures, which is all any fixture carries: the bars are classed on the MAP's OWN breaks (`bucket`'s third argument, A24.25) because they ARE the map's polygons — a Census tract holds about 1,500 households and `VALUE_LAYERS` starts at 10,000, so the community scale puts all five quantiles in ONE class and draws five identical 6 px stubs, which is the 100.0-%-in-one-class collapse `AREA_LAYERS` was cut to remove on the map, re-imported one surface over — and the LOCATION highlight is drawn ONLY where the practice's figure and the metro's polygons are one measurement, a RATE or a MEDIAN, which is exactly the set `AREA_LAYERS` does not re-scale: a practice's five-mile ring holds 162K households and a tract holds 1,500, so marking that ring inside a distribution of tract counts would light the last bar for every practice in the country and mean nothing — this ruling's own defect, one card over. That narrows the BRIEF's sentence, never John's ruling, and is the one judgement in A31 that wants his eye. **Where the summary comes from is A24.14's ternary exactly**: the adapter's where there is one, `summarySet()`'s where there is not — which is why `browse-market-strip` re-bases (the AREA cards read the fixture POLYGONS' median now, not the LISTINGS') and `browse-market-strip-location` is appended (LOCATION mode had no approved state at all). The oracle answers the app with the design's own distribution, `frontend/tests/design-summary.mjs`, derived from `summarySet` as `design-boundaries.mjs` is derived from `areaSet`, so the two targets cannot diverge; both states are Browse captures, so none of `baseline-manifest.json`'s thirteen frozen hashes moves. THE HONEST MEASURE: this makes the strip describe the map, and the map's own geography is still a tract for three layers, a place for growth and a county for payroll — the AREA cards are no more granular than the layer they summarise. **A35** (John, 2026-09-13 — ruling D-C52: "allow a user to zoom in BELOW the level of the last actual map layer … the map allows user to zoom in as far as they want … this message is never seen"). The Browse basemap is Esri's Canvas/World_Light_Gray_Base with its matching Reference labels, and MEASURED on the day (every number re-fetched by an adversarial verifier) each service publishes its cache "from Level 0 … through Level 13" worldwide and "from Level 14 … through Level 16" in North America, with ZERO cached tiles at 17+ at all nine US points probed — for any tile past 16 Esri answers **HTTP 200** with a constant 2,521-byte grey JPEG reading "Map data not yet available", a SUCCESSFUL image, which is why `errorTileUrl` and `tileerror` (both of which fire on an `<img>` ERROR) could never have caught it. The design created both layers with `maxZoom: 18` and NO `maxNativeZoom` and the map with no ceiling of its own, so Leaflet 1.9.4 derived the map's 18 from the layers (`getMaxZoom` → `_layersMaxZoom`), `_getZoomForUrl` put 17 and 18 in the URL, and the tract polygons, the tooltip and the pin — canvas and marker layers with no zoom limit — went on rendering over the grey. Seven literal edits, all in `MarketMapV3.jsx` (`file: 'jsx'`, A28.1's precedent) and none chained: A35.1/A35.2 give each basemap the last level its own service is actually cached to (`maxNativeZoom` **16** for the gray canvas, **19** for World_Imagery — Esri's published US floor at 0.3 m, measured real at every probed point including rural Texas, while Dallas, New York, Atlanta and Denver reach 20 and Austin, Sacramento and Orlando 21, a depth that is per-area and not knowable client-side, so one level of native sharpness is given up for a guarantee the placeholder cannot appear anywhere); A35.3 makes the base layer REQUEST at that level and DISPLAY to **20**, Leaflet upscaling 2×/4×/8×/16× between them; A35.4 gives the labels the same native cap while they keep their own `maxZoom: 18`, so they soften to 18 and then HIDE (a GridLayer draws nothing above its own maxZoom while the base keeps the map's ceiling — 16× upscaled text is not legible, and the z16 reference tile at GHI is real but EMPTY at 872 B and zero opaque pixels, so the labels contribute nothing there either way); A35.5 gives the MAP an explicit `maxZoom: 20` so the + button's ceiling does not depend on which basemap is on; A35.6 writes the new cap FIRST on a basemap switch and then RESETS the layer — `setUrl(url, true)` and a `remove()`/`addTo(map)` — because `GridLayer.redraw()` moves `_tileZoom` to the new clamp but never calls `_resetGrid()`, the only place `_globalTileRange` is recomputed, so a redraw alone left the layer asking for z19 coordinates while holding the z16 world range and `_isValidTile` rejected every one of them: measured in Chromium as a switch to Satellite at zoom 20 that requested NOTHING and left the tile pane empty, against twelve imagery tiles for the same switch at zoom 10, where both services clamp to the same tile zoom. **Fix round 1** (2026-09-13) then gave that same entry the two things the reset had broken and the one it had never had. The credit is assigned BETWEEN `remove()` and `addTo()`, because `Control.Attribution._update()` rebuilds the footer from its own `_attributions` REGISTRY — written from `getAttribution()` at ADD time and cleared from it at REMOVE time, never from the layer's current `options.attribution` — so assigned on the line after `addTo` the first switch re-registered the OLD credit (A35.7's string never reached the footer on Satellite at all, a defect the pre-A35 code shared) and the second left BOTH credits in it for the life of the map, measured as "Tiles © Esri, Source: Esri, Vantor, Earthstar Geographics, and the GIS User Community" on the GRAY basemap; and a `setBase` for the basemap ALREADY on the map now returns, because `MarketMapView`'s watcher carries `status` among its deps and fires one at every mount — free while the line was `setUrl(sameUrl)`, which sets `noRedraw` itself, and not free once the reset is unconditional: 24 tile elements built against the label layer's 12, the A32 class of waste one surface over. The gate for each is behavioural and new: the e2e reads the RENDERED `.leaflet-control-attribution` text after a mount, a switch and a round trip, and counts the tile elements a mount actually builds; and A35.7 replaces the satellite attribution with the service's own current `copyrightText` — "Source: Esri, Vantor, Earthstar Geographics, and the GIS User Community", Maxar being the vendor's former name — VERBATIM rather than composed from, the rule `dataset_registry.attribution_text` already follows, while the gray canvas's "Tiles © Esri" is untouched. `detectRetina` stays unset, deliberately: it adds a zoomOffset WITHOUT touching `maxNativeZoom`, which is the same defect by another door. Why no gate caught it: `frontend/tests/harness.ts` answers every arcgisonline request with a transparent 1×1 GIF at any zoom and every approved state captures at z10 (desktop) or z9 (phone) with no zoom step, so `frontend/tests/smoke.spec.ts` now walks real Chromium from 10 to the ceiling and records every tile URL on both basemaps. Zero approved states re-base and none of the thirteen frozen hashes moves — `_clampZoom(10)` is a no-op, no capture selects Satellite, and the harness blanks every tile. Thirty-one families, 310 entries: A1's 24 derived edits plus 286 literals; `LOCAL_AMENDMENTS.md` carries one row per amendment and the test pins the set both ways (count and ids). `docs/design-reference/design_handoff_practice_match_v2/Practice Match V2.dc.html` remains the **pre-V3 oracle** — what a suspected regression is diffed against, not what the gates compare to. Every visual and DOM oracle is regenerated from the amended V3 design. **A24.19–A24.20** (controller, 2026-09-12) then moved the granular layer to the **Census tract**: `income` draws summary level **140** labelled "Census tract", nationwide, while `growth` (Place) and `econ` (County) deliberately do NOT follow it — a tract-level growth figure is not computable across the 2010→2020 tract boundary change (plan D12, a registered Phase C deferral) — so each layer keeps its own `geo_label` and the Data Layers caption gained "Population growth is measured for the surrounding city or county, not the tract." Both entries are CHAINED (A24.19 reads A24.2's output, A24.20 reads A24.7's). `app/api/market.SHADING` is the single source and `tests/census/test_design_shading_labels.py` pins the design against it, so the map can never label a geography it is not drawing. The design's boundary fixture was regenerated at level 140 — **503 tracts** for the Austin metro where there were 88 ZCTAs — which moved `SIMPLIFY_DEG` to 0.005 and `MAX_FIXTURE_BYTES` to 280 KB; that cap is a BUNDLE budget, because A24.1 interpolates the payload into the design's `state` literal and `frontend/src/logic.js` ships it (measured after: main bundle 165.7 KB gzipped against `bundle-budget.test.ts`'s 220 KB). **A24.21–A24.23** (controller, 2026-09-12) then close the last gap that ruling left: the route has taken a `bbox` since Task 9 and A24.15's loader never sent one, so every request was for the whole metro ENVELOPE — at tract scale **5,935 tracts in New York** where the Browse map's own view holds 3,706, and on the stakeholder's 1460 × 1228 screen **7,470**. **The caps were re-measured for Census tracts the same day (2026-09-12, Task CAP)**: `MAX_FEATURES` 4000 → **12000** and `MAX_BODY_BYTES` 2_000_000 → **6_000_000**, both measured on QA's own tract geometry through the route's own `_BOUNDARY_SQL` and `compose()` byte arithmetic — 12,000 clears 9,767, the densest 4° × 4° box anywhere in the country (the New York–Philadelphia corridor, found by sweeping the lower 48 on a half-degree lattice) and therefore the largest `MAX_BBOX_DEG` admits; 6,000,000 clears 4,269,628, the largest FIRST VIEW at a delivery tolerance of 0.78 CSS px of latitude. `MAX_BBOX_DEG = 4.0` is unchanged and is what refuses a state, which **retires the old ordering argument** ("below the 6,884 tracts a whole-Texas box returns") outright: a whole-Texas box is ~13° on a side and is refused on SPAN before a row is counted, and the largest first view is 7,530, above 6,884. Until that measurement the metro request was a 422 outright and New York opened blank; it is served now, and what the bbox buys is the size of the ANSWER rather than the difference between a map and a blank one. `frontend/src/map/viewport.ts` is the new channel: `MarketMapView` publishes the map's own bounds at mount and on Leaflet's `moveend`/`zoomend`, and `src/market/boundaries.ts` reads them. The box is PADDED by 0.3 of the span on every side — measured, not chosen: that is the padding the engine's own shared canvas renderer is built with, and `Renderer._update` paints nothing beyond it during a drag, so a larger box buys no pixel — then snapped OUTWARD to a grid of one eighth of a 256 px basemap tile (32 CSS px, `360 / 2^(zoom+3)`), so the same ground always produces the same query string and therefore the same 24 h cache entry, instead of the centimetre-scale float jitter `getBounds()` returns on every `moveend`. The debounce is **250 ms**, Leaflet's own zoom-transition settle (`leaflet-src.js:4827`, `leaflet.css:198`), and a publish whose snapped box has not moved notifies nobody, so `invalidateSize()` and a selection pan cost no request at all. Three script edits, every one of them CHAINED: A24.21 rewrites A24.15's whole `loadAreas` — it reads `market.viewport()` ONCE and uses that one value both as the `bbox` it sends and as the token an arriving answer is checked against (A24.15's market guard extended, never replaced), KEEPS the polygons up while a pan reloads (a pan is the same city; a metro change still clears) and asks for NOTHING until a map has published a box, so the boot no longer fires the doomed whole-metro request; A24.22 subscribes in `componentDidMount` beside A24.17's own load; A24.23 unsubscribes in `componentWillUnmount` beside A13.5's three document listeners. An adapter with no `viewport` — the reference, the Claude Design preview, any older build — takes the old whole-metro path unchanged, both new terms being guarded on the method's presence, so every approved state keeps its pixels and none of the thirteen frozen hashes moves. The padding is not free and the cost is recorded rather than hidden: measured on real TIGER tracts (31,252 rows, ten states), a 1440 × 940 Browse map centred on the New York CBSA centroid at zoom 10 sees **3,706** tracts unpadded and **4,811** padded — so the adapter retries ONCE, unpadded, on the route's own `AREA_TOO_LARGE`, which is doing what that refusal literally asks for ("Zoom in or pass a smaller bbox") and not a workaround; a box wider than `MAX_BBOX_DEG` (`BBOX_TOO_LARGE`, a view of more than one metro at a far zoom) falls back ONCE to the whole-metro request this adapter used to make, so a zoomed-out small metro keeps its colour instead of losing it to this change; anything no box can fix — `BAD_LAYER`, a 401, a 404 — is never retried and leaves the map in the SAME unshaded state Task 10 already photographs in `smoke.spec.ts`. The cap numbers are never restated client-side: the SERVER says which one it hit and the adapter answers that code. A Manhattan-centred zoom-10 view is 4,425 tracts even unpadded and stays refused — the route's own "zoom in" — while at zoom 11 it is 3,942 padded and served. `tests/census/test_boundaries.py::test_the_viewport_bbox_narrows_new_yorks_answer_and_both_arms_now_serve` pins both arms through the real route at the real caps, and `test_the_default_new_york_view_serves_at_a_sub_two_pixel_tier` pins the stakeholder's own first view at a delivery tolerance no member can see. `header-1100` and `header-1000` are not among the thirteen: they are Browse screenshots, and `mobile-map`, the Browse states and the two header states change by design. **AMEND-GUARD** (Task HOUSEKEEPING-B, 2026-09-13) is what keeps this ledger honest about what it TAKES AWAY: `frontend/tests/amend-guard.ts` walks every line and every prose sentence an entry's `replace` introduced and fails unless it is still in the amended design or a later row DECLARES the removal, in two tiers — the LINE tier takes the token `consumes <id>` (or the stronger `supersedes <id>`), the SENTENCE tier takes only `supersedes <id>` or, on the entry being retired, `superseded by <id>`, which is A10 and A10.2's own vocabulary. A bare mention of an id is not a declaration and neither is a citation: at `db8bf67` A24.56's row both cited A24.20 and quoted the sentence it was dropping, and A24.20's growth caveat left the product all the same — which is the defect this gate exists for, and 44 of the 68 consumed-line pairs were passing on an incidental mention until the token rule landed. `LOCAL_AMENDMENTS.md`'s own header says how to write a row and a citation.

## Non-negotiables (from po.vin / rounds.vin, still true here)

- **Surgical diffs.** The change contains the ask and nothing else. Never remove a function or feature while doing unrelated work. No drive-by refactors or reformatting.
- **No destructive actions** without explicit instruction in the current conversation.
- **Verification gate before every production deploy — all four:** (1) `poetry run pytest` + `npm run typecheck && npm run build && npm test` (`npm test` is `vitest run --coverage`, the exact step CI runs — a local gate weaker than CI let 0.1.21 ship with the Quality workflow red, 2026-09-13) (**build BEFORE test**: `frontend/tests/vue-only.test.ts` and `bundle-budget.test.ts` read `frontend/dist/_app`, so the documented order could never pass on a fresh worktree or a cold CI runner — corrected 2026-09-12 after it cost an implementer a cycle) and the Quality workflow green on the pushed SHA (`gh run list --branch main --commit <sha>`) — four jobs `success`, read, not assumed; (2) `npm run test:visual:baselines` then `npm run test:e2e` (the `app` project: visual + DOM oracle + smoke) green — the DOM oracle runs under neither `test:smoke` nor `test:visual`, and it is the secondary proof of zero regression on the thirteen non-Browse screens (byte-identical to V2 from V13 until the launch removal, A6/D-I8-6, moved all 28 states by ruled design change); (3) click-through on https://qa.foundation.vin of the changed flow; (4) post-deploy smoke on https://foundation.vin (`scripts/verify-deploy.sh production`). Verified = ship; no need to ask.
- **Push every commit to both remotes:** `origin` (vin-swe/practice-match) and `production` (johndean/practice-match). Conventional commits; trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- **Versions in lockstep:** `frontend/package.json` and `pyproject.toml`, one patch per release (`tests/test_versions.py`).
- **Close the loop:** forwardable plain-language summary + screenshots of the live screens + a one-line engineer's note with any risk.

## Legally load-bearing (from the approved design and the Census spec)

- **Attribution stays visible** on every map ("Tiles © Esri" today; whatever the VIN Foundation's final basemap licence requires) and under Community Context ("Source: U.S. Census Bureau, …"). Attribution strings will come from `dataset_registry.attribution_text`, not hard-coded, once Sub-project 3 lands.
- **Blocked datasets never ship.** Pet-ownership incidence (licence unresolved) and third-party practice-location data must not be ingested or displayed until the VIN Foundation clears them. The admin Data Sources tab shows this gate; keep it.
- **Open licence question — one decision record.** Esri (the approved design) vs CARTO (the Census spec) is **one** open question, recorded in the Census plan (`docs/superpowers/plans/2026-09-05-practice-match-census-data-layer.md`, "Basemap licence — one decision record") and referenced, not restated, here. Do not swap either way without the VIN Foundation's decision.

## Launch-removal list (executed in Task I8a, with real auth)

**Done, as ruled design amendments** (A6.1–A6.6, A7.1–A7.2 — the list left the DESIGN, so the oracle and the app lost it together and every gate kept holding): the prototype jump bar markup and its script (`jumps`, `jumpTo`, `showPrototypeBar`) · the "Prototype — access states" shortcuts and their `gateStates` array · the pre-filled demo credentials and the pre-filled application · the jump bar's "Mobile view" toggle and the now-dead `viewportLabel`/`toggleViewport`.

**Still there, deliberately.** All eight prototype props stay **declared** and none is passed by the app (D-I8-2): `frontend/tests/app-generated.test.ts` requires `app.setup.js` to declare every prop the design declares, so dropping one fails that gate. `startScreen`, `startGate` and `me` are the REFERENCE's way into a state — `frontend/tests/reference-server.mjs` injects them per request from `?props=`, which is how one navigation puts it signed in on the named screen. `prototypeBar` is declared for that parity check alone: A6.4b removed the only thing that ever read it. Wave 2a's account screens added two more the same way: `startNotice` (A8.8b) hands the reference the five sign-in-card notices, and `startAnswerNote` (A9.1a/b, controller amendment A-S5) hands it the applicant-answer card's seeded note — both default to `""`, both reached through the same `?props=` mechanism, and the app never passes either. Wave 2b's seller lifecycle added the eighth the same way: `startMyListings` (A16.11a/b, controller amendment A-SL22 (1)) hands the reference the seller's loaded listings, which is the only way to the EMPTY dashboard A-SL17 put in the oracle — it defaults to `null`, "nothing was handed over", and the app never passes it either. `startViewport` the app takes from the URL only (`?viewport=mobile`), which is how the prototype's 390×800 phone frame stays reachable until a responsive design exists (D-I8-7; the toggle that used to set it lived in the jump bar). Fixture data in `logic.js` (`P`, `MARKETS`, `VETS`, `ECON_K`, `sellerListings`, `requests`, admin rows, `state.me`) also stays — keep field names; the UI reads them: the API replaces `P` and `MARKETS` at boot (`frontend/src/main.ts` → `frontend/src/listings/load.ts`), and the fixture arrays stay in the script as the D6 stub's source for the gates. `sellerListings` and the admin rows still stay the same way — the D6 stub's own source, field names unchanged — but the app installs the seller's real listings and the real review queue over them at boot when the `listings`/`adminListings` adapter is present (A16.1's dashboard ternary and A17.1's Listings-tab ternary, both keyed on adapter presence, not on a boot-time replacement of the fixture array itself).

## Layout

`frontend/` Vue app · `frontend/tests/` Playwright (`screens.ts` = the 55 approved states) · `app/` FastAPI (`api/health.py`, `static.py`, `checks.py`, `tasks/celery_app.py`, `storage.py`, `media/encode.py`, `api/seller_listings.py`, `api/admin_listings.py`, `api/market.py`, `census/` (the market-data pipeline)) · `migrations/` numbered SQL (ledger runner `scripts/migrate.py`) · `seeds/` the demo hospital data and photographs (`hospitals.json`, `hospitals/photos/`) · `scripts/` `start.sh` (roles api|worker|migrate|seed), `deploy.sh`, `verify-deploy.sh`, `verify-image.sh`, `seed_listings.py`, `prepare_photos.py` · `tests/` pytest · `docs/design-reference/` the handoff bundle (never shipped) · `docs/integrations/market-data-api.md` the Census market-data API's integration contract for Sub-project 2 and the admin Data Sources tab (Task B6; kept honest against the routers by `tests/api/test_contract_doc.py`) · `docs/superpowers/{specs,plans}/`.

## Common operations

```bash
docker compose -f docker-compose.dev.yml up -d && poetry run pytest -q -W error --cov=app --cov=scripts --cov-branch --cov=tests/e2e -m "not timing" && poetry run pytest -q -W error --cov=app --cov=scripts --cov-branch --cov=tests/e2e -m timing -p no:randomly --cov-append --cov-report=xml --cov-fail-under=100   # backend gate, exactly as CI runs it — timing-budget tests (tests/test_timing_marker.py) run serially, last, coverage appended (Task CI-TIMING)
cd frontend && npm run typecheck && npm run build && npm test                  # frontend gates — BUILD FIRST: vue-only and bundle-budget read dist/_app; npm test runs coverage at 100 % all four, exactly as CI
docker compose -f docker-compose.dev.yml up -d && cd frontend && npm run test:visual:baselines && npm run test:e2e   # oracles from V3, then visual + DOM + smoke (the app project starts the API against the compose Postgres/Redis)
cd frontend && npm run gen:design && npm run gen:app && npm run gen:logic   # the design bundle, in the order it flows: pristine + ruled amendments -> V3.dc.html, then its template -> App.vue/pseudo.css and its script -> logic.js (generated, never hand-typed)
scripts/deploy.sh QA && scripts/deploy.sh production                           # after the gate
python scripts/seed_listings.py                                                # seed/refresh the QA demo hospitals — run inside the api container (see DEPLOY.md "Seeding the demo hospitals (QA)")
railway logs --service api --environment QA --lines 50
railway variable list --service api --environment QA --json | python3 -c 'import sys,json; print("\n".join(sorted(json.load(sys.stdin))))'   # names only — never pipe values to a terminal
```
