# Local amendments to the V3 Rev 2 design (spec D15)

`Practice Match V3.dc.html` = `Practice Match V3.rev2.dc.html` (pristine, never edited) + the entries below, applied by `frontend/tests/design-amendments.ts` and proved byte-for-byte by `frontend/tests/design-amendments.test.ts`. They retire when a re-issued bundle carries them.

| Id | Date | John's ruling | What changes |
|---|---|---|---|
| A1 | 2026-09-07 | keep the V2 header and do not restyle header or fonts | 24 template elements: every display-size heading paired with V2 (by tag, text and size) takes V2's `text-transform`/`letter-spacing` values in place; the key-fact values `{{ m.v }}` and the 28 px mobile asking price return to V2's uppercase `.005em`; `{{ resultHeadline }}` is untouched (V3's only occurrence, in the mobile list, already equals V2's). No script, no `_ds/**`, no site header (byte-identical V2↔V3 already). |
| A2 | 2026-09-07 | resolve this — the mobile practice card opens the detail (spec D17) | One line of the design's script: the mobile results card's `open` handler. Root cause: it set `browseSel`, which C13 left nothing to read once it removed the peek card that used to display it, so the tap was a no-op; `open` now navigates straight to the detail screen, the same way V2's card and C13's own second-pin-tap already do. |
| A2.2 | 2026-09-07 | the amended handler’s comment now matches what it does (zero-gaps review) | The one-line comment above `results[].open`. Root cause: C13 left the "select into the docked side panel" comment in place after the peek card it described was removed; it now says what the handler does since A2 (open the detail). |
| A2.3 | 2026-09-07 | delete the orphaned browseSel helpers (zero-gaps review, spec D8/D12) | Deletes `hasBrowseSel`, `closeBrowseSel` and `bsel` outright. Root cause: all three read or wrote `browseSel`, and C13 removed the peek card that was their only reader in the template — nothing else in the design or the port referenced any of the three. |
| A2.4 | 2026-09-07 | drop the dead browseSel key, keep the live activeId key (zero-gaps review, spec D8/D12) | The top-level `selectMarker` (not `mobileVals.selectMarker`, which C13 already fixed) drops the `browseSel:` key from its `setState` call and keeps `activeId:`. Root cause: same orphaned key as A2/A2.3 — this handler is not wired to any template prop, but it still wrote `browseSel` on every call. |
| A2.5 | 2026-09-07 | delete the unwired top-level selectMarker (zero-gaps review, same dead-code rule as A2.3) | Deletes the top-level `selectMarker` (the one A2.4 trimmed) outright. Root cause: it was never wired to any template prop — the only `on-select` bindings in the design are `mob.selectMarker` (mobileVals, C13's own fix) and `md.selectFromMap`; nothing else in the design or the port referenced the top-level one. |
| A3 | 2026-09-07 | update across the application "view full market report" to "View full listing" | One template text node (V3:852): the docked panel's Insights-tab primary button, `"View full market report"` → `"View full listing"`. The other tabs' `"Open full listing"` (V3:866) — not part of this ruling. Unified by A11 (2026-09-08). |
| A4 | 2026-09-07 | if user clicks + Compare that action closes the 'What this means' card; when X Compare is clicked it closes the compare and the card appears again (spec D21) | One condition added to the design's script: the `insightOpen` IIFE (the floating interpretation card's gate) now also requires `!s.mdCompareOpen`. Opening Compare hides the floating card; closing Compare (the same toggle, whose note reads "close" while open) shows it again unless the member already dismissed it. No markup or style change. |
| A5.1 | 2026-09-07 | spec §"Prototype wiring" step 2: the prototype signs in through the API; the reference keeps the fixture path because it has no auth prop | The `signIn` handler in `renderVals()`: with a `this.props.auth` adapter it calls `signIn(email, password)` and sets `auth`/`screen`/`email` plus the header strings from the answer, or renders the SERVER's own refusal message and stays on the gate; with no adapter — the reference, and the Claude Design preview — the design's fixture path runs unchanged. The empty-form validation above it is untouched. |
| A5.3a | 2026-09-07 | spec §"Prototype wiring" step 2: sign-out ends the real session, and resets whatever the network did | The `signOut` handler is wrapped in the adapter's promise (`.catch(() => {})`, so the reset happens whatever the network did — a member left looking signed in with a revoked session is the worse failure). |
| A5.3b | 2026-09-07 | closes the parenthesis A5.3a opened (same ruling) | The closing `})` of that handler's `setState` object becomes `}))`. |
| A5.4 | 2026-09-07 | the app reads the signed-in account on load; the reference reaches a gate state through the startGate prop (A-I8 / A-I8.1, spec §"Prototype wiring" step 1) | `componentDidMount` gains five lines: the `startGate` prototype prop (`{ screen: 'gate', gate: <prop> }`) and the account lifecycle mapping for `this.props.me` — `active` → signed in on Browse with the header strings from `/api/me`, `pending`/`needs_review` → the "under review" gate, `declined` → the "not granted" gate, `verified` → the application gate. `unverified` is deliberately unmapped (nothing to show it until I8c's "check your email" exists — that card landed in Task S4 and A8.3b maps it). The reference and the Claude Design preview pass no `me` and take the design's fixture path unchanged. |
| A5.6 | 2026-09-07 | the reference needs a prop-driven way into a gate state once the access-state shortcuts leave (A-I8, D-I8-3) | One entry in the escaped `data-props` JSON, spliced immediately after `startViewport`: `startGate`, an `enum` of `signin`/`apply`/`pending`/`rejected` defaulting to `""`, section "Prototype", label "Start on gate state". The bundle's runtime hands it to the Root as a default (support.js `parseDataProps`), which is what lets `tests/reference-server.mjs` drive a gate state through `?props=`. |
| A5.7 | 2026-09-07 | the reference must render the same account the app does, and the design's copy does not change (A-I8.2, D-I8-8) | One entry in the escaped `data-props` JSON, spliced immediately after `startGate`: `me`, an object prop defaulting to `null`, section "Prototype", label "Signed-in account". A5.4 makes the header render `/api/me`'s computed `role`/`initials`; the app has a session and the reference has none, so the reference is handed the same account through the design's own prop mechanism (`tests/reference-server.mjs` injects it from `?props=`) rather than by editing the fixture — which John's rule for this wave forbids. `support.js` copies a `default` verbatim, so an object and a `null` both arrive unchanged. |
| A6.1 | 2026-09-07 | CLAUDE.md's launch-removal list, executed with real auth: the prototype jump bar's markup | The prototype jump bar's whole `<sc-if value="{{ showPrototypeBar }}">` block leaves the template, plus the blank line it left behind. The "Mobile view" / "Desktop view" toggle lived inside it and goes with it; the phone-frame presentation stays reachable through `startViewport` (D-I8-7). |
| A6.2 | 2026-09-07 | CLAUDE.md's launch-removal list: the "Prototype — access states" shortcuts | The "Prototype — access states" block on the sign-in card — the dashed-border element, its label and its `<sc-for>` over `gateStates`. The two status gates are reached by BEING in that state now (A5.4 maps the account), which is what `pending@` and `declined@` are seeded for. |
| A6.3a | 2026-09-07 | CLAUDE.md's launch-removal list: the pre-filled demo credentials | `state.email`/`state.pw` become empty strings. The design pre-filled a real-looking address and a masked password so the prototype's Sign in worked on the first click; with A5.1 that click reaches the API. |
| A6.3b | 2026-09-07 | the same, where signOut restores them (CLAUDE.md's launch-removal list) | The masked password in the `signOut` reset becomes an empty string. |
| A6.3c | 2026-09-07 | the pre-filled application, including an affirmation nobody made (CLAUDE.md's launch-removal list) | `state.apply` becomes empty with `affirm: false`. Field names kept — the UI reads them. |
| A6.4a | 2026-09-07 | CLAUDE.md's launch-removal list: the jump bar's own array | The `const jumps = [...]` array the removed bar rendered from, plus the blank line it left behind. |
| A6.4b | 2026-09-07 | the flag the removed sc-if read (same ruling) | `showPrototypeBar: this.props.prototypeBar !== false,` leaves `renderVals()`. |
| A6.4c | 2026-09-07 | the jumps key in renderVals()'s return (same ruling) | `nav, jumps,` → `nav,`. |
| A6.4d | 2026-09-07 | the handler the removed buttons called (same ruling) | The `jumpTo` class property is deleted, with one adjacent newline. |
| A6.5 | 2026-09-07 | CLAUDE.md's launch-removal list: the access-state shortcuts' own array | `gateStates: [...]` (the three shortcut buttons) leaves `renderVals()`. |
| A6.6a | 2026-09-07 | a dead mapping is dead code (spec D8/D12, A-I8.1): nothing reads viewportLabel after A6.1 | `viewportLabel` leaves `renderVals()`: the "Mobile view" / "Desktop view" button that read it was in the jump bar A6.1 removed. |
| A6.6b | 2026-09-07 | the same for toggleViewport, the handler the removed button called (A-I8.1) | `toggleViewport` leaves `renderVals()`. `isDesktop`, which reads the same `s.viewport`, is untouched — `startViewport` still drives the phone frame (D-I8-7). |
| A7.1 | 2026-09-07 | spec §Sign-in: the design's 'VIN username' copy changes to 'Email' (design delta) | The sign-in card's field label: `VIN username or email` → `Email`. The API authenticates an email address and knows nothing about VIN usernames. |
| A7.2 | 2026-09-07 | the same copy in the empty-form message (spec §Sign-in) | `"Enter both your VIN username and password."` → `"Enter both your email and password."`. |
| A7.3 | 2026-09-08 | spec §4.1: the sign-in card offers "Forgot your password?" beside "Request access" | One template text node: the sign-in card's footer line becomes `Not approved yet? Request access · Forgot your password?`, the second link opening the new `forgot` gate. `gate-signin` is the only screen this moves, and it is not one of the thirteen V2-oracle screens. |
| A7.4 | 2026-09-08 | spec §4.2: "remove the VIN language" from the sign-in card's sub-line | The sign-in card's sub-line: `Use your VIN credentials.` → `Use the email and password you registered with.` The API authenticates an address and a password of its own; A7.1 already did the same for the field label. |
| A8.1a | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ('approved') | The `form` value gains the notice slot: `error: !!(s.formError || s.formNotice)`, `errorText: s.formError \|\| s.formNotice`. Five outcomes whose only next step is signing in speak through the sign-in card's EXISTING message box rather than a status card that would have shown two identical buttons; the card's markup is untouched and a refusal still wins over a notice. |
| A8.1b | 2026-09-08 | spec §4.3 (agreed): the status cards' secondary button ends the session before it shows the sign-in card — and an applicant has a session too | `goSignin` signs a signed-in visitor out through the adapter before it shows the card (the label and therefore the pixels are unchanged), and `goForgot`/`goSignup` join it as the two new ways into the account cards. |
| A8.1c | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ('approved') | `goApply` sends an anonymous visitor ON THE APP to `signup` (they have no account yet) and a signed-in `verified` account to the application form. The reference has no adapter and keeps the design's own path to `apply`, which is what leaves `gate-apply` on its pixels. |
| A8.2 | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ('approved') | `state` gains `formNotice`, `gateToken` and the five empty form sub-objects (`signup`, `forgot`, `reset`, `invite`, `answer`). All empty, like A6.3's: nothing is pre-filled, and `gateToken` is where the router parks an incoming `?token=` so the app never writes one into a URL of its own (spec S3). |
| A8.3a | 2026-09-08 | S2 review rider (2026-09-08): a token-bearing gate value wins over the active-account redirect | The `active` branch of `componentDidMount` is guarded with `&& !["verify", "reset", "invite"].includes(this.state.gate)`. A5.4 sent every `active` account straight to Browse, and an approved member who follows a reset or invitation link is an `active` account — they would never have seen the page the link was for. |
| A8.3b | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ('approved') | The rest of the bootstrap: `unverified` → the check-email card (A5.4 left it deliberately unmapped because this card did not exist yet); `needs_review` keeps A5.4's synchronous "under review" card and switches to the answer card when `GET /api/applications/me` arrives; `declined` pre-fills the application form from the applicant's own last answers (spec §3, "Re-apply needs no new screen"); `startNotice` puts the reference on the sign-in card with a message; a `/verify` landing posts its token and shows the notice or the expired card, and a landing with NO token shows the expired card outright, spending no request (controller ruling, 2026-09-08 — there is nothing to verify without a token, and posting an empty one bought a 400 from a rate-limited endpoint). Every API call is guarded by `this.props.auth`, so the reference takes the design's fixture path unchanged. |
| A8.4a | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ('approved') | `gateStatus` widens from `pending`/`rejected` to every status value, so the four new status states render through the card the design already ships and add no markup at all. |
| A8.4b | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ('approved') | Four `statusMap` entries (spec §3 rows 2, 3b, 5c, 8): `check-email`, `verify-expired`, `reset-expired`, `unavailable`, each with the grey `headStyle` `pending` and `rejected` already use, byte for byte. "Send it again" has two branches (fix round 1, A-S4.1): it re-posts the sign-up when the visitor got here by signing up and still holds the password, and calls `POST /api/auth/verify/resend` — which needs none, only the session — when it does not and an account is loaded, which is how somebody who reached this card by SIGNING IN as an unverified account arrives. Before that endpoint existed the second case posted an empty password and the uniform 202 answered without issuing anything. |
| A8.5 | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ('approved') | The form values, setters and submits for the five new cards, plus `goSignOut` for the answer card's footer link. Each mirrors the design's own `form`/`setEmail`/`setPw`/`signIn` trio, returns the adapter's promise, and renders the SERVER's own message. The client checks only that both fields are present and the two passwords match — before the request, so a mistyped confirmation cannot burn a single-use token; strength is the server's (zxcvbn ≥ 3, HIBP). `TOKEN_INVALID` is `app/api/auth.py`'s own code, and all three consume paths raise the same `TokenInvalid`. A successful reset or accept-invite clears its sub-object along with `gateToken`, so the plaintext password does not outlive the request that set it (fix round 1). |
| A8.6 | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ('approved') | `submitApply` posts `POST /api/applications` through the adapter, mapping the prototype's short field names to the API's (`vin_member_id`, `school_year`, `license_state`). The design's own required-field check above it is untouched and still runs first; with no adapter the fixture transition to the pending card is exactly what it was. |
| A8.7 | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ('approved') | The five form cards' markup (`gateSignup`, `gateForgot`, `gateReset`, `gateInvite`, `gateAnswer`), appended in the gate's right column after the existing `gateStatus` block. Every element is the sign-in or apply card's — shell, band header, `<label>`/`<span>`/`<input>`, the grey left-bordered error box, the 48 px primary button, the 14 px centred footer line — with only bindings and text changed; `design-amendments.test.ts` proves every `style` value already appears on the design's own gate card and (A1's two ruled declarations aside) on the pristine one. The four status states get no block at all. |
| A8.8a | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ('approved') | `startGate`'s enum in the escaped `data-props` JSON grows from A5.6's four values to all thirteen, which is how the reference reaches each new state through `?props=`. |
| A8.8b | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ('approved') | One entry in the escaped `data-props` JSON, spliced immediately after `me`: `startNotice`, a text prop defaulting to `""`. Five account outcomes end on the sign-in card with a notice, and the reference has no API to produce the outcome — so the oracle hands it the notice through the design's own prop mechanism. `app.setup.js` declares it too (`app-generated.test.ts` requires the design's props to be declared); the app never passes it. |
| A9.1a | 2026-09-08 | A-S5 (2026-09-08): the reference reaches the applicant-answer card's note through a declared prototype prop, `startAnswerNote`, exactly as `startNotice` reaches the sign-in notices; the app never passes it. | One entry in the escaped `data-props` JSON, spliced immediately after `startNotice`: `startAnswerNote`, a text prop defaulting to `""`. The applicant-answer card renders the reviewer's question as its own element, fed by `GET /api/applications/me` — a call `this.props.auth` guards, and the reference has no adapter — so measured against the real `Component` the app rendered the seeded `info_request` and the reference rendered nothing, a difference of one line of 13 px text and the card's height that no `?props=` value could close. `app.setup.js` declares it too (`app-generated.test.ts` requires the design's props to be declared); the app never passes it, and the empty default renders nothing, so the 28 approved states keep their pixels. |
| A9.1b | 2026-09-08 | A-S5 (2026-09-08): the reference reaches the applicant-answer card's note through a declared prototype prop, `startAnswerNote`, exactly as `startNotice` reaches the sign-in notices; the app never passes it. | One line in `componentDidMount`, immediately after the `startNotice` line it mirrors: a set `startAnswerNote` is written into `state.answer.note`. The NOTE only — the answer text, the error and the application id belong to a real applicant, and the reference never submits — and last among the bootstrap's prop reads, so the app, which passes none, keeps whatever `applicationsMe()` fetched. |
| A10 | 2026-09-08 | update the text on login page for #2 Sellers control disclosure to "Sellers control location & disclosure Properties are mapped using precise coordinates, while the exact location details, financial packets, and floor plans are only revealed when the seller authorizes access." | One script literal (V3:3506): the sign-in card's second gate point, title `"Sellers control disclosure"` → `"Sellers control location & disclosure"`, body `"General location by default. Financial packets and floor plans open only when the seller says yes."` → `"Properties are mapped using precise coordinates, while the exact location details, financial packets, and floor plans are only revealed when the seller authorizes access."`. Ids A8–A9 were reserved by the account-screens branch while this was ruled on `main`; both families are above now. Superseded by A10.2 (2026-09-08). |
| A11 | 2026-09-08 | UNIFY — Change all Browse V3 docked-panel CTAs to "View full listing", including the Insights tab. | One template text node (V3:866): the docked panel's other tabs' primary button, `"Open full listing"` → `"View full listing"`, so every docked-panel CTA reads the same; resolves the unification A3's row flagged. |
| A10.2 | 2026-09-08 | the text for #2 Sellers and control disclouse must be updated to the following text: Sellers control what buyers can see The property is accurately mapped, but financial information, and floor plans are only shared with the seller’s approval. | One script literal (V3:3506, after A10): the sign-in card's second gate point, title `"Sellers control location & disclosure"` → `"Sellers control what buyers can see"`, body → `"The property is accurately mapped, but financial information, and floor plans are only shared with the seller’s approval."` Supersedes A10's wording; A10 remains as the record of the first ruling. |
| A12.1 | 2026-09-08 | Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA (John, 2026-09-08 — Seed Listings launch) | One script literal (V3:2710): `practiceName` reads the listing's own name first — `p.name || NAMES[p.id] || p.area + " Veterinary"`. The server decides what `p.name` is (A-L5/A-L5.1: a listing whose `name_disclosed` is false is served the design's own anonymised label), so this one expression honours the disclosure flag without knowing about it. No fixture practice carries a `name`, so the fixture map and the area fallback behave exactly as before. |
| A12.2 | 2026-09-08 | Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA (John, 2026-09-08 — Seed Listings launch) | One script literal (V3:2659): `photoSet`'s `p2` branch fills an empty slot from `p.photos[i]`, with `src`, `hasSrc` and `noSrc` kept in step. `SRC` — the three photographs the design ships for that one practice, keyed by slot id — keeps precedence, so nothing the design placed moves. |
| A12.3 | 2026-09-08 | Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA (John, 2026-09-08 — Seed Listings launch) | One script literal (V3:2682): `photoSet`'s generic branch, which gave every practice but `p2` six empty slots by construction, now fills them from `p.photos[i]`. The six captions, their order and the placeholder text are the design's, untouched. |
| A12.4 | 2026-09-08 | Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA (John, 2026-09-08 — Seed Listings launch) | One script literal (V3:2693): `heroSrc` returns `p.photos[0]` when the listing has photographs and otherwise the design's own `p2` street-view expression, unchanged. |
| A12.5 | 2026-09-08 | Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA (John, 2026-09-08 — Seed Listings launch) | One script literal (V3:2698): `thumbSrc` likewise, but with a different index. The design's `p2` expression here is a SECOND VIEW — the parking photograph, the thumbnail-safe pick that reads at small sizes where the wide street view `heroSrc` returns does not — so a seeded listing takes its SECOND photograph where it has one and its first otherwise, rather than repeating the hero (L6 ruling, 2026-09-08). |
| A12.6 | 2026-09-08 | Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA (John, 2026-09-08 — Seed Listings launch) | One script literal (V3:3290): the Community Context card's Growth value, `p.growth.replace(" since 2015", "")` → `(p.growth || "").replace(…)`. Root cause: D4 leaves `pop`, `growth`, `income` and `hh` null for every seeded listing until the Census plan supplies them, and `renderVals()` computes `detail()` on EVERY render — so an unguarded null was not a blank card but a blank APP, on every screen including the signed-out gate (measured against the real eighteen). `pop` and `income` need no guard: they are interpolated, and Vue renders `null` as the empty string. Pixel-safe — every design fixture carries a non-empty string, so the `||` never fires for them. |
| A12.7 | 2026-09-08 | Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA (John, 2026-09-08 — Seed Listings launch) | One script literal (V3:3292): the same guard on the Households value, `p.hh.replace(" households", "")` → `(p.hh || "").replace(…)`. These two are the only member accesses on the four community figures anywhere in the design. |
| A12.8 | 2026-09-08 | Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA (John, 2026-09-08 — Seed Listings launch) | One script literal (V3:3269): the detail's subtitle, `p.area + ", TX · Established "` → `p.area + ", " + this.stateOf(p.market) + " · Established "`. Root cause: every design fixture is in the Austin metro (`logic.js:26` normalises the nine that carry no market), so the hard-coded state was right for all twenty-one; the eighteen seeded hospitals span seven states and thirteen would have read "New York, TX", "Denver, TX", "Los Angeles, TX" and so on. `stateOf(market)` is the design's own helper, already called as `this.stateOf(...)` by the Browse card and the docked panel. |
| A12.9 | 2026-09-08 | Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA (John, 2026-09-08 — Seed Listings launch) | One script literal (V3:3311): the Overview section's "General location" row, the same `", TX"` a second time, taking the same helper. The seller wizard's own `{ k: "General location", v: (w.city || "—") + (w.city ? ", TX" : "") }` is a different string and is fixture-driven; the amendment's `count: 1` proves it is not reached. |
| A12.10 | 2026-09-08 | Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA (John, 2026-09-08 — Seed Listings launch) | One script literal (V3:3284): `hasDemo: p.id !== "p8"` → `hasDemo: p.id !== "p8" && p.pop != null`. Root cause: D4 leaves `pop`, `growth`, `income` and `hh` null for every seeded listing until the Census plan lands, and `p.id === "p8"` can only ever be true of a design fixture — so a seeded listing rendered the POPULATED four-tile grid with every value blank, directly under "Source: U.S. Census Bureau…". A12.6/A12.7 stopped the TypeError; this reaches the state the design already ships. |
| A12.11 | 2026-09-08 | Eighteen demo hospitals with real addresses and photos replace the design's fixture practices on QA (John, 2026-09-08 — Seed Listings launch) | One script literal (V3:3285): `noDemo: p.id === "p8"` → `noDemo: p.id === "p8" || p.pop == null`, the other half of the same condition, so the dashed "Community data unavailable for this location" card renders instead. Pixel-safe: `p8` still matches by id and every other design fixture carries a non-null `pop`. |
| A13.1 | 2026-09-08 | must fix this drop-down to be an acutal drop-down vs the popup following the same design logic of a normal dropdown | `setMarket` becomes a class property beside `setF`, taking `setF`'s own "an event OR a bare value" line so the old contract still holds, and closing the menu on a choice. One implementation, called by the dropdown rows. The `setState` body is the old handler's, verbatim. Beside it, `scrollMarketOption` brings a row into view (`scrollIntoView({ block: "nearest" })`) and `moveMarketHighlight` records the keyboard highlight and calls it — the rows are all in the DOM while the menu is open, so no re-render is waited on; the panel's own callback ref calls it again on mount, which is how OPENING scrolls to a highlight seeded before the rows existed. The row is resolved through the field the component recorded (`this._marketMenuEl.querySelector`), not `document.getElementById`: the id is one this component mints, and `setMarket`'s own trigger lookup two lines up is scoped the same way (final review m5). `setMarket` also returns focus to the trigger after a choice, so neither the mouse path nor the keyboard path drops the user on `<body>` (round 4). |
| A13.2 | 2026-09-08 | must fix this drop-down to be an acutal drop-down vs the popup following the same design logic of a normal dropdown | `renderVals()`: `marketMenuOpen`, `toggleMarketMenu`, `marketTriggerLabel`, `marketFieldStyle`, `marketSelectStyle`, `marketCaretStyle`, `marketActiveId`, `marketMenuRef`, `marketPanelRef`, `marketMenuKeys` and the option rows — every one modelled key for key on the Market data card's layer menu (`md.toggleLayerMenu`/`md.layerSelectStyle`/`md.layerMenuCaretStyle`) and the compare menu (`md.compareTriggerLabel`/`md.compareMenuRef`), with the row's highlight taking the row's own hover grey. Each row carries an `optId`, which is what `marketActiveId` names. `Object.keys(MARKETS).indexOf(...)` is clamped with `Math.max(0, …)`: a market MARKETS no longer holds (Seed Listings drops a metro with no listings) gave -1, and `keys[-1]` reached `setMarket` as `undefined`. The orphaned `setMarket:` render key is dropped: the `<select>` was its only reader (dead-code rule, A2.3/A2.5). `marketPanelRef` is the panel's own mount ref — the panel's mount is the first moment its rows exist, so it is where OPENING scrolls the highlighted row into view, the arrow keys having seeded the highlight while the panel was still unrendered; the design's own `md.compareMenuRef` idiom, used because `DCLogic.setState` runs its callback before Vue has rendered (final review m2). `toggleMarketMenu` also clears `giveMenu`: opening one menu closes the others, in every direction (final review m7). |
| A13.3 | 2026-09-08 | must fix this drop-down to be an acutal drop-down vs the popup following the same design logic of a normal dropdown | The markup. The 40 px field keeps its own declarations and its search glyph and gains `position: relative` (as the "More filters" wrapper has); the `<select>` becomes the layer menu's trigger and a `role="listbox"` panel of `role="option"` rows, anchored `top: 46px; z-index: 700` like the "More filters" popover in the same toolbar row. The trigger carries `aria-activedescendant` (the only element that holds focus) and `aria-controls` naming the panel's `id`, and each row carries its own `id`, so the keyboard highlight is announceable and the reference resolves. The colour-ramp chip is left out — markets have no ramp, and absent beats faked. The trigger is `role="combobox"` and each row carries `tabindex="-1"` (final review I1): ARIA 1.2 supports `aria-activedescendant` on `combobox` and not on `button`, so the whole attribute chain hung off a role that need not expose it, and a listbox driven by that attribute keeps focus on the element holding it rather than letting Tab walk into rows whose keys are not bound. This is the APG Select-Only Combobox pattern the markup already implemented in every other respect. |
| A13.4 | 2026-09-08 | must fix this drop-down to be an acutal drop-down vs the popup following the same design logic of a normal dropdown | `trackMenuDismiss()` and its two teardowns, modelled line for line on `trackWidth` and the `componentWillUnmount` it extends. Escape and outside-click are the two dismissals a normal dropdown has; the design has never had either, and they are added for the one menu the ruling names. |
| A13.5 | 2026-09-08 | must fix this drop-down to be an acutal drop-down vs the popup following the same design logic of a normal dropdown | `componentDidMount` arms them, immediately after the `trackWidth()` line it mirrors. |
| A13.6 | 2026-09-08 | must fix this drop-down to be an acutal drop-down vs the popup following the same design logic of a normal dropdown | Dead code, under the bundle's own rule (as A2.2–A2.5 deleted the `browseSel` orphans): the `market:` render key goes. Its only reader was `<select value="{{ market }}">`, which A13.3 replaced — the amended template holds no `{{ market }}`, and no module under `frontend/src` reads `v.market`. |
| A13.7 | 2026-09-08 | must fix this drop-down to be an acutal drop-down vs the popup following the same design logic of a normal dropdown | The same rule on the option rows A13.2 builds: `v: m` was the `<option value>` a `role="option"` button does not have. The rows read `label`, `selected`, `go`, `rowStyle`, `tickStyle` and `optId`, and `go` closes over `m` itself. |
| A14.1 | 2026-09-08 | the Give button must be identical to the https://vinfoundation.org/ where the button is an actual drop down (match button design pixel-by-pixel) | `giveFocus` (V3:2065), a class property beside `money(n)`: Arrow/Home/End movement inside the Give menu, wrapping at both ends. Focus is the highlight — vinfoundation.org has no focus style of its own — so it moves focus and nothing else, and skips rows Vue has unmounted. |
| A14.2 | 2026-09-08 | the Give button must be identical to the https://vinfoundation.org/ where the button is an actual drop down (match button design pixel-by-pixel) | `renderVals()`: `giveMenuOpen`, `toggleGiveMenu`, `giveMenuRef`, `giveButtonRef`, `givePanelRef`, `giveWrapStyle`, `giveButtonStyle`, `giveUnderlineStyle`, `giveLinks` (John's four labels and hrefs, each with its row style, ref, key handler and dismiss-on-choose) and `giveMenuKeys`. Every literal was measured on the live site: #339dde idle pill, #07386f hover/open pill, 10px radius, `2px 22px` padding, Montserrat 600 18px/24.3px, a 3px #339dde underline 4.34px below the pill. The two `'Montserrat'` declarations here are the only two in the design — the scope John's ruling names. `givePanelRef` is the panel's own mount ref — the panel's mount is the first moment its links exist, so it is where an arrow key that OPENED the menu spends its pending index; the design's own `md.compareMenuRef` idiom, used because `DCLogic.setState` runs its callback before Vue has rendered, which is the trap review round 1's C1 found (final review m2). `giveMenuKeys` takes Home and End on the trigger while the menu is open, the way `marketMenuKeys` does (final review m6), and `toggleUserMenu` — this amendment's own anchor line — plus both of Give's open paths clear the other menus, so the invariant holds in every direction (final review m7). |
| A14.3 | 2026-09-08 | the Give button must be identical to the https://vinfoundation.org/ where the button is an actual drop down (match button design pixel-by-pixel) | The markup (V3:112–122). The inert Give `<button>` — the one Give control in the pristine file, in the header of every screen — becomes the header's own wrapper/`sc-if`/panel shape, modelled on the nav and account menus beside it: a `position: relative` div, an `aria-haspopup="menu"` trigger with Font Awesome 5.15.4's `angle-down` inlined at the live 11.25 × 18 px box, an underline element, and a `role="menu"` panel — white, `1px solid #07386f`, square, no shadow, 28px below the pill — of four `role="menuitem"` links at 14px/600 #07386f with the live `8px 20px` padding and 8px transparent left border. No new icon file; no `target="_blank"`, because the live site's links have none. |
| A14.4 | 2026-09-08 | the Give button must be identical to the https://vinfoundation.org/ where the button is an actual drop down (match button design pixel-by-pixel) | The Give branch of A13.4's `pointerdown` closure — an outside click closes the menu, a click inside the wrapper does not. Placed ahead of the metro guard and inert while the Give menu is closed, so A13's dismissal is unchanged. |
| A14.5 | 2026-09-08 | the Give button must be identical to the https://vinfoundation.org/ where the button is an actual drop down (match button design pixel-by-pixel) | The Give branch of A13.4's `keydown` closure — Escape closes the menu and returns focus to the trigger. A13's single guard becomes two with the same net effect for the metro menu, proved by a characterisation case. |
| A14.6 | 2026-09-08 | the Give button must be identical to the https://vinfoundation.org/ where the button is an actual drop down (match button design pixel-by-pixel) | One `@font-face` in the helmet's own `<style>`: Montserrat SemiBold (600), self-hosted from the bundle's new `assets/fonts/Montserrat-SemiBold.woff2` under the SIL Open Font Licence 1.1 (`assets/fonts/OFL.txt`, shipped beside it), `font-display: swap`. The live Give button's face, which the bundle's ProximaNova ladder has no 600 of; the app carries the same rule in `frontend/src/styles/global.css` under the `assets/` → `/assets/` rewrite, against a byte-identical copy. No other element's face changes. |
| A14.7 | 2026-09-08 | the Give button must be identical to the https://vinfoundation.org/ where the button is an actual drop down (match button design pixel-by-pixel) | Tab out of the open menu closes it (review round 1, m2 — ruled): a third closure in A13.4's `trackMenuDismiss`, registered and torn down beside the `pointerdown` and `keydown` ones. A `focusout` whose `relatedTarget` is outside the Give wrapper dismisses; one inside it (the trigger, another row) is a move within the control, and a null `relatedTarget` is the window losing focus, so neither closes. Escape and outside-click were John's ruling; Tab is the third way out of a menu the keyboard can now enter. The metro branches were untouched by this entry — it read `giveMenu` and nothing else; A13.8 later gives the same closure the metro menu's own branch, without changing any of the Give behaviour described here. |
| A14.8 | 2026-09-08 | the Give button must be identical to the https://vinfoundation.org/ where the button is an actual drop down (match button design pixel-by-pixel) | The last leg of the "opening me closes you" invariant (final review m7 — ruled): `toggleNavMenu` clears `giveMenu` as well as `userMenu`, following the design's own line exactly. `toggleUserMenu` (A14.2), `toggleMarketMenu` (A13.2) and Give's two open paths (A14.2) carry the other four directions; this is the one toggle no other amendment touches. Nothing else about the nav menu changes. |
| A13.8 | 2026-09-08 | must fix this drop-down to be an acutal drop-down vs the popup following the same design logic of a normal dropdown | Tab out of the metro listbox closes it (final review m4 — ruled), on exactly the reasoning A14.7 was accepted on two commits earlier: Escape and outside-click were the two dismissals A13.4 gave the control, and Tab is the third way out of a dropdown a keyboard can now enter — the more so since A13.3's `tabindex="-1"` makes Tab from the trigger leave the control outright. A metro branch in the `focusout` closure, in A14.4's own shape. It applies LAST, after A14.7: the closure it edits is A14.7's own, and A13.4 cannot reach forward to a closure that does not exist when it runs. The Give behaviour is bit-identical — a null `relatedTarget` returned before and returns before, on both branches. |
| A15.1 | 2026-09-09 | HAS FAILED and wiped out all the images - if the logic is trying to match and failing then surface all images uploaded and have the user articulate what it is and render ALL images - what was 9 images now are only showing 3 after this hotfix!!! | One script literal (V3:2659): `photoSet`'s `p2` branch tile object. `caption: v[1]` → `caption: (p.photoCaptions && p.photoCaptions[i]) || v[1]`, and the same substitution inside the placeholder. Root cause: the design's six captions are FIXED per practice type and the API sent none, so a photograph could only ever be captioned truthfully by being the one that shows that slot's subject — a ceiling A-L10 turned into a floor: it rendered 73 of the 195 photographs John supplied. A photograph now carries its own description (`photo_captions`, `migrations/090_listing_photo_captions.sql`) and the design's caption is the fallback. Pixel-safe: no design fixture carries `photoCaptions` at all, so the `&&` guard is falsey and every caption is the design's own. |
| A15.2 | 2026-09-09 | HAS FAILED and wiped out all the images - if the logic is trying to match and failing then surface all images uploaded and have the user articulate what it is and render ALL images - what was 9 images now are only showing 3 after this hotfix!!! | One script literal (V3:2679, with V3:2681): the same two substitutions in the generic branch, where the caption and the placeholder are separate lines. The design's four caption lists, their order and the placeholder's shape are untouched — they are what a photograph with no description of its own still reads. |
| A15.3a | 2026-09-09 | HAS FAILED and wiped out all the images - if the logic is trying to match and failing then surface all images uploaded and have the user articulate what it is and render ALL images - what was 9 images now are only showing 3 after this hotfix!!! | One script literal (V3:2677): the generic branch's `return views.map(...)` becomes `const tiles = views.map(...)`, so the six slot tiles can be concatenated with the photographs beyond them. Head of the same edit A15.3b closes. |
| A15.3b | 2026-09-09 | HAS FAILED and wiped out all the images - if the logic is trying to match and failing then surface all images uploaded and have the user articulate what it is and render ALL images - what was 9 images now are only showing 3 after this hotfix!!! | One script literal (V3:2684, anchored on V3:2683-2691): the tail of that expression. After the six slot tiles, one tile per photograph beyond the sixth — id `ph-<listing>-extraN`, index continuing the design's numbering, caption the photograph's own description and otherwise `"Photo N"` (the design has no seventh caption to lend). No template edit: the detail grid and the docked panel's carousel already iterate whatever `photoSet` returns, so eleven tiles wrap into more rows and the carousel counts 1/11. Pixel-safe: no design fixture carries `p.photos` at all — `p2`'s three photographs are the `SRC` map, keyed by slot id — so the guard is falsey, the extra arm is `[]` and no tile is appended in any approved state. |
| A15.3c | 2026-09-09 | HAS FAILED and wiped out all the images - if the logic is trying to match and failing then surface all images uploaded and have the user articulate what it is and render ALL images - what was 9 images now are only showing 3 after this hotfix!!! | One script literal (V3:2650, anchored on V3:2648): the `p2` branch's `return [` becomes `const tiles = [`, the head of the same append. Its slot count is `tiles.length` rather than `views.length` because that branch's six slots are an inline array. |
| A15.3d | 2026-09-09 | HAS FAILED and wiped out all the images - if the logic is trying to match and failing then surface all images uploaded and have the user articulate what it is and render ALL images - what was 9 images now are only showing 3 after this hotfix!!! | One script literal (V3:2661, anchored on V3:2660 and on the `const equine` line that follows the branch): the tail of the `p2` branch's append. `p2` is a design fixture and will never carry a seventh photograph, but a branch that behaves differently from the one beside it is the kind of divergence the next reader pays for. |
| A16.1 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: the dashboard's rows are `(s.myListings !== undefined ? s.myListings : (this.props.listings ? [] : s.sellerListings))`. Three states, not two. `!== undefined`, not `&& length`: a LOADED empty array is a real answer and must empty the table, which is the whole of controller amendment A-SL17 (John, 2026-09-09: "a real seller with zero listings should see the dashboard shell with no invented/sample listings"). Where the array is not there at all, who is asking decides (A-SL23 (2)): with an adapter, zero rows — a real seller is never shown four invented Austin listings because their load failed — and without one, the design's own fixtures, which is the reference and the Claude Design preview. The APP holds `seller-dash`'s frozen pixels through the SUCCESS path instead: the harness answers `GET /api/seller/listings` with those same four rows, derived from this array by `frontend/tests/design-seller-listings.mjs`. |
| A16.2 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: Continue and Edit fetch the draft and open it through A16.17's `openDraft` — the ONE place a `WizardDraft` becomes `editingId`, `w` and `wizAssets`, shared with A16.14's create. Both handlers were `setState({ sellerView: "wizard", step: 1 })` and nothing else, so `w` stayed at its empty initial value and every `w.x || "—"` in `previewRows` rendered an em dash — the "stubs and not functional" the ruling names. The REJECTION arm resets what the success arm sets (A-SL25 (2)): it used to reset nothing, so a refused Edit landed the seller on step 1 still holding the listing they had edited before — its id, its fields and its photographs — and every Continue then patched THAT one. With no adapter the design's own path runs unchanged; a refusal shows the server's message in `wizErr`, the design's own error slot. |
| A16.3 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: View opens `l.id` rather than the hard-coded `"p1"` the four fixture rows shared. `|| "p1"` keeps the fixtures' own destination for a row with no id. |
| A16.4 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: step 6's tiles are the listing's own, and the design's four-item literal is what shows with NO ADAPTER. The ternary keys on `this.props.listings`, exactly as A16.1's does on the dashboard (A-SL25 (1)): keying it on `s.wizAssets` put the dashboard's own defect on the wizard, because the design's step rail jumps to any step with no save and no read — so "Create a listing" then step 6 showed a brand-new listing three photographs that do not exist, and step 8 said "Photos attached 3" on the screen that exists to say what is about to be published. Each tile's name is the seller's own caption (A-SL20), then the DESIGN's own slot caption at that position, then "Photo N" past the six slots the design captions — `main`'s A15.3b's own last resort. Never a filename. The `.slice(0, 3 + (w.photos || 0))` stays on the fallback only. The synthetic practice handed to `photoSet` carries the draft's own `name` and `city` (A-SL23 (7) I5). |
| A16.5 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: "Add files" opens the browser's own file dialog through the adapter, puts what it returns where its type belongs and asks what a photograph shows. UNCAPPED (A-SL20, John 2026-09-09: "surface all images uploaded … and render ALL images") — no `Math.min` on this path, and the API's four-photograph cap went with the same ruling. One chained promise with ONE rejection handler (A-SL23 (4)): choose, upload or store, describe, re-read, and a refusal at any of them lands in `wizErr` rather than escaping unhandled with the new photograph missing from the tiles. A document is routed to the document route by its own type and never asked for a caption (A-SL23 (6) m7). The two guards are separate (A-SL25 (8)): with an adapter the design's counter arm is unreachable, and with no listing behind the wizard the press does nothing at all. With no adapter the design's counter runs unchanged. |
| A16.6 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: Continue PATCHes the step. The design's own three validations are untouched and run FIRST, so a refusal costs no request; only the advance is wrapped, and a refused save leaves the seller on the step with the server's message in `wizErr`. |
| A16.7 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: Submit posts, then runs the design's own `setState` unchanged — so the "Submitted" card appears at once and the dashboard behind it is refreshed with the server's own rows, through A16.17's `reloadListings()` (A-SL25 (3)). Spelled inline the rejection handler was the SIBLING of the fulfilment handler and covered `submit()` alone, so a 429 on the reload behind it escaped unhandled. The design's own optimistic row is prepended to `sellerListings`, which A16.1 does not render once an adapter is present; it stays because it is the DESIGN's line and the reference runs it (A-SL23 (6) m8). |
| A16.8 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: Pause, Republish and Withdraw call `setStatus` and reload through A16.17's `reloadListings()` (A-SL25 (3) — the inline spelling left the reload's own refusal unhandled). The design's own optimistic `setState` is kept as the no-adapter path; with an adapter the reload is the feedback. A REFUSAL is written into that row's own `note` — the design's own field, no new markup — and the next successful load overwrites it (A-SL23 (5)). It used to go to `wizErr`, which only the wizard renders, so a refused Withdraw was invisible and then surfaced later out of context. |
| A16.9 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: `componentDidMount` loads the seller's own listings through A16.17's `reloadListings()`, in A5.4's own shape and at the same seam, gated on the account holding the seller role. A refusal renders ZERO rows (`myListings: []`, A-SL23 (2)): leaving it unset fell back to the design's four Austin fixtures, so a seller whose load failed was shown four listings that were not theirs with live Pause and Withdraw buttons on them. |
| A16.10 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: `statusPill` gains a `declined` entry, label "Declined", in `withdrawn`'s own triple — the `bad` tone `adminVals` already names (spec §16 Q1, John's ruled default). Without it a declined row fell through `map[status] || map.draft` and read "Draft". Pixel-safe: no approved state has a declined listing. |
| A16.11a | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One `data-props` literal: an eighth declared prototype prop, `startMyListings`, spliced after A9.1a's `startAnswerNote` with `me`'s own `json` editor because the value is an array. A-SL17's empty dashboard is a state the design's fixtures cannot express and the reference has no API to reach; this is the mechanism A8.8b and A9.1a established. The app never passes it. |
| A16.11b | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: `componentDidMount` writes the prop into `myListings`, one line after A16.9's load and last, so the app keeps whatever it loaded. An empty ARRAY is truthy — `[]` is what `seller-dash-empty` injects — while the default `null` leaves the design's fixtures in place. |
| A16.12 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: "Photos attached" counts tiles whose badge is `Photo`. The design's own list is three photographs and a PDF, so `uploads.length` called all four photographs; with a real listing behind it the row would tell a seller they had attached seven when five were pictures (A-SL22 (4)). Pixel-safe: `w.photos` is 0 on every approved state, so the design's slice is its three Photo tiles and both spellings count 3. |
| A16.13 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: the preview's "General location" row names the listing's own `w.state` instead of a hard-coded `", TX"` — A12.8/A12.9's ruled edit, in the one place the wizard repeats the claim. The design's fixtures are all in the Austin metro; a seeded seller editing an Oregon hospital read "Portland, TX". Pixel-safe: `wizard-preview` reaches this row with `w.city` empty, which is the em dash on both spellings. |
| A16.14 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: "Create a listing" calls `create()`, READS THE NEW LISTING BACK and opens that draft through A16.17's `openDraft` — `editingId`, `w` and `wizAssets` all set before the first step renders (A-SL23 (1), A-SL25 (1)). Two defects in one line: the create path wrote nothing at all, and it reset only `step`/`wizSubmitted`/`wizErr`, so pressing it after editing a listing opened the wizard still holding that listing's `editingId` and its fields — every Continue patched it, Submit submitted it, and a published one was taken off the market. `.catch` rather than a sibling handler, so a refused create and a refused read land in the same arm: the wizard opens on nothing at all with the message in `wizErr`. `s.creating` (A16.16) makes the second press of a slow button create nothing more (A-SL25 (5)); there is no spinner, because the design has no busy state and Rev 3 owns one. With no adapter the design's own one-liner runs untouched; on the app the four `wizard-*` captures reach the wizard through here, so the harness answers the POST with a fixed id and the GET with the design's own three photograph tiles as a real draft (`design-wizard-draft.mjs`). |
| A16.15 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: "Save and exit" PATCHes the step it is on before it switches to the dashboard, and reloads the rows (A-SL23 (3)). The button has always been labelled *Save and exit* and only ever flipped the view — harmless while the wizard was a prototype, a lost step now that Continue genuinely writes. The chain reads `patch → reloadListings → exit` with ONE rejection arm at the end, which can only ever be the patch's because `reloadListings()` settles its own (A-SL25 (3)). A refusal keeps the seller in the wizard with the server's message in `wizErr`, because leaving would discard the very fields it refused. Revised by A-SL27 (2) (round-3 re-review, MAJOR-D): it saves in the adapter's PARTIAL mode — `patch(editingId, step, w, true)` — which leaves a blank required number (`est`, `price`) out of the body instead of sending `""`; this button has none of the step guards Continue runs behind, a draft is incomplete by nature, and `400 est must be a number.` had been holding the seller in the wizard behind the button labelled *Save*. The rejection arm now fires only on a genuine server refusal. Pixel-safe: none of the four `wizard-*` captures presses it, and with no adapter the design's own one-liner runs untouched. |
| A16.16 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: the design's own state literal gains `wizAssets: []` and `creating: false` beside `step`, `wizErr` and `wizSubmitted` (A-SL25 (1) and (5)). A16.4 reads `wizAssets` on every adapter render — `renderVals()` computes `wizardVals()` whatever screen is showing — so declaring it an array from the first tick is what makes "an ARRAY on every adapter path" true at the one place that can guarantee it; `creating` is A16.14's double-create guard. Pixel-safe: with no adapter A16.4 never looks at `wizAssets` and nothing reads `creating`. |
| A16.17 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | Two script literals in one edit, as class methods beside the seller helpers (A-SL25 (1) and (3)). `openDraft(id, d, err)` is the ONE place a `WizardDraft` becomes `editingId`, `w` and `wizAssets`: Create and Edit both go through it on both of their arms, `w` starts from the DESIGN's own initial literal rather than from `st.w` so no field of the previous listing survives into the next, and a refusal of either opens the wizard on nothing at all. `reloadListings()` is the one loader every read of the seller's collection goes through — the bootstrap, the three dashboard transitions, submit and save-and-exit — and it carries its own rejection arm (`myListings: []`), which is what the callers kept forgetting: spelled inline, each rejection handler was the SIBLING of its fulfilment handler and a 429 on the reload behind a successful write escaped unhandled. Neither is called with no adapter. |
| A16.18 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: every step-rail row's `go` SAVES the step it leaves before it moves (A-SL27 (3), on the round-3 re-review's MAJOR-E). The design's `go` was a pure state move — harmless in a prototype that persisted nothing; once this family made persistence real and per step, the rail became the one navigation control that silently discarded typed work, under chrome reading "Saved automatically". Shaped exactly like `next()`'s single rejection arm: with an adapter and a listing, `patch(editingId, step, w, true)` first — partial mode, since the rail has no guard and the step may be half-filled — then the move, with `wizAssets` re-set from the answer; a refusal keeps the seller on the step they were typing on with the message in `wizErr`; steps 6 and 8 have no fields, so the adapter re-reads and issues no PATCH. With no adapter the design's own move runs untouched. Pixel-safe with one harness route: three captures press the rail (`wizard-step-7`, `wizard-preview`, `wizard-done`) and `prepare()` answers `…/{id}?step=N` with the same design draft as the bare read, so the render is identical and `baseline-manifest.json` is unchanged. |
| A16.19 | 2026-09-08 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal: the wizard's Back button SAVES the step it leaves before it moves (A-SL29 (1), on the round-4 re-review's MAJOR-F) — A16.18's edit on the other navigation control, which round 3's "the ONE navigation control that silently discards work" missed by one: type on step 5 → Back → Save and exit wrote step 4 and lost step 5, under "Saved automatically". Mechanically identical to A16.18: with an adapter and a listing, `patch(editingId, step, w, true)` first — partial mode, Back has no guard — then the design's own `Math.max(1, step - 1)` move with `wizAssets` re-set from the answer; a refusal keeps the seller on the step they were typing on with the message in `wizErr`; steps 6 and 8 have no fields, so the adapter re-reads and issues no PATCH; on step 1 it saves and stays. Pixel-free: no approved capture presses Back, with no adapter the design's one-liner runs untouched, and on the app the press is answered by the `…/{id}?step=N` route A16.18 gave `prepare()`. |
| A16.20a | 2026-09-09 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal (V3:2078): the header nav (`go`) SAVES the step the wizard is on before it navigates away (A-SL30 (3), on the round-5 re-review's Info-15) — the rail's and Back's own shape on a third door out of the wizard, neither a re-run of MAJOR-E/F since `go` only ever changed `screen`: returning to Seller showed the typed values from memory, but nothing had been persisted, and a tab closed between the two clicks lost it for good. With an adapter, the wizard open and a listing being edited, `patch(editingId, step, w, true)` first — partial mode, Continue's own rejection arm — then the navigation, with `wizAssets` re-set from the answer; a refusal keeps the seller on the wizard with the message in `wizErr` and the navigation never happens; steps 6 and 8 have no fields, so the adapter re-reads and issues no PATCH. Outside the wizard, or with no adapter, `go` is the design's own one-liner, unchanged. Pixel-free: no approved capture presses a header nav item from inside the wizard. |
| A16.20b | 2026-09-09 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal (V3:3490): Sign out ATTEMPTS the same save, and ends the session REGARDLESS of the answer (A-SL30 (3), on Info-15) — deliberately not A16.20a's shape, by John's own ruling: a session end is the seller's explicit act and must never be held hostage to a save nobody asked for, so "the attempt is the most the chrome's 'Saved automatically' can honestly offer". With an adapter, the wizard open and a listing being edited, the save is chained with its OWN swallowed rejection (`.catch(() => {})`, the shape the sign-out call beside it has always used) ahead of the existing `auth.signOut()` step, never inside its single rejection arm — whatever the save answers, sign-out proceeds. Outside the wizard, or with no adapter, the chain starts from `Promise.resolve()` exactly as the design's own one-liner did. Pixel-free: no approved capture signs out from inside the wizard. |
| A16.21 | 2026-09-09 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One script literal (V3:3180): the step-6 tile re-describes an EXISTING photograph, seeded ones included, by clicking it (A-SL25 (10)). Each `uploads` entry gains `describe`, PHOTOGRAPHS ONLY (`a.kind !== "Photo"` is `null`) and only where there is a listing to save it to (`!s.editingId` is `null` too — the design's own fixture path, with no adapter, never reaches this branch). ONE chained promise, ONE rejection arm into `wizErr` (A-SL23 (4)'s `attach` shape), routed by the tile's own discriminator: `caption(editingId, a.id, text)` for an ASSET, the positional route `describe(editingId, a.position, text)` for a SEED entry — the SAME adapter method the ask itself is, overloaded, because the tile's click chains ask-then-write exactly as "Add files" already does (A16.5): `describe(id, position, describe())`. The tiles refresh from the returned draft. Pixel-safe: A16.22 is the only markup change and no approved capture clicks a tile. |
| A16.22 | 2026-09-09 | none of the existing Photes and Documents are being render4ed in the "EDIT" of an existing listing by hospital across all the data seeded on and also none of the actual inputs are appearing in the PREVIEW and SUBMIT, it appears to be stubs and not functional, this gap must be corrected and the UX true | One template literal (V3:1339): the step-6 tile's `<div style="width: 92px;">` gains `onClick="{{ u.describe }}"` — the design's own way of wiring an already-bound handler, `wiz.addPhoto`'s own convention — plus `cursor: pointer` and a static `title`, cosmetic only. `u.describe` is `null` for a document tile or when there is nothing to save to (A16.21's own guard), and Vue attaches no listener for a falsy `onClick`, so the template stays a one-line, unconditional addition to the one occurrence in the pristine file. |
| A17.1 | 2026-09-09 | every Admin tab must show real database data, never dummy rows | One script literal: the Listings tab's rows are `(s.adminListingRows !== undefined ? s.adminListingRows : (this.props.adminListings ? [] : <the design's own five fixture rows>))` — A16.1's own shape, applied to the review queue (Task SL8, controller amendment A-SL24 (1)). A LOADED empty queue is a real answer and must empty the table; where the array is not there at all, who is asking decides: with an `adminListings` adapter, zero rows — a reviewer is never shown five listings that are not real because their load failed — and without one, the design's own fixtures (the reference and the Claude Design preview). The APP holds `admin-listings`'s frozen pixels through the SUCCESS path instead: `frontend/tests/harness.ts` answers `GET /api/admin/listings` with ALL FIVE of those rows, "Flagged" included — A-SL24 (4)'s "the fifth fixture row is not reproduced" is the LIVE mapping's own limit (no `listing.status` value backs it, so `PILLS`/`ACTIONS` can never route a real row to it), not a limit on this oracle-only fixture, which carries a row's own pill and action styles verbatim — derived from this array by `frontend/tests/design-admin-listings.mjs`. |
| A17.2 | 2026-09-09 | every Admin tab must show real database data, never dummy rows | One script literal: `componentDidMount` loads the review queue through `this.props.adminListings.list()`, in A16.9's own shape and one line after A16.11b's, gated on the account holding the staff or admin role. A refusal renders ZERO rows (`adminListingRows: []`, A-SL24 (1)): leaving it unset would fall back to the design's five fixture rows, showing a reviewer five listings that are not real with live Publish/Reject/Unpublish buttons on them. |
| A18.1 | 2026-09-09 | the arrow icons are backwards on each location, reverse each | One template literal (V3:852): the docked panel's Insights-tab CTA arrow. `navigate-arrow.svg` points left unrotated and sat AFTER "View full listing", pointing back at its own label; it gains `transform: rotate(180deg)` — the design's own flip, the docked panel's Next arrow — and points right, toward the listing it opens. The bytes of the `<img>` are otherwise unchanged; `transform` precedes `filter` as on that arrow. |
| A18.2 | 2026-09-09 | the arrow icons are backwards on each location, reverse each | One template literal (V3:930): the detail's "Back to results" link. The same glyph carried `transform: rotate(180deg)` BEFORE its label, pointing right — away from the results it returns to; the rotation is removed and it points left. The SVG files are untouched (the docked panel's prev/next pair and the two sign-out arrows share the glyph and are as designed). |
| A19.1 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | One script literal (V3:1937), the line after the interest modal's own `interest: "closed"` entry in `state`: two state keys beside the interest modal's. `lightbox` is null while closed and `{ pid, at }` — listing id, slot id of the photograph showing — while open; `lightboxFocus` is the one-shot "move focus into the dialog on mount" flag the mount ref spends (the A14 `giveMenuAt` idiom). Closed in every approved state, so no state renders an overlay. |
| A19.2 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | One script literal (V3:2715–2750 — the block ends on the `marketPanel` line it was anchored to): `openLightbox` (records the opener from `e.currentTarget` — new to the design — and clears the three header menus, not the metro one, which its own closures shut first), `closeLightbox` (clears and returns focus to the opener), `lightboxPhotos()` (the docked panel's own `photoSet(p).filter(hasSrc)`, so A15's extra tiles page and "N of M" equals the carousel's counter), `stepLightbox` (wrap-around, the panel's modulo) and `lightboxVals()` (`open`, `src`, `caption`, `counter`, `label`, `multiple`, `prev`, `next`, `close`, `backdrop` — closes only when `e.target === e.currentTarget` — and `ref`, which stores the dialog element and spends `lightboxFocus` once). Every later-running closure reads `this.state`. Step 10 finding (live Chromium, both targets): a synchronous `focus()` on a node the same patch just mounted is silently dropped — layout/style is not yet committed — so `ref`'s `el.focus()` is deferred one macrotask through the design's own `setTimeout(…, 0)` idiom; the focus still lands, and `lightboxFocus` is still spent synchronously so a second render before the timer fires cannot re-arm it. |
| A19.3 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | One script literal (V3:3873): `renderVals()` gains `lightbox: this.lightboxVals()` beside the interest modal's keys, so the root-level block reads `{{ lightbox.open }}` and the rest. |
| A19.4 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | One script literal (V3:3275): `detail()`'s filled tiles gain `open(e)` and `openLabel` ("Expand photo: " + the photograph's own caption, A15); empty tiles are the design's own objects, untouched. Anchored on the `photos:` line alone, never the `photoHeroId` orphan beneath it. |
| A19.5 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | One script literal (V3:2799): the docked panel's photos object gains `open(e)` and `openLabel` for the photograph its carousel is showing (`cur`), so the lightbox opens on that one and counts as the panel does. |
| A19.6 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | One template literal (V3:957): in each detail tile's 168 px frame, a third `sc-if` on `ph.hasSrc` holding a contentless, transparent, borderless absolute `<button aria-label="{{ ph.openLabel }}">` laid over the `<image-slot>` — the design's icon-button reset plus `inset: 0` — which paints no pixel while closed and never exists on an empty slot (whose click on the reference is the design tool's file chooser). |
| A19.7 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | One template literal (V3:741): the same hit-target over the docked panel's current photograph, on a `hasAny` sc-if (the panel's own habit — it gates the pills the same way), placed before the arrows so the prev/next buttons, the pills and the dots keep painting above it and stay clickable. |
| A19.8 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | One template literal (V3:1710–1723), the overlay, once, at the root: the interest modal's scrim at `z-index: 1100` — above Leaflet's attribution and controls at 1000 on Browse; a `role="dialog" aria-modal="true" aria-label="Photograph N of M" tabindex="-1"` box with the tile frame's declarations, the modal box's shadow and entrance and `outline: none`; the photograph at natural size bounded to the viewport minus 24 px (V3:1713); the panel's 38 px close button with `close-x-gray.svg` whitened, "Close photo" (V3:1714); the panel's prev/next arrows verbatim, hidden for a single photograph (V3:1719, V3:1722); the panel's counter and caption pills verbatim (V3:1728, V3:1729). Four compositions are asserted as exceptions in the test. |
| A19.9 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | One script literal (V3:1978): the lightbox branch of the shared `keydown` closure, ahead of A14.5's Give guard — Escape closes and returns focus to the opener, ArrowLeft and ArrowRight step with wrap-around, each preventing the default. Skipped while closed, so A13's and A14's Escape are byte-identical. Its find is A14.5's output. Fix round 2 (A-LB3, 2026-09-09 — the focusout trap A19.10 tried does not hold in real Chromium, ruled on fix round 1's NEEDS_CONTEXT): this closure now also owns Tab. `box.querySelectorAll("button")` reads the dialog's own controls in DOM order (Close photo, then Previous/Next when `multiple`) — the container is `tabindex="-1"` and is never one of them; on the last, Tab wraps to the first; on the first, or on the container itself (where the mount-ref idiom leaves focus right after opening), Shift+Tab wraps to the last. One code path, no timer, no `relatedTarget`: the browser's own Tab motion is prevented only at the two wrap points. |
| A19.10 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | Originally one script literal at the top of the shared `focusout` closure: a trap that deferred `box.focus()` one macrotask, via `setTimeout`, whenever a non-null `relatedTarget` left the dialog for somewhere `aria-modal` said was inert — the design's own idiom, applied above the `const to` line so the pinned Give and metro text stayed contiguous. Step 10 (fix round 1, 2026-09-09) built the e2e gate this ruling asked for and found the trap does not hold in real Chromium, reproducibly: forward Tab off the last control reaches real page content for a keypress before self-correcting, and Shift+Tab from the first control never reaches the last at all. Root cause is structural: a null `relatedTarget` means both "the window blurred" (must be ignored, A14.7's own case) and "focus left the dialog's own tabbable set" (must not), and the arm cannot tell its two cases apart. Fix round 2 (A-LB3, 2026-09-09) removes the trap entirely — Tab is instead handled deterministically in the shared `keydown` closure, A19.9's row above. This row's find is still the closure head A14.7 introduced and A13.8 rewrote (V3:2001–2006, from the closure's `const out` line; 0 in the pristine file, so it still applies after A13.8), and its replace is now that same head plus a comment recording why nothing else stands here — so a reader who finds this closure otherwise untouched by A19 knows a trap was tried and retracted, not never attempted. |
| A19.11 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | One script literal, adapted at the SL9 merge (2026-09-09) once A16.20a (this branch's own, applied earlier in the list) had already split `go()`'s one-liner into a save-before-navigate chain: the original single `this.setState({ screen, interest: "closed", userMenu: false });` A19.11 targeted no longer occurs verbatim, so the anchor widens to A16.20a's own two calls that still set `screen`/`interest`/`userMenu` together — the no-adapter/no-save early return (V3:2079) and the save's success arm — both of which now also clear `lightbox`/`lightboxFocus`, a screen change closing the lightbox exactly as the original ruling asked. The refusal arm sets none of the three and is correctly left alone: no screen change happens on a failed save. |
| A19.12 | 2026-09-09 | the images/photos should be clickable and they expand and have < > to view all images larger with simple X to close | One script literal (V3:3496): `signOut`'s reset clears `lightbox` and `lightboxFocus` too, so a session that ends cannot leave the scrim over the gate card. The app's route sync (`frontend/src/router/useStateRouteSync.ts`) is the third leg — Browser Back — because the reference has no router. |
| A21.1 | 2026-09-10 | a missing figure is omitted, never zeroed (controller amendment A-C28) | Two script literals: the `econ` and `vets` entries in the `communities()` return. B7 feeds `vets` and `econ_k` counts per listing; a community we have no figure for must not render as zero. The two `|| 0` defaults go — `econ: ECON_K[p.id] != null ? ECON_K[p.id] * 1000 : undefined,` and `vets: VETS[p.id]` — so a missing key renders `undefined` and the layer omits it (the renderer already filters on `undefined`). The reference passes no API and every design fixture carries all twenty-one keys, so every approved state keeps its pixels. |
| A21.1b | 2026-09-10 | a missing figure is omitted, never zeroed (controller amendment A-C28) | One script literal in the `marketVals` assembly: after reading the raw value in the `["income", "pets", ...].forEach` loop, skip it entirely with `if (raw == null) return;` before calling `bucket()` or `fmtMetric()`. A21.1 makes `undefined` flow here; A21.1b keeps it out of the `vals[k]` object so the renderer never sees it. The map's overlay painter already checks `if (v == null) return;` before painting, and no consumer requires the key to exist. The reference and the Claude Design preview hold the fixture data; every approved state keeps its pixels. |
| A21.2b | 2026-09-10 | render no verdict, NaN or undefined for listings without figures (Task B10, D-C31) | Five script literals in `marketPanel`: `per10k` becomes `undefined` when households or vets is undefined, the income index checks for undefined before computing, and the competition level and fill bars are only rendered when per10k is a number. These prevent a panel rendering `String(undefined)`, `NaN` ratios and "Lean" verdicts for listings with no figures. Exact changes: `const per10k = (c.hh && c.vets) ? (c.vets / (c.hh / 10000)) : undefined;`, `const incomeIdx = c.income ? Math.round(...) : undefined;`, `const compLevel = (per10k !== undefined && per10k < 1.4) ? ... : undefined;` and `const compFill = (per10k !== undefined && per10k < 1.4) ? 1 : ... : 0;` (clamped to 0 so the bar does not render when there is no data). The reference and the Claude Design preview hold the fixture data with all metrics populated; every approved state keeps its pixels. |
| A21.2c | 2026-09-10 | compEstab renders vets count or nothing, never "undefined" (Task B10, D-C31) | One script literal: `compEstab: (c.vets !== undefined) ? String(c.vets) : undefined,`. A listing with no vets figure must not render the string "undefined". The reference and the Claude Design preview hold the fixture data; every approved state keeps its pixels. |
| A21.2d | 2026-09-10 | overviewTiles renders only populated tiles (Task B10, D-C31) | One script literal array: the four Community Context tiles check each metric for undefined before rendering its value. The two sub-lines were built by concatenation, so an absent growth or income index left the unit behind and the tile read a bare `"% (5 yrs)"` or `"% vs US"`; each sub-line is now `undefined` as a whole when its figure is. The fourth tile (Pet Households) is left verbatim here and guarded by A21.2i. The reference and the Claude Design preview hold the fixture data; every approved state keeps its pixels. |
| A21.2e | 2026-09-10 | oppTiles renders verdicts only when metrics are defined (Task B10, D-C31) | One script literal array: the three Opportunity tiles check their metrics before rendering verdicts. Affluence checks `incomeIdx`, Population Growth checks `c.growth`, and Sector Payroll checks `c.econ`; all three show an empty label and `on: false` when their metric is undefined. The reference and the Claude Design preview hold the fixture data; every approved state keeps its pixels. |
| A21.2f | 2026-09-10 | compPer10k renders the ratio or undefined, never throws (Task B10, D-C31) | One script literal (V3:2839): the docked panel's competition-per-10k field. `compPer10k: per10k.toFixed(1),` → `compPer10k: (per10k !== undefined) ? per10k.toFixed(1) : undefined,`. When `per10k` is undefined, the method call would throw; now it returns undefined so the template's guard (`!== null`) renders nothing. |
| A21.2g | 2026-09-10 | a composite of unknowns is not a low score; it is not a score (Task B10, D-C31) | One script literal (V3:2777): the docked panel's market opportunity score. `const score = Math.max(0, Math.min(100, Math.round(…)))` → `const score = (c.income === undefined || c.growth === undefined || per10k === undefined) ? undefined : Math.max(…)`. A composite of unknowns is not a score; when any metric is missing the score is undefined and the label renders nothing. |
| A21.2h | 2026-09-10 | no score, no label — the ring says nothing rather than "Challenging" (Task B10, D-C31) | One script literal (V3:2855): the docked panel's score label. `scoreLabel: score >= 75 ? "Attractive" : …` → `scoreLabel: score === undefined ? undefined : score >= 75 ? "Attractive" : …`. The template guards with `!== null` so undefined renders no verdict. |
| A21.3a | 2026-09-10 | the growth layer stops naming a year it does not use (controller amendment A-C29) | One script literal: the growth layer's `label` in the `VALUE_LAYERS` map. `label: "Population Growth Since 2015 (ACS)",` → `label: "Population Growth (ACS)",`. The figure derives from the active vintage — 2018 today, not 2015 — and hard-coded years belong nowhere user-facing. |
| A21.3b | 2026-09-10 | the growth layer stops naming a year it does not use (same ruling) | One script literal: the layer's description in `LAYER_META`. `sub: "Change since 2015 · ACS population estimates",` → `sub: "Change · ACS population estimates",`. |
| A21.3c | 2026-09-10 | the growth layer stops naming a year it does not use (same ruling) | One script literal: the card's growth row. Both `blurb: "Change since 2015"` and `caption: "Growth since 2015"` → `blurb: "Change"` and `caption: "Growth"`. |
| A21.3d | 2026-09-10 | the detail Growth row extracts its vintage from the API string, never hard-coded (controller amendment A-C29) | One script literal in the detail's community facts: the Growth row's value and sub-line. `v: (p.growth || "").replace(" since 2015", ""), sub: "Since 2015"` → `v: (() => { const g = (p.growth || "").split(" since "); return g[0]; })(), sub: (() => { const g = (p.growth || "").split(" since "); return g.length > 1 ? "Since " + g[1] : ""; })()`. The API's string carries the vintage ("+11.6% since 2018"); hard-coding "since 2015" no longer matches. |
| A21.2i | 2026-09-10 | an absent pet-household estimate renders nothing, not "undefined" (Task B10, D-C31) | One script literal (V3:2836): the docked panel's overview tiles, pet households field. `{ v: this.fmtMetric("households", c.pets), k: "Est. Pet Households", sub: "derived estimate" }` → `{ v: (c.pets !== undefined) ? this.fmtMetric("households", c.pets) : undefined, k: "Est. Pet Households", sub: "derived estimate" }`. When `c.pets` is undefined, the tile value is undefined and renders nothing instead of the string "undefined". |
| A21.2j | 2026-09-10 | no competition figure, no verdict — never "undefined Competition" (Task B10, D-C31) | One script literal (V3:2840): the docked panel's competition level. `compLevel: compLevel + " Competition",` → `compLevel: (compLevel !== undefined) ? compLevel + " Competition" : undefined,`. When `compLevel` is undefined, the field is undefined and renders nothing instead of "undefined Competition". |
| A21.2k | 2026-09-10 | no score, no number in the ring (Task B10, D-C31) | One script literal (V3:2854): the docked panel's market opportunity score. `score: String(score),` → `score: (score !== undefined) ? String(score) : undefined,`. When `score` is undefined, the field is undefined and renders nothing instead of the string "undefined". |
| A21.2l | 2026-09-10 | no score, no ring — a conic gradient of undefined is a broken circle (Task B10, D-C31) | One script literal (V3:2856): the docked panel's score ring background. `scoreRing: "width: 46px; height: 46px; border-radius: 999px; display: grid; place-items: center; background: conic-gradient(#4c9a6a " + score + "%, #e6ecf1 0); font-family: var(--rf-display);",` → `scoreRing: (score === undefined) ? undefined : "width: 46px; height: 46px; border-radius: 999px; display: grid; place-items: center; background: conic-gradient(#4c9a6a " + score + "%, #e6ecf1 0); font-family: var(--rf-display);",`. When `score` is undefined, the ring is undefined and renders nothing instead of a broken conic gradient. |
| A21.1c | 2026-09-10 | a missing figure is omitted, never zeroed (D-C31, Task B10) | Five script expressions in the `communities()` return, the ROOT CAUSE A21.1 left standing. `num()` returns 0 for null and `parseFloat(...) || 0` did the same for growth, so `pop`, `hh`, `income`, `growth` and the `pets` estimate derived from `hh` were all 0 for a listing we have no figures for — and every `!== undefined` guard added for this task was satisfied by that 0. The panel read "0" Population, "0.0% (5 yrs)", "$0K" Median Income and a "Flat" growth verdict. Each expression now yields `undefined` when its source is null and keeps exactly the parse it had when the source is present: `const hh = p.hh != null ? num(p.hh) : undefined;`, `pop: p.pop != null ? num(p.pop) : undefined`, `income: p.income != null ? num(p.income) : undefined`, `growth: p.growth != null ? (parseFloat(...) || 0) : undefined`, `pets: hh !== undefined ? Math.round(hh * 0.57) : undefined`. `num()` itself is untouched — it is read elsewhere. Every design fixture carries all four strings, so every approved state keeps its pixels. |
| A21.2m | 2026-09-10 | three bars at the floor are a reading, not an absence (D-C31, Task B10) | One script literal: the docked panel's competition bars. `compBars: [1, 2, 3].map(...)` → `compBars: (per10k === undefined) ? [] : [1, 2, 3].map(...)`. With no ratio, `compFill` was 0 and the row painted three grey bars, which reads as "the lowest competition there is". An empty array renders no bars at all. |
| A21.2n | 2026-09-10 | a metro median is the median of what we know, never of zeros we invented (D-C31, Task B10) | One script literal: the Market data strip cards' `vals`/`mid`. `num(raw)` coerced every absent figure to 0, so the six cards printed `$0K` / `0` / `+0.0%` under the words "metro median". The list is now filtered on `raw != null` before it is mapped, and `mid` is `undefined` rather than `|| 0` when nothing in the metro carries that figure. `bars` needs no change: `vals` is empty in exactly that case. |
| A21.2o | 2026-09-10 | a strip card with no figure keeps its title, source and link and shows no value (D-C31, Task B10) | One script literal: `value: this.fmtMetric(k, mid),` → `value: (mid !== undefined) ? this.fmtMetric(k, mid) : undefined,`. The template already guards the value with `!== null`, so the card keeps its title, its source line and its "View on map" link and shows nothing where the figure would be. |
| A21.2p | 2026-09-10 | a compare row with no figure carries no bar (D-C31, Task B10) | One script literal: the Compare rows' two bar styles. The four `bucket(...)` calls over `num(raw(k))` turned an absent figure into a minimum-width bar — the same false reading A21.2m removes from the competition row. One `bar(k)` helper replaces them, returning the design's own style string byte for byte when the figure is there and `undefined` when it is not; the template's `<span :style>` then paints nothing. |
| A21.4a | 2026-09-10 | a listing with no figures reaches the design’s own "Community data unavailable" card on the panel too (D-C31, A-C31 (2)) | One script literal in `marketPanel`'s return: `hasDemo: sel.pop != null`, `noDemo: sel.pop == null` — the detail's own idiom, keyed exactly as A12.10/A12.11 keyed the detail's — and `overviewTitle: sel.communityLabel || "Market Overview (10 min drive)"` for D-C32's half. |
| A21.4b | 2026-09-10 | a listing with no figures reaches the design’s own "Community data unavailable" card on the panel too (same ruling) | One template literal: the Insights tab body opens a `hasDemo` branch around the Market Overview, Competitive Landscape and Market Opportunity sections. |
| A21.4c | 2026-09-10 | a listing with no figures reaches the design’s own "Community data unavailable" card on the panel too (same ruling) | One template literal: the branch closes before the "View full listing" button, and the `noDemo` branch between them (V3:847) is the DETAIL's own unavailable card (V3:1038), copied element for element — no second card is invented. The button stays outside both branches: navigation is not data. |
| A21.4d | 2026-09-10 | a listing with no figures reaches the design’s own "Community data unavailable" card on the panel too (same ruling) | One template literal: the "Drive-time figures are approximated…" footnote moves inside a second `hasDemo` branch. It describes figures, so it goes with them. |
| A21.5a | 2026-09-10 | a buyer is never shown a drive-time area disguised as a named city (D-C32) | One template literal: the docked panel's Insights heading. `Market Overview (10 min drive)` → `{{ md.panel.overviewTitle }}`, which is that same string whenever the API sent no label and the label itself — "Within 10 minutes of the practice" — when it did. |
| A21.5b | 2026-09-10 | a buyer is never shown a drive-time area disguised as a named city (same ruling) | One script literal in the detail's community facts: a new `demoScope` sentence and the Population tile's sub-line. `sub: "Community, 2023"` → `sub: p.communityLabel || "Community, 2023"`, and `demoScope` reads `"Figures describe " + (p.communityLabel ? "the area " + p.communityLabel.charAt(0).toLowerCase() + p.communityLabel.slice(1) : "the community around the practice") + ", not the practice itself."` — the design's own sentence, byte for byte, when there is no label. |
| A21.5c | 2026-09-10 | a buyer is never shown a drive-time area disguised as a named city (same ruling) | One script literal: the Households tile's sub-line, the other one that says "the community". `sub: "In the community"` → `sub: p.communityLabel || "In the community"`. Applied after A12.7, whose replace preserves this `find` unchanged. |
| A21.5d | 2026-09-10 | a buyer is never shown a drive-time area disguised as a named city (same ruling) | One template literal: the Community Context attribution paragraph's trailing sentence becomes `{{ d.demoScope }}`. The Census attribution itself is legally load-bearing and is untouched — only the sentence that describes the AREA moves into the data. |
| A22 | 2026-09-10 | Preserve existing seed wording/detail — widen the ownership dropdown to carry the seeds' six phrasings beside the design's four (Task SL10) | One template literal (V3:3163): the seller wizard's step 1, the ownership select. The design's four options (`"Sole proprietor"`, `"Two-doctor partnership"`, `"Multi-doctor LLC"`, `"Other"`) expand to ten, inserting the seeds' own six phrasings in their own order: `"Sole proprietor (LLC)"`, `"Sole proprietor (S-corp)"`, `"Three-doctor LLC"`, `"Four-doctor partnership"`, `"Four-doctor LLC"` and `"Five-doctor LLC"` between the single-doctor variants and `"Multi-doctor LLC"`. The API's `OWNERSHIPS` tuple and the design's option array are identical and pinned two-way by pytest; `tests/api/test_seller_listings.py::test_the_design_ownership_options_equal_ownerships_tuple` — Task SL10 step 1 — enforces the byte-for-byte equivalence. |
| A23 | 2026-09-10 | collapsing or expanding the Market data card closes both its menus (Task MD1) | One script literal (V3:2474): `toggleLegend: () => this.setState({ mdLegendOff: s.mdLegendOff !== true, mdLayerMenu: false, mdCompareMenu: false })`. Exactly ONE of the card's two menus escapes the collapse, and it is the one John reported: the layer menu's panel (`frontend/src/App.vue:475-476`) is `position: absolute; left: 16px; top: 118px; z-index: 620` and sits OUTSIDE both of the card's `v-if="v.md?.legendOpen"` templates (`:382-410` and `:413-472`; the card div closes at `:411`), so nothing unmounts it and it floats over the map with no card above it. The comparison listbox never floated: its panel (`:434-435`) is nested inside the second `legendOpen` template in normal flow (`margin-top: 6px`) and has always unmounted with the card. `mdCompareMenu` is cleared all the same, for a weaker and different reason — a menu left open in state reappears already-open when the card is expanded again, which is its own surprise — not because it escapes the card's region. The clear is UNCONDITIONAL: the one `setState` runs on expand exactly as on collapse, so neither menu can come back open in either direction (the four-quadrant characterisation in `frontend/src/logic.test.ts` pins both). The fix mirrors the design's own idiom where `insightOpen` gates the "What this means" panel on `s.mdLegendOff !== true`, which is why that panel behaves correctly. |
| A25.1 | 2026-09-10 | a listing with no coordinates keeps its place in the results and does not get a pin (Task MP1) | Ids A20 and A24 are RESERVED and were skipped, not orphaned: A20 by the image-identifiability plan (`docs/superpowers/plans/2026-09-09-image-identifiability-protection.md`, Tasks P11-P12) and A24 by the neighbourhood-shading spec (`docs/superpowers/specs/2026-09-10-neighbourhood-shading-design.md` §9, A24.1-A24.8), both in flight at the time of writing — so the next free id after this family is A26, not A24. One script literal (V3:2306): the pin list. `practices: list.map(…)` → `practices: list.filter((p) => Number.isFinite(p.lat) && Number.isFinite(p.lng)).map(…)`. Root cause: `app/api/listings.py`'s `serialise` nulls `lat`/`lng` whenever `location_disclosed` is false, and `migrations/016_listing.sql` declares that column `NOT NULL DEFAULT false` — the nulls are the DEFAULT, not an edge case — and `MarketMapView.vue`'s `engine.marker([p.lat, p.lng], …)` handed them to Leaflet 1.9.4, whose `toLatLng([null, null])` returns `null` (the array branch is gated on `typeof a[0] !== "object"`, and `typeof null === "object"`), so `_setPos` read `.lat` off a null. ONE listing was enough: the `forEach` has no try/catch, so pin drawing stopped there for every later listing, and `LayerGroup.addLayer` had already stored the marker, so the poisoned layer re-threw from inside Leaflet's own event loop on every zoom pass. `Number.isFinite`, not `!= null`: a `NaN` reaches `toLatLng` intact and breaks one step further on. The rail, the count, the sort and the filters are untouched — the listing is real and only the map skips it. Pixel-safe: every design fixture and all eighteen seeds carry a point. |
| A25.2 | 2026-09-10 | a listing with no coordinates keeps its place in the results and does not get a pin (Task MP1) | One script literal (V3:2289): the drive-time ring's centre. `driveCenter: sel ? [sel.lat, sel.lng] : cfg.center` → the same expression with `Number.isFinite` on both of the selection's coordinates. `MarketMapView.vue` guards on `props.driveCenter` being TRUTHY and `[null, null]` is an array, so it passed the guard and reached `engine.ring`, which is `L.circle` and calls the same `toLatLng`. The fallback is the expression's OWN else-branch, the metro centre; no new centre is invented. |
| A25.3 | 2026-09-10 | a listing with no coordinates keeps its place in the results and does not get a pin (Task MP1) | One script literal (V3:2292): the map's community list. `communities: comms.map(…)` → the same, filtered on a finite point. `mosaicBbox` takes `Math.min`/`Math.max` over these lats and lngs and `null` coerces to 0, so — measured on the real producer — one unlocated Austin community stretched the metro box from [29.86, -98.24] to [-0.13, 0.15]: 100,482,513 mosaic cells to iterate, a hung tab rather than a missing shape. A community with no centroid cannot be shaded. ONLY the map's list: `comms` itself is untouched, so the Market data strip cards still take their metro medians over that listing's figures and the docked panel still reads its own community — a figure is not a point (A21.2n unchanged). |
| A25.4 | 2026-09-10 | a missing figure is omitted, never zeroed — the panel’s last-resort community too (D-C31, Task MP1) | One script literal (V3:2771): `marketPanel`'s last-resort community object, `{ pop: 0, hh: 0, income: 0, growth: 0, pets: 0, vets: 0 }` → the same keys, all `undefined`. The one place left in the panel where a zero stood in for an absence, and the one A21.1c and the A21.2 family could not reach: it is taken when the selection has no community of its own AND the market has no communities at all. Measured on the real producer it rendered "0" Population, "0.0% (5 yrs)", "$0K" Median Income, "0" Veterinary Establishments and a "Flat" growth verdict. The arm is KEPT rather than deleted — it is what stops the panel throwing on an empty market — and every A21 guard downstream already reads `undefined` as "we have no figure". |
| A25.5 | 2026-09-10 | the docked panel and the detail must not disagree about whether a listing has community data (Task MP1) | One script literal (V3:2825–2826): the docked panel's `hasDemo`/`noDemo` gain the detail's own first term — `sel.id !== "p8" && sel.pop != null` / `sel.id === "p8" \|\| sel.pop == null`. A21.4a ported the detail's idiom to the panel and dropped that term, so the design's own fixture for "Community data unavailable" showed a FULL profile in the docked panel and the unavailable card on the detail behind it. The p8 term is kept on both rather than dropped from both because p8 carries `pop`, `growth`, `income` and `hh` in the fixture: the id test is the design's deliberate way of demonstrating the unavailable state, and dropping it would delete that demonstration. `p.id === "p8"` can only ever be true of a design fixture — a real listing's id is a uuid — so nothing the API serves is affected. Applied after A21.4a, whose output is this `find` (0 in the pristine file). Pixel-safe: no approved state opens the panel on p8 — `browse-market-panel` and `interest-modal` use Cedar Park and Round Rock. |
| A25.6 | 2026-09-10 | no point, no ring — the drive-time ring is not painted around the metro for a listing whose location is withheld (Task MP1, fix round 1) | One script literal (V3:2488): the C7 drive-time ring's gate. `showDrive: !!sel` → `showDrive: !!(sel && Number.isFinite(sel.lat) && Number.isFinite(sel.lng))`. The FOURTH leg into the same trap and the only one that asserts something false rather than omitting something true. `MarketMapView.vue:91` draws the ring on `showDrive && driveCenter`; before A25.2 an unlocated selection reached `engine.ring([null, null], 16000, …)` — `L.circle`, the same `toLatLng` — and threw, so no ring was ever painted. A25.2 restored the expression's own else-branch and thereby made that branch PAINTABLE for the first time: a 16 km dashed "roughly ten minutes' drive" circle centred on the middle of Austin, around a place the practice is not. A missing pin omits; this fabricates, and it contradicted ruling item 1 of the same brief (nothing drawn at the metro centre). `showDrive` takes the same finite-coordinate test the pin list uses; nothing else changes — no substitute copy, no note, no empty state — and a located listing's ring is untouched, which a characterisation case pins in both directions. |
| A26.1 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | Four class members in the design's script (V3:2131–2155), beside A13.1's metro machinery. `openFilterMenu` is the family's ONE open path — the toggle and the arrow keys both call it — and therefore the only place the five instances name the four overlay menus John's m7 ruling governs; writing `fMenu` is what closes whichever sibling was open (`Object.assign` semantics), so the invariant INSIDE the family is structural and there is nothing to forget. `setFilter` (V3:2137) is the choice: it calls the design's own `setF`, so the state transition and the 320 ms loading settle are byte-for-byte the `<select>`'s `onChange`, then shuts the panel and returns focus to the trigger the way `setMarket` does — the chosen row unmounts under the pointer, so focus would otherwise land on `<body>`. `scrollFilterOption` and `moveFilterHighlight` are A13.1's two, taking the instance's key as their first argument. |
| A26.2 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3772–3842): the `filters:` `.map()` body. The five ARRAY LITERALS above it are untouched — every key, option value and approved label is exactly as it was; only the render values change, key for key on A13.2's metro menu (`open`↔`marketMenuOpen`, `toggle`↔`toggleMarketMenu`, `caretStyle`↔`marketCaretStyle`, `triggerLabel`↔`marketTriggerLabel`, `hostRef`↔`marketMenuRef`, `panelRef`↔`marketPanelRef`, `keys`↔`marketMenuKeys`, `rowStyle`/`tickStyle` verbatim). `style` is the `<select>`'s own string with three declarations prefixed — `display: inline-flex; align-items: center; gap: 8px;`, which the "More filters" button in the same row already carries — because a `<button>` lays out a label and a chevron where the user agent used to draw its own arrow; nothing else about the closed box changes. `aria` (V3:3791) is derived from the design's OWN first option, all five of which read "&lt;name&gt;: Any", rather than authoring five new strings: these five have no `<label>` and no `aria-label` today, a `<label>` cannot name a `<button>`, and `screens.ts`'s `layerTrigger` addresses the Market data card's two listbox triggers as the UNLABELLED ones. `fMenuAt` is a rendered highlight guarded BY THE KEY (`open && s.fMenuAt === i`), so a stale index can never paint on a sibling. The `<select>`'s three orphaned render keys go with it under the bundle's own dead-code rule, exactly as A13.6/A13.7 dropped `market:` and `v: m`: `value:` fed `value="{{ fl.value }}"`, `v:` fed `<option value="{{ o.v }}">` and `set:` fed `onChange="{{ fl.set }}"`, and after A26.10 the template holds none of the three. `setF` itself, and its event-or-value line, are untouched and still what `setFilter` calls. |
| A26.3 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3694–3765): the `moreFilters:` `.map()` body — the three dropdowns inside the "More filters" popover, Task F2. The three ARRAY LITERALS above it are untouched: every key, option value and approved label is exactly as it was. John called "More filters" the CORRECT implementation, so his words do not reach the three under it; the user's experience does. A `<select>`'s popup is an OPERATING SYSTEM window and renders above this popover's own `z-index: 700`, so converting only the toolbar five would have put the dark menu he photographed on top of his own exemplar, one click deeper, rather than removed it. Key for key on A26.2, because it IS A26.2's loop with three instances instead of five: one state slot, one open path, one set of closures, no new machinery — and the eight filter keys are disjoint (`est`/`ownership`/`sqft` against `type`/`price`/`revenue`/`doctors`/`building`), so `fMenu` still names exactly one dropdown across BOTH loops. Two things differ, and both are the design's own: `label:` stays, because it is the caption the popover renders AND the trigger's accessible name (a `<label>` does not name a `<button>`, which takes its name from its own contents first), so no `aria:` key is invented as A26.2 had to; and `cur` keeps the `|| "Any"` guard the `<select>`'s `value:` carried, because the design's state literal seeds the five toolbar keys and none of these three. The `<select>`'s three orphaned render keys go with it under the bundle's own dead-code rule, exactly as A13.6/A13.7 and A26.2 dropped theirs: `value:` fed `value="{{ mf.value }}"`, `set:` fed `onChange="{{ mf.set }}"` and the rows' `v:` fed `<option value>`. The comment sits INSIDE the map body, never between the last array row and `].map(`: `tests/seeds/test_hospitals_json.py`'s `_MORE_BLOCK` requires `      ]` to follow the last row directly and fails loudly rather than silently testing nothing, which A26.2 learned on its sibling `_BAR_BLOCK`. |
| A26.4 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3683): `toggleMore` clears `fMenu`/`fMenuAt` unconditionally. The "More filters" popover is the three inner dropdowns' PARENT, not their peer — opening one of the three inside it must not close it — so it is not one of the cross-close edges; one unconditional clear covers both needed directions instead, closing the popover shutting any dropdown inside it (a child cannot latch behind an unmounted parent) and opening the popover shutting a toolbar dropdown. |
| A26.5 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One branch (V3:1968–1971) in `trackMenuDismiss`'s existing shared `pointerdown` closure: an outside click closes the open dropdown. No new listener — A13.4 armed this one and A19 already shares it. Placed after Give's branch and BEFORE the metro's, because the metro branch's guard is an early `return`: a branch appended after it would be dead whenever the metro menu is closed, which is almost always. Both existing branches keep their own bytes, the invariant A14.4 established for A13's and `design-amendments.test.ts` pins for each of the five. The host is resolved through the keyed collection this component records (A14's `_giveItemEls` idiom), never across the document — A13's deliberate rule (final review m5). |
| A26.6 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One branch (V3:1997) in the same function's shared `keydown` closure: Escape closes the open dropdown, in the same position and for the same reason. No focus return is written and none is needed — the option rows carry `tabindex="-1"` (A13.3) so focus never leaves the trigger, which is exactly why A13's own Escape branch does not return focus where A14's Give branch has to. |
| A26.7 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One branch (V3:2014–2017) in the same function's shared `focusout` closure: Tab out closes the open dropdown, in the same position. The window-blur rule above it — a null `relatedTarget` dismisses nothing (A19, A-LB3) — is the closure's own early return and covers this branch unchanged. |
| A26.8a | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3421): `toggleNavMenu` gains `fMenu`/`fMenuAt`. The first of the six inbound cross-close edges — opening any other menu shuts an open filter dropdown. The family's own OUTBOUND edge is written once, in A26.1's `openFilterMenu`. |
| A26.8b | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3428): `toggleUserMenu` gains `fMenu`/`fMenuAt`. |
| A26.8c | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3438): `toggleGiveMenu` gains `fMenu`/`fMenuAt`. |
| A26.8d | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3489): Give's arrow-open gains `fMenu`/`fMenuAt` — the same menu's second open path, which m7 already required to carry the other three keys. |
| A26.8e | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3615): `toggleMarketMenu` gains `fMenu`/`fMenuAt`. |
| A26.8f | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3639): the metro listbox's arrow-open gains `fMenu`/`fMenuAt` — that menu's second open path, as A26.8d is Give's. |
| A26.9a | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:2078): `go()`'s signed-out arm clears `fMenu`/`fMenuAt`, so navigating away cannot leave a panel latched over the next screen. This matters because the design's existing menu keys DO latch — `go("detail")` after opening the metro listbox leaves `marketMenu: true` — and A26 declines to ship a ninth instance of a known defect while also declining to fix the existing ones here (reported separately as D-F2, which this ruling does not authorise). |
| A26.9b | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:2079): `go()`'s no-draft arm, the same clear. |
| A26.9c | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:2081): `go()`'s save-then-navigate arm, the same clear. |
| A26.10 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One template literal (V3:400–412): the five toolbar filters' `<select>` becomes A13.3's shape per instance — the layer menu's trigger and its panel, wrapped in the `position: relative` div the "More filters" control in this same row already uses so the panel can anchor beneath the field. A `<select>`'s popup is drawn by the OPERATING SYSTEM, not by the page: on macOS Chromium it is the dark menu in John's screenshots, it ignores every declaration in `fl.style`, and it renders above every in-page `z-index`; no CSS reaches it and only replacing the element does. Each row carries an `id` and the TRIGGER carries `aria-activedescendant`, because the focused element is the only place a screen reader reads it and focus stays on the trigger throughout; the trigger is `role="combobox"` (ARIA 1.2 supports `aria-activedescendant` on `combobox`, not on `button` — A13's final review I1) and the rows carry `tabindex="-1"`, the other half of the same pattern. The panel carries NO width declaration: A13's metro panel is `width: 300px` because its field is `min-width: 300px`, these five are five different widths, `min-width: 100%` appears nowhere in the pristine bundle, and an absolutely positioned box with no width shrink-wraps its widest row — which is always at least the current label, since the label IS one of the rows. Absent beats invented. |
| A26.11 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One template literal (V3:428–448): the popover's three `<select>`s become A26.10's shape per instance, dropped into the design's own `<label>` without disturbing it — the caption `<span>` is byte-unchanged and the field becomes the layer menu's trigger and its panel, wrapped in the `position: relative` div the popover's own parent already uses so the panel can anchor beneath it. The trigger carries `width: 100%`, which the popover's own "Done" button carries and is therefore the design's own declaration; it is load-bearing rather than decorative, because the `<select>` was a flex item of the column `<label>` and stretched, a button inside the new wrapper is not one and would shrink-wrap, and A26.16's `min-width: 100%` resolves against that wrapper — so a trigger narrower than its wrapper would leave the panel wider than the trigger, the opposite of what John ruled. `aria-label` is `mf.label`, the caption already shown: a `<label>` does not name a `<button>`, so without it the trigger would be named by its current option — and it is also what keeps `screens.ts`'s `layerTrigger` (`button[aria-haspopup="listbox"]:not([aria-label])`) pointing at the Market data card's two. Clicking the caption still reaches the control as it did with the `<select>`: a `<label>`'s activation behaviour forwards to its first labelable descendant and does nothing for a click that lands on an interactive descendant, so an option row's own click is not forwarded. |
| A26.12 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | **Q2 widening, own id so it can be lifted out.** John's 2026-09-08 m7 ruling — "opening any one of the four menus closes the other three" — was never written in three of the twelve ordered directions among `navMenu`, `userMenu`, `giveMenu` and `marketMenu`. Give's own six were complete, which is why the gap survived that review, and why it is a real defect rather than a theoretical one: Give and the metro listbox ALSO carry global pointerdown and focusout dismissal (A13.4/A13.8), so their pointer paths were covered by accident, while `navMenu` and `userMenu` have no outside-click, Escape or Tab dismissal of any kind. Two mouse clicks reach it. Ruled yes by the controller on 2026-09-11 on the lines A26.8 was already editing; the wider menu-hygiene gap is D-F2 and is NOT built here. No pixel moves — each writes `false` over a flag already `false` in every approved state, since no state opens two menus at once. One script literal (V3:3421): `toggleNavMenu` gains `marketMenu`/`marketMenuAt`, so the collapsed-header nav menu closes the metro listbox. It co-renders with the toolbar at `header-1000`. |
| A26.13 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3428): `toggleUserMenu` gains `navMenu` and `marketMenu`/`marketMenuAt` — the two m7 directions the account menu never carried. See A26.12's row for the ruling and the reasoning. |
| A26.14 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3615): `toggleMarketMenu` gains `navMenu` and `userMenu`, which is the direction section 8 of the A26 ruling states verbatim — open the account menu, click the metro trigger, both stay open. See A26.12's row for the ruling and the reasoning. |
| A26.15 | 2026-09-11 | the dropdown "more filters" is correct implementation while everything else on the filter bar is implemented incorrectly and not using the site design, this must be corrected | One script literal (V3:3639): the metro listbox's ARROW-key open path gains `navMenu`, `userMenu` and `giveMenu`. The fourth broken m7 direction, found by the Q2 characterisation case's exhaustive enumeration of ordered pairs rather than by reading — A26.12-A26.14 fixed the three the ruling's section 8 named, all of them pointer paths, and `nav then metro (arrow)` then failed. Same defect, same ruling: A14's own m7 fix had to cover BOTH of Give's open paths for exactly this reason, and m7's own comment names this user ("a pure-keyboard user could hold this listbox and the header's Give menu open at once"). The account menu has no focusout dismissal (D-F2), so Shift+Tab to the metro trigger and one ArrowDown reaches the state with a keyboard alone. Own id, like A26.12-A26.14. No pixel moves: it writes `false` over three flags already `false` in every approved state. |
| A26.16 | 2026-09-11 | the panel takes the width of the trigger that opened it, so their edges line up | One declaration, on BOTH of this family's listbox panels at once (V3:406, V3:437) — Task F1b, run together with Task F2 so the popover's three are born with the width rather than acquiring it in a third pass. Task F1's review measured the newly approved `browse-filter-menu` capture: the Practice type trigger is 155 px and its panel 153 px, so the panel's right edge sat 2 px INSIDE the button that opened it. Structural rather than per-control — the row's chrome is 4 px narrower than the trigger's and the widest row claws about 2 px back — so four of the five sat ~2 px narrow and Property inverted, opening far wider than its 129 px collapsed trigger; the family had no consistent panel-to-trigger relationship at all. "The design is silent here" was never the honest description: A26.10's panel string is A13's metro panel string with `width: 300px` DELETED and nothing else changed, and FOUR of the four absolutely positioned menu panels the PRISTINE bundle itself carries have a fixed pixel width (account 208 px, the other header menu 236 px, More filters 262 px, the layer menu 300 px), and the two others usually cited alongside them — A13's listbox at 300 px and A14's Give panel at `width: max-content; min-width: 130px` — are AMENDMENTS, not pristine. The controller's ruling said "six of six ... in the pristine bundle" and that was wrong; it is corrected here rather than repeated. The point it was making survives intact: the design is not silent about panel widths, it fixes them, and pristine contains no `max-content` anywhere — the compare menu has none only because it is in normal flow and fills its trigger by construction, and A13 pins its panel and its field to the same 300 px deliberately, which is the design's own stated intent for this idiom. `min-width: 100%` invents no number for any of the eight controls: each trigger already sits in a `position: relative` wrapper with its panel absolutely positioned inside it, so it resolves against the wrapper — the trigger. It is a NEW declaration and therefore the SECOND named exception to this family's "every declaration must already appear in the pristine bundle" gate, after the More-filters anchoring pair; `design-amendments.test.ts` retires the two `not.toContain('min-width: 100%')` trip-wires Task F1 left for exactly this moment and asserts instead that the declaration is on these two panels and nowhere else. Rejected: A14's `width: max-content; min-width: <px>` (eight invented numbers) and one shared fixed width (chosen for the longest option across all eight, leaving seven panels wider than they need to be). A13's metro panel keeps its own measured 300 px and is untouched. |
| A27.1 | 2026-09-11 | each tile names where its own number comes from, and a ring median says it is approximate (D-C38) | One script literal: the detail's Median income tile. `sub: "Household, 2023"` -> `sub: p.incomeNote || "Household, 2023"`. Root cause: A21.5b/A21.5c wired the Population and Households sub-lines to `communityLabel` and deliberately took only those two — the ones that said "the community" — so the median was the one area figure on the card whose caption could not follow its own geography. It is also the figure that most needs one: a catchment median is a household-weighted average of the tract medians inside the ring rather than a published Census figure (`app/census/materialize.py`), it can never be suppressed, and `docs/integrations/market-data-api.md` has always asked for the word "approximate" beside it. The API composes the whole sub-line (`income_note`) because the tile has ONE sub-line and it must carry the area and the qualifier together. With no note the design's literal stands byte for byte, which is what keeps `detail` on its frozen hash. |
| A27.2 | 2026-09-11 | growth keeps its city-or-county figure and its own sub-line says so (D-C38) | One script literal: the detail's Growth tile sub-line. CHAINED on A21.3d (the `find` is A21.3d's whole `replace`, as A21.5c chains on A12.7's), which already took the vintage from the API's own string. `sub` now reads `p.growthScope + " · since " + year` where the API named a geography, and A21.3d's own `"Since " + year` — or `""` — where it did not. Root cause, and the reason John rejected the cheaper option that relabels every tile uniformly: `population_growth_pct` CANNOT vary by band. `app/census/materialize.py` computes it once per listing outside the band loop and writes that one value into all three bands (plan D12), so a uniform "within about 5 miles" caption would have put the whole City of Dallas under a ring heading — the defect moved one tile over rather than fixed. Its resolution below place-or-county waits on the 2010->2020 tract crosswalk, a registered Phase C deferral: this tile gains an honest label and no neighbourhood detail. |
| A27.3 | 2026-09-11 | the ring is described by distance, not by time, and the sentences that said otherwise are corrected in the same release (D-C39) | One script literal: the docked panel's Insights heading default. CHAINED on A21.4a, which introduced the line. `"Market Overview (10 min drive)"` -> `"Market Overview"`; `communityLabel` still overrides the whole heading when the figures came from the catchment, which is A21.5a's mechanism unchanged. Root cause: the band is an 8 km straight-line buffer from the practice point (spec §8), not a routed drive time — spec §15 still lists true isochrones as OPEN for V1 — and the heading asserted a ten-minute ring over PLACE-band figures on 28 of 29 listings. This is the one A27 entry with no null branch to preserve: the default IS the false sentence. It re-bases the Browse states that render the panel, which John took knowingly; none of `baseline-manifest.json`'s thirteen frozen hashes is a Browse capture. |
| A27.4 | 2026-09-11 | the ring is described by distance, not by time, and the sentences that said otherwise are corrected in the same release (same ruling) | One template literal: the docked panel's footnote, the second sentence D-C39 names. CHAINED on A21.4d, which wrapped this paragraph in the `hasDemo` branch. `"Drive-time figures are approximated from a straight-line catchment around the practice."` -> `"A catchment figure is a straight-line area of about 5 miles around the practice, not a driving route."` Root cause: two claims in one sentence, of which the first is false — there is no drive time anywhere in the pipeline — while the straight-line catchment IS the measurement, so the correction keeps it and says how far it reaches. The paragraph's other two sentences are untouched, byte for byte. The THIRD such sentence, the Browse Market data card's "Figures describe the community around each practice…", is corrected by A27.5 in the row below — its own id and its own ruling, not a silent widening of this one. |
| A27.5 | 2026-09-11 | the third sentence that described the figures as the community is corrected too (D-C39, extended on the implementer's report) | One template literal (the Browse "Market data" card's footnote). `Figures describe the community around each practice, not the practice itself. ` → `Figures describe the area around each practice, not the practice itself. `. D-C39 named TWO sentences that describe the ring by time; the implementer found a THIRD and correctly declined to widen its own scope rather than silently fixing it. This one was already loose over place-band figures and becomes plainly wrong once D-C38 serves most of them from the catchment, so leaving it would have shipped a release that corrects two false sentences and makes a third worse. "Area" rather than a named geography because this footnote covers every listing on the screen at once and they no longer all answer from the same band. |
