# Local amendments to the V3 Rev 2 design (spec D15)

`Practice Match V3.dc.html` = `Practice Match V3.rev2.dc.html` (pristine, never edited) + the entries below, applied by `frontend/tests/design-amendments.ts` and proved byte-for-byte by `frontend/tests/design-amendments.test.ts`. They retire when a re-issued bundle carries them.

| Id | Date | John's ruling | What changes |
|---|---|---|---|
| A1 | 2026-09-07 | "keep the V2 header and do not restyle header or fonts" | 24 template elements: every display-size heading paired with V2 (by tag, text and size) takes V2's `text-transform`/`letter-spacing` values in place; the key-fact values `{{ m.v }}` and the 28 px mobile asking price return to V2's uppercase `.005em`; `{{ resultHeadline }}` is untouched (V3's only occurrence, in the mobile list, already equals V2's). No script, no `_ds/**`, no site header (byte-identical V2↔V3 already). |
| A2 | 2026-09-07 | "resolve this" — the mobile practice card opens the detail (spec D17) | One line of the design's script: the mobile results card's `open` handler. Root cause: it set `browseSel`, which C13 left nothing to read once it removed the peek card that used to display it, so the tap was a no-op; `open` now navigates straight to the detail screen, the same way V2's card and C13's own second-pin-tap already do. |
| A2.2 | 2026-09-07 | zero-gaps review: the comment above the handler A2 just changed still described the old (dead) behaviour | The one-line comment above `results[].open`. Root cause: C13 left the "select into the docked side panel" comment in place after the peek card it described was removed; it now says what the handler does since A2 (open the detail). |
| A2.3 | 2026-09-07 | zero-gaps review: a dead mapping is dead code (spec D8/D12), applied to the design's script via the amendment mechanism | Deletes `hasBrowseSel`, `closeBrowseSel` and `bsel` outright. Root cause: all three read or wrote `browseSel`, and C13 removed the peek card that was their only reader in the template — nothing else in the design or the port referenced any of the three. |
| A2.4 | 2026-09-07 | zero-gaps review: drop the dead `browseSel` key, keep the live `activeId` key (spec D8/D12) | The top-level `selectMarker` (not `mobileVals.selectMarker`, which C13 already fixed) drops the `browseSel:` key from its `setState` call and keeps `activeId:`. Root cause: same orphaned key as A2/A2.3 — this handler is not wired to any template prop, but it still wrote `browseSel` on every call. |
| A2.5 | 2026-09-07 | zero-gaps review: a dead handler is dead code, same rule as A2.3 | Deletes the top-level `selectMarker` (the one A2.4 trimmed) outright. Root cause: it was never wired to any template prop — the only `on-select` bindings in the design are `mob.selectMarker` (mobileVals, C13's own fix) and `md.selectFromMap`; nothing else in the design or the port referenced the top-level one. |
| A3 | 2026-09-07 | "update across the application 'view full market report' to 'View full listing'" (spec D18) | One template text node (V3:705): the docked panel's Insights-tab primary button, `"View full market report"` → `"View full listing"`. The other tabs' `"Open full listing"` (V3:717) is untouched — not part of this ruling; flagged to John for possible unification. |
| A4 | 2026-09-07 | "if user clicks + Compare that action 'closes' the 'What this means card' and when X Compare is clicked it closes the compare and 'What this means card' appears again" (spec D21) | One condition added to the design's script: the `insightOpen` IIFE (the floating interpretation card's gate) now also requires `!s.mdCompareOpen`. Opening Compare hides the floating card; closing Compare (the same toggle, whose note reads "close" while open) shows it again unless the member already dismissed it. No markup or style change. |
| A5.1 | 2026-09-07 | spec §"Prototype wiring" step 2: the prototype signs in through the API | The `signIn` handler in `renderVals()`: with a `this.props.auth` adapter it calls `signIn(email, password)` and sets `auth`/`screen`/`email` plus the header strings from the answer, or renders the SERVER's own refusal message and stays on the gate; with no adapter — the reference, and the Claude Design preview — the design's fixture path runs unchanged. The empty-form validation above it is untouched. |
| A5.3a | 2026-09-07 | spec §"Prototype wiring" step 2: sign-out ends the real session | The `signOut` handler is wrapped in the adapter's promise (`.catch(() => {})`, so the reset happens whatever the network did — a member left looking signed in with a revoked session is the worse failure). |
| A5.3b | 2026-09-07 | closes the parenthesis A5.3a opened (same ruling) | The closing `})` of that handler's `setState` object becomes `}))`. |
| A5.4 | 2026-09-07 | amendment A-I8 / A-I8.1: the app reads the signed-in account on load, and the reference needs a prop-driven way into a gate state | `componentDidMount` gains five lines: the `startGate` prototype prop (`{ screen: 'gate', gate: <prop> }`) and the account lifecycle mapping for `this.props.me` — `active` → signed in on Browse with the header strings from `/api/me`, `pending`/`needs_review` → the "under review" gate, `declined` → the "not granted" gate, `verified` → the application gate. `unverified` is deliberately unmapped (nothing to show it until I8c's "check your email" exists — that card landed in Task S4 and A8.3b maps it). The reference and the Claude Design preview pass no `me` and take the design's fixture path unchanged. |
| A5.6 | 2026-09-07 | amendment A-I8, decision D-I8-3: the reference reaches a gate state through a prop once the access-state shortcuts leave | One entry in the escaped `data-props` JSON, spliced immediately after `startViewport`: `startGate`, an `enum` of `signin`/`apply`/`pending`/`rejected` defaulting to `""`, section "Prototype", label "Start on gate state". The bundle's runtime hands it to the Root as a default (support.js `parseDataProps`), which is what lets `tests/reference-server.mjs` drive a gate state through `?props=`. |
| A5.7 | 2026-09-07 | amendment A-I8.2: the reference must render the same account the app does, and the design's copy does not change | One entry in the escaped `data-props` JSON, spliced immediately after `startGate`: `me`, an object prop defaulting to `null`, section "Prototype", label "Signed-in account". A5.4 makes the header render `/api/me`'s computed `role`/`initials`; the app has a session and the reference has none, so the reference is handed the same account through the design's own prop mechanism (`tests/reference-server.mjs` injects it from `?props=`) rather than by editing the fixture — which John's rule for this wave forbids. `support.js` copies a `default` verbatim, so an object and a `null` both arrive unchanged. |
| A6.1 | 2026-09-07 | CLAUDE.md's launch-removal list, executed with real auth | The prototype jump bar's whole `<sc-if value="{{ showPrototypeBar }}">` block leaves the template, plus the blank line it left behind. The "Mobile view" / "Desktop view" toggle lived inside it and goes with it; the phone-frame presentation stays reachable through `startViewport` (D-I8-7). |
| A6.2 | 2026-09-07 | CLAUDE.md's launch-removal list | The "Prototype — access states" block on the sign-in card — the dashed-border element, its label and its `<sc-for>` over `gateStates`. The two status gates are reached by BEING in that state now (A5.4 maps the account), which is what `pending@` and `declined@` are seeded for. |
| A6.3a | 2026-09-07 | CLAUDE.md's launch-removal list: the pre-filled demo credentials | `state.email`/`state.pw` become empty strings. The design pre-filled a real-looking address and a masked password so the prototype's Sign in worked on the first click; with A5.1 that click reaches the API. |
| A6.3b | 2026-09-07 | the same, where `signOut` restores them | The masked password in the `signOut` reset becomes an empty string. |
| A6.3c | 2026-09-07 | the pre-filled application, including an affirmation nobody made | `state.apply` becomes empty with `affirm: false`. Field names kept — the UI reads them. |
| A6.4a | 2026-09-07 | CLAUDE.md's launch-removal list: the jump bar's script | The `const jumps = [...]` array the removed bar rendered from, plus the blank line it left behind. |
| A6.4b | 2026-09-07 | the flag the removed `sc-if` read | `showPrototypeBar: this.props.prototypeBar !== false,` leaves `renderVals()`. |
| A6.4c | 2026-09-07 | the `jumps` key in `renderVals()`'s return | `nav, jumps,` → `nav,`. |
| A6.4d | 2026-09-07 | the handler the removed buttons called | The `jumpTo` class property is deleted, with one adjacent newline. |
| A6.5 | 2026-09-07 | CLAUDE.md's launch-removal list: the access-state shortcuts' own array | `gateStates: [...]` (the three shortcut buttons) leaves `renderVals()`. |
| A6.6a | 2026-09-07 | a dead mapping is dead code (spec D8/D12, A-I8.1) | `viewportLabel` leaves `renderVals()`: the "Mobile view" / "Desktop view" button that read it was in the jump bar A6.1 removed. |
| A6.6b | 2026-09-07 | the same for the handler it labelled (A-I8.1) | `toggleViewport` leaves `renderVals()`. `isDesktop`, which reads the same `s.viewport`, is untouched — `startViewport` still drives the phone frame (D-I8-7). |
| A7.1 | 2026-09-07 | spec §Sign-in: "The design's 'VIN username' copy changes to 'Email' (design delta)" | The sign-in card's field label: `VIN username or email` → `Email`. The API authenticates an email address and knows nothing about VIN usernames. |
| A7.2 | 2026-09-07 | the same copy in the empty-form message (spec §Sign-in) | `"Enter both your VIN username and password."` → `"Enter both your email and password."`. |
| A7.3 | 2026-09-08 | spec §4.1 (John, 2026-09-08: "approved"): the sign-in card offers "Forgot your password?" beside "Request access" | One template text node: the sign-in card's footer line becomes `Not approved yet? Request access · Forgot your password?`, the second link opening the new `forgot` gate. `gate-signin` is the only screen this moves, and it is not one of the thirteen V2-oracle screens. |
| A7.4 | 2026-09-08 | spec §4.2 (John: "remove the VIN language") | The sign-in card's sub-line: `Use your VIN credentials.` → `Use the email and password you registered with.` The API authenticates an address and a password of its own; A7.1 already did the same for the field label. |
| A8.1a | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ("approved") | The `form` value gains the notice slot: `error: !!(s.formError || s.formNotice)`, `errorText: s.formError \|\| s.formNotice`. Four outcomes whose only next step is signing in speak through the sign-in card's EXISTING message box rather than a status card that would have shown two identical buttons; the card's markup is untouched and a refusal still wins over a notice. |
| A8.1b | 2026-09-08 | spec §4.3 (agreed): the status cards' "Sign in" ends the session first | `goSignin` signs a signed-in visitor out through the adapter before it shows the card (the label and therefore the pixels are unchanged), and `goForgot`/`goSignup` join it as the two new ways into the account cards. |
| A8.1c | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ("approved") | `goApply` sends an anonymous visitor ON THE APP to `signup` (they have no account yet) and a signed-in `verified` account to the application form. The reference has no adapter and keeps the design's own path to `apply`, which is what leaves `gate-apply` on its pixels. |
| A8.2 | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ("approved") | `state` gains `formNotice`, `gateToken` and the five empty form sub-objects (`signup`, `forgot`, `reset`, `invite`, `answer`). All empty, like A6.3's: nothing is pre-filled, and `gateToken` is where the router parks an incoming `?token=` so the app never writes one into a URL of its own (spec S3). |
| A8.3a | 2026-09-08 | S2 review rider (2026-09-08): a token-bearing gate value wins over the active-account redirect | The `active` branch of `componentDidMount` is guarded with `&& !["verify", "reset", "invite"].includes(this.state.gate)`. A5.4 sent every `active` account straight to Browse, and an approved member who follows a reset or invitation link is an `active` account — they would never have seen the page the link was for. |
| A8.3b | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ("approved") | The rest of the bootstrap: `unverified` → the check-email card (A5.4 left it deliberately unmapped because this card did not exist yet); `needs_review` keeps A5.4's synchronous "under review" card and switches to the answer card when `GET /api/applications/me` arrives; `declined` pre-fills the application form from the applicant's own last answers (spec §3, "Re-apply needs no new screen"); `startNotice` puts the reference on the sign-in card with a message; a `/verify` landing posts its token on arrival and shows the notice or the expired card. Every API call is guarded by `this.props.auth`, so the reference takes the design's fixture path unchanged. |
| A8.4a | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ("approved") | `gateStatus` widens from `pending`/`rejected` to every status value, so the four new status states render through the card the design already ships and add no markup at all. |
| A8.4b | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ("approved") | Four `statusMap` entries (spec §3 rows 2, 3b, 5c, 8): `check-email`, `verify-expired`, `reset-expired`, `unavailable`, each with the grey `headStyle` `pending` and `rejected` already use, byte for byte. RECORDED LIMIT: "Send it again" re-posts the sign-up, and for a visitor who arrived by SIGNING IN as an unverified account the password in state is empty — the API's uniform 202 still answers, but no new link is issued; the card's own body already says to use the same email and password. |
| A8.5 | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ("approved") | The form values, setters and submits for the five new cards, plus `goSignOut` for the answer card's footer link. Each mirrors the design's own `form`/`setEmail`/`setPw`/`signIn` trio, returns the adapter's promise, and renders the SERVER's own message. The client checks only that both fields are present and the two passwords match — before the request, so a mistyped confirmation cannot burn a single-use token; strength is the server's (zxcvbn ≥ 3, HIBP). `TOKEN_INVALID` is `app/api/auth.py`'s own code, and all three consume paths raise the same `TokenInvalid`. |
| A8.6 | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ("approved") | `submitApply` posts `POST /api/applications` through the adapter, mapping the prototype's short field names to the API's (`vin_member_id`, `school_year`, `license_state`). The design's own required-field check above it is untouched and still runs first; with no adapter the fixture transition to the pending card is exactly what it was. |
| A8.7 | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ("approved") | The five form cards' markup (`gateSignup`, `gateForgot`, `gateReset`, `gateInvite`, `gateAnswer`), appended in the gate's right column after the existing `gateStatus` block. Every element is the sign-in or apply card's — shell, band header, `<label>`/`<span>`/`<input>`, the grey left-bordered error box, the 48 px primary button, the 14 px centred footer line — with only bindings and text changed; `design-amendments.test.ts` proves every `style` value already appears on the design's own gate card and (A1's two ruled declarations aside) on the pristine one. The four status states get no block at all. |
| A8.8a | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ("approved") | `startGate`'s enum in the escaped `data-props` JSON grows from A5.6's four values to all thirteen, which is how the reference reaches each new state through `?props=`. |
| A8.8b | 2026-09-08 | account screens composed from the V3 gate card — John, 2026-09-08 ("approved") | One entry in the escaped `data-props` JSON, spliced immediately after `me`: `startNotice`, a text prop defaulting to `""`. Four account outcomes end on the sign-in card with a notice, and the reference has no API to produce the outcome — so the oracle hands it the notice through the design's own prop mechanism. `app.setup.js` declares it too (`app-generated.test.ts` requires the design's props to be declared); the app never passes it. |
